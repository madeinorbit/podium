{
  "cell": "A1C",
  "harness": "claude",
  "cwd": "/tmp/pod-2918/probes/claude-a1c",
<<<<<<< HEAD
  "at": "2026-08-27T04:43:11.647Z",
  "pin": {
    "cell": "A1C",
    "at": "2026-08-27T04:43:11.684Z",
=======
  "at": "2026-08-27T03:31:01.896Z",
  "pin": {
    "cell": "A1C",
    "at": "2026-08-27T03:31:01.924Z",
>>>>>>> 73b46bd2b (Record Claude current-pin redrive)
    "sourceRoot": "/tmp/pod-2918/source-40c",
    "checkoutSha": "40c198eae5987da8cfa81ac20582ad9161d8270e",
    "serverSha": "40c198eae5987da8cfa81ac20582ad9161d8270e",
    "daemonSha": "40c198eae5987da8cfa81ac20582ad9161d8270e",
    "web": {
      "wireSchemaDigest": "986ebf5e8e57820c",
      "wireVersion": 2,
<<<<<<< HEAD
      "builtAt": "2026-08-27T04:30:53.133Z",
=======
      "builtAt": "2026-08-27T01:52:00.954Z",
>>>>>>> 73b46bd2b (Record Claude current-pin redrive)
      "appVersion": "dev+40c198e",
      "sourceSha": "40c198e",
      "bundleVersion": "bundle+BN6ReJPR"
    },
<<<<<<< HEAD
    "serverPid": "1185151",
    "daemonPid": "1185204",
=======
    "serverPid": "735180",
    "daemonPid": "789168",
>>>>>>> 73b46bd2b (Record Claude current-pin redrive)
    "serverAlive": true,
    "daemonAlive": true,
    "serverCwd": "/tmp/pod-2918/source-40c",
    "daemonCwd": "/tmp/pod-2918/source-40c",
    "freeMemory": {
      "MemTotal": "12242288 kB",
<<<<<<< HEAD
      "MemAvailable": "5141716 kB",
      "SwapFree": "37207600 kB"
=======
      "MemAvailable": "5103156 kB",
      "SwapFree": "36703240 kB"
>>>>>>> 73b46bd2b (Record Claude current-pin redrive)
    },
    "forbiddenOverrides": {
      "PODIUM_STATE_DIR": null,
      "PODIUM_AGENT_HOME": null,
      "ABDUCO_SOCKET_DIR": null,
      "TMUX_TMPDIR": null,
      "PODIUM_WEB_DIR": null
    }
  },
  "verdict": "FAIL",
<<<<<<< HEAD
  "summary": "dead-session send was accepted as queued, but its assistant needle did not arrive after queue-triggered resurrection",
  "control": {
    "fired": true,
    "what": "a baseline prompt and the exact Claude child PID appearing before death",
    "detail": "baseline user=true; baseline reply=true; agentPid=1186968"
  },
  "evidence": [
    "BASELINE          {\"needle\":\"P2874-A1C-CONTROL-MTB1FW3V\",\"sent\":{\"result\":{\"data\":{\"ok\":true,\"disposition\":\"delivered\"}}},\"user\":{\"ok\":true,\"ms\":1148,\"items\":[{\"role\":\"user\",\"text\":\"Reply with exactly this word and nothing else: P2874-A1C-CONTROL-MTB1FW3V. Do not use tools.\",\"t",
    "BEFORE MAP        [{\"pid\":1186968,\"ppid\":1186967,\"cwd\":\"/tmp/pod-2918/probes/claude-a1c\",\"cmd\":\"/home/mgw/.local/bin/claude --append-system-prompt This project uses Podium's issue tracker. You have a `podium issue` CLI. Run `podium issue prime` for your current issue and ready work. Discovered work that can ship separately becomes a top-level issue plus a `discovered-from` dependency; it lands in Proposed automatically, so do not claim or stage it. Decomposition required for your deliverable becomes an internal sub-issue under the current issue; blocking adjacent work is also a blocking sub-issue. Write `--description` as 1–3 plain, context-free sentences for the human and put technical detail/instructions in `--brief`. [spec:SP-6144] `podium issue ready` lists unblocked work; `podium issue claim`/`close` as you go. Nothing advances an issue for you: an issue you are actively working must never sit in `backlog` — set the stage yourself with `podium issue update --id <id> --stage planning|in_progress|review` as the work moves. Note that creating or retitling an issue leaves it in `backlog`; only `claim` sets `in_progress`. Editing an issue outside your assigned one needs `--outside-scope`. Never reuse an existing issue for something completely different — new work gets a new issue or sub-issue. Switch yourself to it only on the human's push; otherwise file it for another agent. A session on a DRAFT may join the issue that covers its work: `podium issue attach --id <issue>`. Once on a real issue you cannot reattach elsewhere — for a new piece of work you move onto, use --spinoff/--subissue below. When you commit on a SHARED branch (main or a long-lived project branch), add a trailer line `Podium-Issue: <your issue ref>` to the commit message so the commit stays attributable to your issue. ",
    "CHILD PID         1186968",
    "CHILD CMD         /home/mgw/.local/bin/claude --append-system-prompt This project uses Podium's issue tracker. You have a `podium issue` CLI. Run `podium issue prime` for your current issue and ready work. Discovered work that can ship separately becomes a top-level issue plus a `discovered-from` dependency; it lands in Proposed automatically, so do not claim or stage it. Decomposition required for your deliverable becomes an internal sub-issue under the current issue; blocking adjacent work is also a blocking sub-issue. Write `--description` as 1–3 plain, context-free sentences for the human and put technical detail/instructions in `--brief`. [spec:SP-6144] `podium issue ready` lists unblocked work; `podium issue claim`/`close` as you go. Nothing advances an issue for you: an issue you are actively working must never sit in `backlog` — set the stage yourself with `podium issue update --id <id> --stage planning|in_progress|review` as the work moves. Note that creating or retitling an issue leaves it in `backlog`; only `claim` sets `in_progress`. Editing an issue outside your assigned one needs `--outside-scope`. Never reuse an existing issue for something completely different — new work gets a new issue or sub-issue. Switch yourself to it only on the human's push; otherwise file it for another agent. A session on a DRAFT may join the issue that covers its work: `podium issue attach --id <issue>`. Once on a real issue you cannot reattach elsewhere — for a new piece of work you move onto, use --spinoff/--subissue below. When you commit on a SHARED branch (main or a long-lived project branch), add a trailer line `Podium-Issue: <your issue ref>` to the commit message so the commit stays attributable to your issue. New work while on an issue — litmus: could the current issue close with it untouched? Yes → spin-off: `attach --spinoff \"<title>\" --confirm-rehome` (top-level + discovered-from edge). No (current issue cannot ship without it) → decomposition: `attach --subissue \"<title>\" --confirm-rehome`. A native subagent must not self-attach; its parent attaches it. Otherwise, file it for another agent. If you discover something another issue's agent should know (a fix to merge, a conflict, a dependency), send it mail: `podium issue mail send <id> --body \"…\"` — it is delivered to whoever works that issue. Any reviewable deliverable you produce for your issue (UX shots, mockups, concept docs, HTML/MD proposals) must ALSO be attached to the issue so it shows in its sidebar: save it under your issue worktree (a scratchpad or /tmp path is outside it and is refused), then `podium issue artifact <id> --add <path> --title \"…\"`. Adding COPIES the bytes to the server, so the artifact keeps working uncommitted and outlives the worktree — never commit a screenshot or a scratch doc just to attach it, and delete the file after adding it if it does not belong in the repo. For a terminal screenshot captured in the current issue session checkout, use `--terminal-evidence` from that checkout; it is an explicit acknowledgement and accepts raster images only, never raw scrollback or terminal text. Redact text into the owning worktree and attach it normally. Re-add each significant iteration; the issue artifact list is how the human finds and reviews visual work later, even after the chat scrolls away. When you finish a turn and there are natural next actions the user might pick, offer them: `podium offer --message \"…\" --action \"Label::prompt to send when clicked\"` (repeat --action — prefer 2–3, up to 6 only when the decision genuinely branches that wide; `podium offer clear` to remove). Write the offer to be judged in five seconds, cold, alongside many others: the FIRST LINE of --message becomes the card headline — state the outcome as done (\"Login screen ready to merge\"), never the activity (\"I've been working on…\"); at most two more lines add where things stand and the one thing to judge. One decision per offer — a second topic is a discovered issue or the next turn's offer, never a \"by the way\". Action labels are imperative, ≤3 words, recommended action FIRST (it renders as the primary button). An action that only makes sense WITH an explanation (send back, request changes) must use `--action-input \"Label::prompt\"` instead — the UI collects the user's feedback and appends it to the prompt. Attach the evidence: `--artifact <path>` names issue artifacts (added via `podium issue artifact --add` the same turn) that render as thumbnails so the user can judge without opening the session. Curate review evidence explicitly with repeated `--artifact <path>` in priority order: put the single best review target first (including an interactive HTML concept when it carries the interaction), then supporting screenshots. Offer cards render at most three artifact items, so choose the three most useful rather than every frame. Failures are offers too — cause, fix, decision, matter-of-fact; no apologies. The buttons show under the chat (and, on mobile, as a card in the Tray); a user turn clears the offer, and so does your own next turn (a stale offer self-clears once the conversation moves on). When you move your issue to `review`, ALWAYS post an offer naming the next steps (merge via --action, send back via --action-input, …) — review-ready work surfaces ONLY through your offer, the stage alone renders nothing. Before posting, test it: reading only the first line and the buttons, does the user know (a) what happened and (b) what to decide? Issue and session titles: 3–5 words naming the thing, not the activity. No \"Implement\"/\"Complete\"/\"Investigate\" openers. Only bugs may lead with \"Bug:\". --settings /home/mgw/.local/state/podium/p2918/hooks/8c520a8d-6568-4538-b8fb-bbd56ba1973c.json",
    "KILL              SIGKILL sent=true",
    "CHILD GONE        true",
    "DEAD SEND         {\"ok\":true,\"queued\":true,\"disposition\":\"queued\"}",
    "AFTER SEND STATUS {\"sessionId\":\"8c520a8d-6568-4538-b8fb-bbd56ba1973c\",\"agentKind\":\"claude-code\",\"harness\":\"claude-code\",\"driverId\":\"claude-pty\",\"requestedDriverId\":null,\"status\":\"exited\",\"phase\":\"idle\",\"machine\":\"flatblock\",\"model\":\"claude-opus-5\",\"effort\":\"high\",\"contextUsageP",
    "TYPED REFUSAL     false",
    "SILENT ACCEPT     true",
    "RESURRECTED REPLY false in 120036ms",
    "AFTER REPLY STATUS {\"sessionId\":\"8c520a8d-6568-4538-b8fb-bbd56ba1973c\",\"agentKind\":\"claude-code\",\"harness\":\"claude-code\",\"driverId\":\"claude-pty\",\"requestedDriverId\":null,\"status\":\"exited\",\"phase\":\"idle\",\"machine\":\"flatblock\",\"model\":\"claude-opus-5\",\"effort\":\"high\",\"contextUsageP"
  ],
  "data": {
    "sid": "8c520a8d-6568-4538-b8fb-bbd56ba1973c",
    "dead": "P2918-A1C-DEAD-MTB1GNCU",
=======
  "summary": "dead-session send was accepted without a typed refusal",
  "control": {
    "fired": true,
    "what": "a baseline prompt and the exact Claude child PID appearing before death",
    "detail": "baseline user=true; baseline reply=true; agentPid=1023334"
  },
  "evidence": [
    "BASELINE          {\"needle\":\"P2874-A1C-CONTROL-MTAYURM7\",\"sent\":{\"result\":{\"data\":{\"ok\":true,\"disposition\":\"delivered\"}}},\"user\":{\"ok\":true,\"ms\":543,\"items\":[{\"role\":\"user\",\"text\":\"Reply with exactly this word and nothing else: P2874-A1C-CONTROL-MTAYURM7. Do not use tools.\",\"ts",
    "BEFORE MAP        [{\"pid\":1023334,\"ppid\":1023333,\"cwd\":\"/tmp/pod-2918/probes/claude-a1c\",\"cmd\":\"/home/mgw/.local/bin/claude --append-system-prompt This project uses Podium's issue tracker. You have a `podium issue` CLI. Run `podium issue prime` for your current issue and ready work. Discovered work that can ship separately becomes a top-level issue plus a `discovered-from` dependency; it lands in Proposed automatically, so do not claim or stage it. Decomposition required for your deliverable becomes an internal sub-issue under the current issue; blocking adjacent work is also a blocking sub-issue. Write `--description` as 1–3 plain, context-free sentences for the human and put technical detail/instructions in `--brief`. [spec:SP-6144] `podium issue ready` lists unblocked work; `podium issue claim`/`close` as you go. Nothing advances an issue for you: an issue you are actively working must never sit in `backlog` — set the stage yourself with `podium issue update --id <id> --stage planning|in_progress|review` as the work moves. Note that creating or retitling an issue leaves it in `backlog`; only `claim` sets `in_progress`. Editing an issue outside your assigned one needs `--outside-scope`. Never reuse an existing issue for something completely different — new work gets a new issue or sub-issue. Switch yourself to it only on the human's push; otherwise file it for another agent. A session on a DRAFT may join the issue that covers its work: `podium issue attach --id <issue>`. Once on a real issue you cannot reattach elsewhere — for a new piece of work you move onto, use --spinoff/--subissue below. When you commit on a SHARED branch (main or a long-lived project branch), add a trailer line `Podium-Issue: <your issue ref>` to the commit message so the commit stays attributable to your issue. ",
    "CHILD PID         1023334",
    "CHILD CMD         /home/mgw/.local/bin/claude --append-system-prompt This project uses Podium's issue tracker. You have a `podium issue` CLI. Run `podium issue prime` for your current issue and ready work. Discovered work that can ship separately becomes a top-level issue plus a `discovered-from` dependency; it lands in Proposed automatically, so do not claim or stage it. Decomposition required for your deliverable becomes an internal sub-issue under the current issue; blocking adjacent work is also a blocking sub-issue. Write `--description` as 1–3 plain, context-free sentences for the human and put technical detail/instructions in `--brief`. [spec:SP-6144] `podium issue ready` lists unblocked work; `podium issue claim`/`close` as you go. Nothing advances an issue for you: an issue you are actively working must never sit in `backlog` — set the stage yourself with `podium issue update --id <id> --stage planning|in_progress|review` as the work moves. Note that creating or retitling an issue leaves it in `backlog`; only `claim` sets `in_progress`. Editing an issue outside your assigned one needs `--outside-scope`. Never reuse an existing issue for something completely different — new work gets a new issue or sub-issue. Switch yourself to it only on the human's push; otherwise file it for another agent. A session on a DRAFT may join the issue that covers its work: `podium issue attach --id <issue>`. Once on a real issue you cannot reattach elsewhere — for a new piece of work you move onto, use --spinoff/--subissue below. When you commit on a SHARED branch (main or a long-lived project branch), add a trailer line `Podium-Issue: <your issue ref>` to the commit message so the commit stays attributable to your issue. New work while on an issue — litmus: could the current issue close with it untouched? Yes → spin-off: `attach --spinoff \"<title>\" --confirm-rehome` (top-level + discovered-from edge). No (current issue cannot ship without it) → decomposition: `attach --subissue \"<title>\" --confirm-rehome`. A native subagent must not self-attach; its parent attaches it. Otherwise, file it for another agent. If you discover something another issue's agent should know (a fix to merge, a conflict, a dependency), send it mail: `podium issue mail send <id> --body \"…\"` — it is delivered to whoever works that issue. Any reviewable deliverable you produce for your issue (UX shots, mockups, concept docs, HTML/MD proposals) must ALSO be attached to the issue so it shows in its sidebar: save it under your issue worktree (a scratchpad or /tmp path is outside it and is refused), then `podium issue artifact <id> --add <path> --title \"…\"`. Adding COPIES the bytes to the server, so the artifact keeps working uncommitted and outlives the worktree — never commit a screenshot or a scratch doc just to attach it, and delete the file after adding it if it does not belong in the repo. For a terminal screenshot captured in the current issue session checkout, use `--terminal-evidence` from that checkout; it is an explicit acknowledgement and accepts raster images only, never raw scrollback or terminal text. Redact text into the owning worktree and attach it normally. Re-add each significant iteration; the issue artifact list is how the human finds and reviews visual work later, even after the chat scrolls away. When you finish a turn and there are natural next actions the user might pick, offer them: `podium offer --message \"…\" --action \"Label::prompt to send when clicked\"` (repeat --action — prefer 2–3, up to 6 only when the decision genuinely branches that wide; `podium offer clear` to remove). Write the offer to be judged in five seconds, cold, alongside many others: the FIRST LINE of --message becomes the card headline — state the outcome as done (\"Login screen ready to merge\"), never the activity (\"I've been working on…\"); at most two more lines add where things stand and the one thing to judge. One decision per offer — a second topic is a discovered issue or the next turn's offer, never a \"by the way\". Action labels are imperative, ≤3 words, recommended action FIRST (it renders as the primary button). An action that only makes sense WITH an explanation (send back, request changes) must use `--action-input \"Label::prompt\"` instead — the UI collects the user's feedback and appends it to the prompt. Attach the evidence: `--artifact <path>` names issue artifacts (added via `podium issue artifact --add` the same turn) that render as thumbnails so the user can judge without opening the session. Curate review evidence explicitly with repeated `--artifact <path>` in priority order: put the single best review target first (including an interactive HTML concept when it carries the interaction), then supporting screenshots. Offer cards render at most three artifact items, so choose the three most useful rather than every frame. Failures are offers too — cause, fix, decision, matter-of-fact; no apologies. The buttons show under the chat (and, on mobile, as a card in the Tray); a user turn clears the offer, and so does your own next turn (a stale offer self-clears once the conversation moves on). When you move your issue to `review`, ALWAYS post an offer naming the next steps (merge via --action, send back via --action-input, …) — review-ready work surfaces ONLY through your offer, the stage alone renders nothing. Before posting, test it: reading only the first line and the buttons, does the user know (a) what happened and (b) what to decide? Issue and session titles: 3–5 words naming the thing, not the activity. No \"Implement\"/\"Complete\"/\"Investigate\" openers. Only bugs may lead with \"Bug:\". --settings /home/mgw/.local/state/podium/p2918/hooks/5c047d36-64f9-4b16-9262-6c7f1e9cd059.json",
    "KILL              SIGKILL sent=true",
    "CHILD GONE        true",
    "DEAD SEND         {\"ok\":true,\"queued\":true,\"disposition\":\"queued\"}",
    "TYPED REFUSAL     false",
    "SILENT ACCEPT     true"
  ],
  "data": {
    "sid": "5c047d36-64f9-4b16-9262-6c7f1e9cd059",
    "dead": "P2918-A1C-DEAD-MTAYV6NU",
>>>>>>> 73b46bd2b (Record Claude current-pin redrive)
    "payload": {
      "ok": true,
      "queued": true,
      "disposition": "queued"
    },
    "typed": false,
    "delivered": true,
<<<<<<< HEAD
    "revivedReply": false,
    "revivedReplyMs": 120036,
    "afterSend": {
      "sessionId": "8c520a8d-6568-4538-b8fb-bbd56ba1973c",
      "agentKind": "claude-code",
      "harness": "claude-code",
      "driverId": "claude-pty",
      "requestedDriverId": null,
      "status": "exited",
      "phase": "idle",
      "machine": "flatblock",
      "model": "claude-opus-5",
      "effort": "high",
      "contextUsagePercent": null,
      "account": "native:claude-code:6623e9a1ae253fe9ebb48a7adf7de0d0fefdb570042366c63f25b9b0a7864241",
      "error": null,
      "draft": false,
      "nativeSubagentCount": 0,
      "nativeSubagents": [],
      "subagents": [],
      "issue": null,
      "commits": [],
      "files": [],
      "unackedMessages": 0
    },
    "afterReply": {
      "sessionId": "8c520a8d-6568-4538-b8fb-bbd56ba1973c",
      "agentKind": "claude-code",
      "harness": "claude-code",
      "driverId": "claude-pty",
      "requestedDriverId": null,
      "status": "exited",
      "phase": "idle",
      "machine": "flatblock",
      "model": "claude-opus-5",
      "effort": "high",
      "contextUsagePercent": null,
      "account": "native:claude-code:6623e9a1ae253fe9ebb48a7adf7de0d0fefdb570042366c63f25b9b0a7864241",
      "error": null,
      "draft": false,
      "nativeSubagentCount": 0,
      "nativeSubagents": [],
      "subagents": [],
      "issue": null,
      "commits": [],
      "files": [],
      "unackedMessages": 0
    },
    "childPid": 1186968,
    "childCmd": "/home/mgw/.local/bin/claude --append-system-prompt This project uses Podium's issue tracker. You have a `podium issue` CLI. Run `podium issue prime` for your current issue and ready work. Discovered work that can ship separately becomes a top-level issue plus a `discovered-from` dependency; it lands in Proposed automatically, so do not claim or stage it. Decomposition required for your deliverable becomes an internal sub-issue under the current issue; blocking adjacent work is also a blocking sub-issue. Write `--description` as 1–3 plain, context-free sentences for the human and put technical detail/instructions in `--brief`. [spec:SP-6144] `podium issue ready` lists unblocked work; `podium issue claim`/`close` as you go. Nothing advances an issue for you: an issue you are actively working must never sit in `backlog` — set the stage yourself with `podium issue update --id <id> --stage planning|in_progress|review` as the work moves. Note that creating or retitling an issue leaves it in `backlog`; only `claim` sets `in_progress`. Editing an issue outside your assigned one needs `--outside-scope`. Never reuse an existing issue for something completely different — new work gets a new issue or sub-issue. Switch yourself to it only on the human's push; otherwise file it for another agent. A session on a DRAFT may join the issue that covers its work: `podium issue attach --id <issue>`. Once on a real issue you cannot reattach elsewhere — for a new piece of work you move onto, use --spinoff/--subissue below. When you commit on a SHARED branch (main or a long-lived project branch), add a trailer line `Podium-Issue: <your issue ref>` to the commit message so the commit stays attributable to your issue. New work while on an issue — litmus: could the current issue close with it untouched? Yes → spin-off: `attach --spinoff \"<title>\" --confirm-rehome` (top-level + discovered-from edge). No (current issue cannot ship without it) → decomposition: `attach --subissue \"<title>\" --confirm-rehome`. A native subagent must not self-attach; its parent attaches it. Otherwise, file it for another agent. If you discover something another issue's agent should know (a fix to merge, a conflict, a dependency), send it mail: `podium issue mail send <id> --body \"…\"` — it is delivered to whoever works that issue. Any reviewable deliverable you produce for your issue (UX shots, mockups, concept docs, HTML/MD proposals) must ALSO be attached to the issue so it shows in its sidebar: save it under your issue worktree (a scratchpad or /tmp path is outside it and is refused), then `podium issue artifact <id> --add <path> --title \"…\"`. Adding COPIES the bytes to the server, so the artifact keeps working uncommitted and outlives the worktree — never commit a screenshot or a scratch doc just to attach it, and delete the file after adding it if it does not belong in the repo. For a terminal screenshot captured in the current issue session checkout, use `--terminal-evidence` from that checkout; it is an explicit acknowledgement and accepts raster images only, never raw scrollback or terminal text. Redact text into the owning worktree and attach it normally. Re-add each significant iteration; the issue artifact list is how the human finds and reviews visual work later, even after the chat scrolls away. When you finish a turn and there are natural next actions the user might pick, offer them: `podium offer --message \"…\" --action \"Label::prompt to send when clicked\"` (repeat --action — prefer 2–3, up to 6 only when the decision genuinely branches that wide; `podium offer clear` to remove). Write the offer to be judged in five seconds, cold, alongside many others: the FIRST LINE of --message becomes the card headline — state the outcome as done (\"Login screen ready to merge\"), never the activity (\"I've been working on…\"); at most two more lines add where things stand and the one thing to judge. One decision per offer — a second topic is a discovered issue or the next turn's offer, never a \"by the way\". Action labels are imperative, ≤3 words, recommended action FIRST (it renders as the primary button). An action that only makes sense WITH an explanation (send back, request changes) must use `--action-input \"Label::prompt\"` instead — the UI collects the user's feedback and appends it to the prompt. Attach the evidence: `--artifact <path>` names issue artifacts (added via `podium issue artifact --add` the same turn) that render as thumbnails so the user can judge without opening the session. Curate review evidence explicitly with repeated `--artifact <path>` in priority order: put the single best review target first (including an interactive HTML concept when it carries the interaction), then supporting screenshots. Offer cards render at most three artifact items, so choose the three most useful rather than every frame. Failures are offers too — cause, fix, decision, matter-of-fact; no apologies. The buttons show under the chat (and, on mobile, as a card in the Tray); a user turn clears the offer, and so does your own next turn (a stale offer self-clears once the conversation moves on). When you move your issue to `review`, ALWAYS post an offer naming the next steps (merge via --action, send back via --action-input, …) — review-ready work surfaces ONLY through your offer, the stage alone renders nothing. Before posting, test it: reading only the first line and the buttons, does the user know (a) what happened and (b) what to decide? Issue and session titles: 3–5 words naming the thing, not the activity. No \"Implement\"/\"Complete\"/\"Investigate\" openers. Only bugs may lead with \"Bug:\". --settings /home/mgw/.local/state/podium/p2918/hooks/8c520a8d-6568-4538-b8fb-bbd56ba1973c.json",
=======
    "childPid": 1023334,
    "childCmd": "/home/mgw/.local/bin/claude --append-system-prompt This project uses Podium's issue tracker. You have a `podium issue` CLI. Run `podium issue prime` for your current issue and ready work. Discovered work that can ship separately becomes a top-level issue plus a `discovered-from` dependency; it lands in Proposed automatically, so do not claim or stage it. Decomposition required for your deliverable becomes an internal sub-issue under the current issue; blocking adjacent work is also a blocking sub-issue. Write `--description` as 1–3 plain, context-free sentences for the human and put technical detail/instructions in `--brief`. [spec:SP-6144] `podium issue ready` lists unblocked work; `podium issue claim`/`close` as you go. Nothing advances an issue for you: an issue you are actively working must never sit in `backlog` — set the stage yourself with `podium issue update --id <id> --stage planning|in_progress|review` as the work moves. Note that creating or retitling an issue leaves it in `backlog`; only `claim` sets `in_progress`. Editing an issue outside your assigned one needs `--outside-scope`. Never reuse an existing issue for something completely different — new work gets a new issue or sub-issue. Switch yourself to it only on the human's push; otherwise file it for another agent. A session on a DRAFT may join the issue that covers its work: `podium issue attach --id <issue>`. Once on a real issue you cannot reattach elsewhere — for a new piece of work you move onto, use --spinoff/--subissue below. When you commit on a SHARED branch (main or a long-lived project branch), add a trailer line `Podium-Issue: <your issue ref>` to the commit message so the commit stays attributable to your issue. New work while on an issue — litmus: could the current issue close with it untouched? Yes → spin-off: `attach --spinoff \"<title>\" --confirm-rehome` (top-level + discovered-from edge). No (current issue cannot ship without it) → decomposition: `attach --subissue \"<title>\" --confirm-rehome`. A native subagent must not self-attach; its parent attaches it. Otherwise, file it for another agent. If you discover something another issue's agent should know (a fix to merge, a conflict, a dependency), send it mail: `podium issue mail send <id> --body \"…\"` — it is delivered to whoever works that issue. Any reviewable deliverable you produce for your issue (UX shots, mockups, concept docs, HTML/MD proposals) must ALSO be attached to the issue so it shows in its sidebar: save it under your issue worktree (a scratchpad or /tmp path is outside it and is refused), then `podium issue artifact <id> --add <path> --title \"…\"`. Adding COPIES the bytes to the server, so the artifact keeps working uncommitted and outlives the worktree — never commit a screenshot or a scratch doc just to attach it, and delete the file after adding it if it does not belong in the repo. For a terminal screenshot captured in the current issue session checkout, use `--terminal-evidence` from that checkout; it is an explicit acknowledgement and accepts raster images only, never raw scrollback or terminal text. Redact text into the owning worktree and attach it normally. Re-add each significant iteration; the issue artifact list is how the human finds and reviews visual work later, even after the chat scrolls away. When you finish a turn and there are natural next actions the user might pick, offer them: `podium offer --message \"…\" --action \"Label::prompt to send when clicked\"` (repeat --action — prefer 2–3, up to 6 only when the decision genuinely branches that wide; `podium offer clear` to remove). Write the offer to be judged in five seconds, cold, alongside many others: the FIRST LINE of --message becomes the card headline — state the outcome as done (\"Login screen ready to merge\"), never the activity (\"I've been working on…\"); at most two more lines add where things stand and the one thing to judge. One decision per offer — a second topic is a discovered issue or the next turn's offer, never a \"by the way\". Action labels are imperative, ≤3 words, recommended action FIRST (it renders as the primary button). An action that only makes sense WITH an explanation (send back, request changes) must use `--action-input \"Label::prompt\"` instead — the UI collects the user's feedback and appends it to the prompt. Attach the evidence: `--artifact <path>` names issue artifacts (added via `podium issue artifact --add` the same turn) that render as thumbnails so the user can judge without opening the session. Curate review evidence explicitly with repeated `--artifact <path>` in priority order: put the single best review target first (including an interactive HTML concept when it carries the interaction), then supporting screenshots. Offer cards render at most three artifact items, so choose the three most useful rather than every frame. Failures are offers too — cause, fix, decision, matter-of-fact; no apologies. The buttons show under the chat (and, on mobile, as a card in the Tray); a user turn clears the offer, and so does your own next turn (a stale offer self-clears once the conversation moves on). When you move your issue to `review`, ALWAYS post an offer naming the next steps (merge via --action, send back via --action-input, …) — review-ready work surfaces ONLY through your offer, the stage alone renders nothing. Before posting, test it: reading only the first line and the buttons, does the user know (a) what happened and (b) what to decide? Issue and session titles: 3–5 words naming the thing, not the activity. No \"Implement\"/\"Complete\"/\"Investigate\" openers. Only bugs may lead with \"Bug:\". --settings /home/mgw/.local/state/podium/p2918/hooks/5c047d36-64f9-4b16-9262-6c7f1e9cd059.json",
>>>>>>> 73b46bd2b (Record Claude current-pin redrive)
    "killed": true,
    "gone": true,
    "killError": ""
  }
}
