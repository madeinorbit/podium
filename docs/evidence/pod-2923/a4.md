# POD-2923 — Codex A4a/A4b re-drive

## Pin and rig

The named instance was `p2924`; `instance:default` was not touched. The
successful re-drive was spawned at `2026-08-27 09:11:36 CEST` from full commit
`ac5dcef3f5ed6b93e896576a8c5b8e3db877db65`. At the independent pre-drive
verification (`2026-08-27 09:11:08 CEST`), `server.sha` and `daemon.sha` were
that full commit and `apps/web/dist/podium-build.json` reported `sourceSha`
`ac5dcef`; the verifier found exactly one `p2924` daemon and the server on
port `19894`. The daemon was restarted with the already-installed
`codex-cli 0.149.1`, which is inside the manifest's `>=0.147 <0.150` range.

## Positive control

The live A4 probe initially refused because its stream watcher saw zero
transcript deltas while the second native viewer had parked the chat send.
That refusal was not scored. A canonical `sessions.transcriptRead` at
`2026-08-27 09:19 CEST` independently showed the control turn had completed:
the user prompt, assistant response, two Bash attempts (the first was the
host sandbox failure and the second succeeded), and the final assistant
response were durable. The successful command created exactly one marker
file. This fires the positive control: the turn ran and the tool path was
alive, even though no permission ask was raised.

The named Codex configuration contained `approvals_reviewer = "auto_review"`
and trusted the probe worktree. The Codex app-server therefore reviewed the
Bash request itself. `interactions.list` remained empty; the probe observed no
permission card in chat and no permission ask in the native terminal. The
native viewer attached with `outputSeen=false` and `4,950` initial bytes, but
there was no ask to resolve.

## Cell scoring

### A4a — BLOCKED (instrument)

- Chat card: not observed; Codex auto-reviewed the request before Podium had
  an ask to enumerate.
- Same ask in terminal: not observed; no ask existed to mirror, so this half
  was not evaluated as a terminal-surface failure.
- Answering resolves both: not evaluated; there was no answerable interaction
  to answer.

The cell is `BLOCKED`, not `FAIL`: the positive control fired, but the Codex
instrument's `auto_review` posture prevented the permission path from being
handed to Podium. This is a fresh reading, not inheritance of the old blocked
row at `6685c59`.

### A4b — BLOCKED (instrument)

The first-answer control could not fire because no permission ask existed.
No first or second `interactions.answer` was sent. Consequently, the typed
second-answer error and the no-double-action clause were not evaluated. The
cell is `BLOCKED`, not `FAIL`, for the same observed Codex auto-review limit.

The rig was stopped at `2026-08-27 09:19:33 CEST`; no Podium offer or OAuth
operation was used.

## Earlier diagnostic attempt

At `2026-08-27 09:03:24 CEST`, before switching the daemon to the installed
supported release, the rig resolved the system Codex `0.150.1` as outside the
app-server range and selected `generic-pty`; its positive control did not fire.
That attempt is explicitly excluded from both cell verdicts.
