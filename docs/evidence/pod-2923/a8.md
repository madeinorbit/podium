# POD-2923 — Codex A8 logged-out spawn

## Pin and positive controls

The drive used named instance `p2924`, never `instance:default`, and spawned at
`2026-08-27 09:34:46 CEST`. The full server and daemon spawn pin was
`c7d84065703a8bf6a0c7105b37c8d665c7be5db3`. The served web bundle reported
`sourceSha=ac5dcef`; it was reused from an exact `ac5dcef3f5ed6b93e896576a8c5b8e3db877db65`
worktree after confirming `apps/web` was unchanged through the current pin.
The independent pre-drive verifier at `2026-08-27 09:34:26 CEST` found one
daemon, the server on `:19894`, clean product files, and the expected arm.
Codex was the preinstalled `codex-cli 0.149.1` release.

Three controls fired:

- The logged-in control session bound `codex-app-server/server` before the
  credential was moved.
- The logged-out session attached with `outputSeen=true` and `14,515` native
  terminal bytes; its product row and account inventory both declared Codex
  logged out.
- After restoring the exact existing credential and restarting the daemon, a
  fresh session again bound `codex-app-server/server`.

## Logged-out half

The existing
`/home/mgw/.local/state/podium/p2924/agent-home/.codex/auth.json` was moved to
the adjacent `.a8-parked` name; no credential was created or rotated. The
logged-out session was
`bc6ba463-7d67-4c98-a41d-ce6e50912126`, with
`driver=generic-pty`, `requested=codex-app-server`, `family=terminal`,
`condition=logged-out`, and `status=live`.

The native Codex screen visibly offered all three login routes:
`Sign in with ChatGPT`, `Sign in with Device Code`, and `Provide your own API
key`. `accounts.list` reported the native Codex account as
`status=not-configured` and `loginRequired=true`; machine inventory reported
Codex 0.149.1 at the supported release path with `login.state=out`. This is the
working login path required by A8, not merely a file-move inference.

## Post-login half

The daemon restart after moving the credential was
`1540209 -> 1548112`; after restoring it, the restart was
`1548112 -> 1550967`. Both restarts recorded the same full server/daemon pin
`c7d84065703a8bf6a0c7105b37c8d665c7be5db3` and used the explicit 0.149.1
binary directory.

The fresh post-login session was
`9b19f70e-9c64-4675-ad4e-7b49ffe74526`; it reported
`driver=codex-app-server`, `family=server`, and `status=live`. The native
viewer attached to that server session, confirming the binding was not just a
requested-driver value. No operator OAuth was performed: the safe post-login
control was restoration of the pre-existing credential.

## Verdict

`A8 PASS`: a logged-out Codex spawn exposed a working native login path, and a
fresh session after restoring the credential landed on the server driver. The
named rig was torn down at `2026-08-27 09:37:07 CEST`; `auth.json` was present
again and `.a8-parked` was absent.
