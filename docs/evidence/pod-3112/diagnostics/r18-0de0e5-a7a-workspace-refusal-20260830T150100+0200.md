# OpenCode A7a exact-tip workspace refusal

At 2026-08-30 15:00 CEST, the reviewed r18 harness produced a **REFUSED, unscored** A7a reading on exact server and daemon product pin `0de0e5b1d4336780b328d15ed0a9cba1eaa873ac`. The untouched served web bundle truthfully reported `sourceSha=deeb58e`; OpenCode `1.18.25` matched SHA-256 `d91e0d33676d0839f7cde87924cd4127ea88c9d6784eea9f009a7d08bdc60eeb`.

The final wholly fresh instance was `p3112-a7a-0de0e5-r3` on ports `21793`, `48793`, and `48794`, with product-derived state, `PODIUM_UPDATE_CHANNEL=edge`, rootless Chromium child libraries, an approved credential symlink, and committed fixture repository `/tmp/pod-3112-a7a-0de0e5-r3/dummy-repo`. The fixture was registered to daemon machine `b4942c80-1746-4ee1-9fc3-533071913927`; no harness-authored `HOME`, `PODIUM_STATE_DIR`, `ABDUCO_SOCKET_DIR`, or `PODIUM_RUNTIME_DRIVER` override was used.

The visible Experimental setting and Driver control selected `opencode-server`. Session `088fc782-c9fa-42f2-9897-57a48d15616d` bound `driverId=requestedDriverId=opencode-server`, `driverFamily=server`, account `native:opencode`, and provider conversation `ses_fad3fc667ffeGI3WMjRU2kEkIu`; its provider process key and PID `258198` were exact to the fixture cwd and instance. The seed prompt appeared exactly once in the Chat transcript and received exactly one reply.

The daemon restarted from PID `255890` to `259861` at the same exact pin and adopted the surviving server-family session and client terminal. On the subsequent browser reload, however, the committed harness could not find the sole registered `dummy-repo` project group within 30 seconds. Before restart the Chat surface retained two normalized seed-marker occurrences while Native reported zero, and the post-restart recall prompt was never sent because the workspace positive control refused first.

This is not a valid PASS or FAIL: the harness could not complete the Native/Chat recall assertions, so no A7a acceptance-ledger row was written and no `a7a-ready` or `a7a-continue` marker was created. Raw temporary evidence hashes were `r18-reading.json=4f65fb22a79367e74b6a7336bfff6e336e05935e320dd36a16474325fdfff319`, `server.log=814ed07ab00afd15d8a986474c161b3c5ad96507ebce57a3929d77d026b6c9bc`, and `daemon.log=fb4a7f0ffa4d96367994fc84a0779c940e0de0bf26ffada2a56f8aff331f2c02`.

Teardown completed at 2026-08-30 15:01 CEST. The isolated server, daemon, surviving attach child, provider process, credential symlink, and all three listeners were removed; A7b was not attempted, and operator/default state plus ports `19797` and `32090` were untouched.
