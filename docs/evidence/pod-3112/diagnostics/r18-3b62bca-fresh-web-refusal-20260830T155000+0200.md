# OpenCode A7a fresh-web workspace refusal

At 2026-08-30 15:49 CEST, the reviewed r18 harness produced a **REFUSED, unscored** A7a reading with server, daemon, and freshly built browser bytes all pinned to integration tip `3b62bca086121b3221cad7d80e9b530dbd4656b7`. The canonical `bun run build` completed under the non-queued `test:heavy` lease and wrote web `sourceSha=3b62bca`, `appVersion=dev+3b62bca`, and `bundleVersion=bundle+DuBNjXbX`; the lease was released before runtime launch.

The wholly fresh instance was `p3112-a7a-3b62bc-r1` on ports `21953`, `48953`, and `48954`, with product-derived state, `PODIUM_UPDATE_CHANNEL=edge`, rootless Chromium child libraries, an approved credential symlink, and committed fixture `/tmp/pod-3112-a7a-3b62bc-r1/dummy-repo`. The fixture was registered to actual isolated daemon machine `762a0ddc-b051-4e8f-b360-9f625e1ff881`; no runtime-driver, state, socket, or home override was authored by the harness.

The visible Experimental setting and Driver control selected `opencode-server`. Session `b3926c16-559a-4579-b4c6-b54ce3ffaf9b` bound `driverId=requestedDriverId=opencode-server`, `driverFamily=server`, account `native:opencode`, and provider conversation `ses_fad12a4d9ffeLnINb85ACAaYio`; provider process key `podium-oc-b3926c16-559a-4579-b4c6-b54ce3ffaf9b` and PID `431903` were exact to the fixture cwd and instance. The seed user and assistant items each appeared exactly once, Chat showed two marker occurrences, but the pre-restart Native surface showed zero.

The daemon restarted from PID `429510` to `433234` at the same exact pin and connected before the harness restart marker was released. On the subsequent browser reload, the harness could not find the sole registered `dummy-repo` project group within 30 seconds, so it could not execute post-restart Native/Chat assertions or recall.

This is not a valid PASS or FAIL: the Native and workspace controls did not both fire and recall never ran, so no A7a acceptance-ledger row was written and neither `a7a-ready` nor `a7a-continue` was created. Raw temporary evidence hashes were `r18-reading.json=654351b04964e66e7a1050a6c51e9929b92dba5160d88564875a0ddd6819c100`, `server.log=ddab8bb47ae1124ee8c3c725e04a6eb71e053c82f7c07f7e4551a25fc582a9fc`, and `daemon.log=87306539e140ae579fb078f03f7b135866eb9e1eaea81be88210c87c8c9cb296`.

Teardown completed at 2026-08-30 15:50 CEST. The isolated server, daemon, provider, attach child, credential symlink, and owned listeners were removed; A7b was not attempted, and operator/default state plus ports `19797` and `32090` were untouched.
