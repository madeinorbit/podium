# POD-3036 Claude SDK vs PTY acceptance

The 2026-08-28 13:00–13:40 CEST `p3036` run is discarded. See
`INVALID-DISCARDED.md`. Do not score it.

No-copy rig: instance `p3036n`, pin `c71b896a9`, TOS=1, explicit
`runtimeContract`. Never copy or parse credential files. Isolated agent-home
must have no `.credentials.json` before every cell.
