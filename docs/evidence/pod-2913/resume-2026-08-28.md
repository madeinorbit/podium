# POD-2913 current-tip review-drive resume

Assembled at 2026-08-28 01:51:01 CEST.

Scope is limited to the nine landed-but-undriven review checks. The requested epic tip is
`b3343f39a68e97f5f2c22072db8c2858fa916722`; the clean evidence checkout remains
`117eca85621b1c46715c3b92f7e3809481626bb3`. No product files, POD-2980 files, or
`docs/plans/pod-1761-results.tsv` / scorer files were changed.

## Append-only result rows

These rows preserve the eight columns required by the shared results file. The rows below are
in this separate artifact because the shared results/scorer files belong to POD-2929.

~~~tsv
what	driver	verdict	commit	control	alone	date	issue
POD-2604 parent boundary 6c6689046 -> dac2f0c09; named parent launch hit File name too long before SessionStart	static boundary / no runtime	BLOCKED parent cannot launch a named instance	dac2f0c0981	no — SessionStart and durable control did not fire	n/a — no instance spawned; default instance fenced	2026-08-28 01:51	POD-2604
POD-2637 parent boundary c1d69321b -> 27f133619; parent predates the named-socket fix and cannot launch the required named arm	static boundary / no runtime	BLOCKED parent cannot launch a named instance	27f1336190	no — terminal-evidence control did not fire	n/a — no instance spawned; default instance fenced	2026-08-28 01:51	POD-2637
POD-2761 parent boundary a841ade74 -> 3c4d9a29; named parent attach arm cannot launch before the socket-path fix	static boundary / no runtime	BLOCKED parent cannot launch a named instance	3c4d9a297	no — SessionStart and switch control did not fire	n/a — no instance spawned; default instance fenced	2026-08-28 01:51	POD-2761
stream in a running turn; durable control 2 frames/2 items, fine watch acquired, 17 preview frames, 16/16 growth transitions, per-row monotonic	opencode-server (server family)	PASS 17 preview frames	b3343f39a68e97f5f2c22072db8c2858fa916722	yes — transcriptDelta 2 frames/2 items; phase idle	yes — only named p2913z8 drive instance and its scratch directory	2026-08-28 01:46	POD-2773
stream in a running turn; durable control 2 frames/2 items, fine watch acquired, 29 preview frames, 28/28 growth transitions, per-row monotonic	grok-acp (server family)	PASS 29 preview frames	b3343f39a68e97f5f2c22072db8c2858fa916722	yes — transcriptDelta 2 frames/2 items; phase idle	yes — sequential arm, no concurrent driver in the probe directory	2026-08-28 01:47	POD-2773
~~~

## Pin and boundary evidence

The POD-2773 runtime used exact source checkout
`/home/mgw/src/podium/.worktrees/issue-1761-agent-runtime` at
`b3343f39a68e97f5f2c22072db8c2858fa916722`.

- Server spawn pin: `b3343f39a68e97f5f2c22072db8c2858fa916722`, pid 3568201.
- Daemon spawn pin: `b3343f39a68e97f5f2c22072db8c2858fa916722`, pid 3568375.
- Named instance: `p2913z8`; probe base `/tmp/pod-2913-z8r5`; scratch directory
  `/tmp/pod-2913-z8r5/repo`. Both processes had the exact source cwd and were stopped by
  the cleanup trap; a post-drive scan found no process carrying `PODIUM_INSTANCE=p2913z8`.
- Web source was inspected at the same tip. The existing dist stamp was
  `sourceSha=e68fbc7`, built 2026-08-23 10:50:51Z. The server warned that this served
  bundle's wire digest differs from the current server. The POD-2773 probe is the supplied
  direct websocket probe and does not load or render the web bundle; this result makes no
  web-UI claim and records the bundle mismatch instead of hiding it.
- Both driver cells joined an already-running turn after 8.5 seconds. Opencode reported
  `driverId=opencode-server`, `fineWatchAcquired=true`, `controlDeltas=2`,
  `controlItems=2`, `previewFrames=17`, first/last chars 1/1455. Grok reported
  `driverId=grok-acp`, `fineWatchAcquired=true`, `controlDeltas=2`,
  `controlItems=2`, `previewFrames=29`, first/last chars 6/699. Neither arm had an
  agent error.

For the three blocked arms, the static boundary and socket ancestry were rechecked at
2026-08-28 01:51:01 CEST:

- POD-2604: parent `6c668904614fc5a79877e5e791634b5c570f019d` has no
  `state.error.detail` propagation in `synthesis.ts`; fix
  `dac2f0c0981ed8865843d13be115e99816b3c3c3` adds it. Current-tip consumers include
  server synthesis/materialization, Grok classification, client chat, and chat transcript
  rendering. Existing current-tip evidence records the parent `create-session: File name
  too long` before its positive control.
- POD-2637: parent `c1d69321b7c1b0626cb0459d4dceb324831b5dac` has zero
  `terminalEvidence` / `terminal-evidence` production symbols; fix
  `27f1336190feb880e1a1f3ef9b79c0472c1a5a09` and the current tip have the terminal
  evidence path and consumers in the CLI, issue client, registry, and artifact service.
  The parent predates the named-socket fix, so no named parent process was started.
- POD-2761: parent `a841ade7407d91e72e946d05ebd281fe59f557b6` samples
  `hasMaster(record.label)` before spawn; fix
  `3c4d9a297b093f0311d9934e86a5a0c910abade8` uses `session.adopted` after spawn,
  and the current daemon consumes that branch. Its named parent arm is blocked by the
  same pre-`ab9d698ab4c75e80c18cfee1f444b709ef588f0d` socket path.
- For all three, `git merge-base --is-ancestor <parent> ab9d698ab...` returned success,
  so this is a launch block, not an invalid product boundary. The default instance was
  free when checked but remains fenced by the coordinator; no default fallback was used.

POD-2773 is a drive issue rather than a product-fix issue: its issue commits are evidence
scripts/README material, so there is no pre-fix product boundary to manufacture. The Grok
quota block was not carried forward; the post-quota current-tip arm produced tokens and
streamed.
