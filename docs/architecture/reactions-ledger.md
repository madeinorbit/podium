# Reactions ledger

> Generated by `bun scripts/reactions-ledger.ts --write`; checked without `--write`.
> The typed source of truth is `apps/server/src/composition/reactions.ts`.

A reaction is admitted only for a semantically asynchronous effect. Atomic caller workflows live in application orchestrators and are documented separately.

| Reaction | Trigger | Durability | Restart replay | Idempotency | Ordering | Retry / failure owner | Principal | Scope invariant | Observability |
|---|---|---|---|---|---|---|---|---|---|
| `settings.feature-cache` | settings.changed | in-memory | none — Startup reads the current durable settings row. | settings revision (safe-repeat) | EventBus listener order within one process. | No retry; the next settings event or process restart refreshes the snapshot. Owner: settings module | system; actor=system; scope=acted-on entity | Read-only derived cache; it performs no attributed write. | settings.changed |
| `sessions.machine-derived-fields` | machine.metadataChanged | in-memory | none — Session load derives machine names from current durable rows. | machineId + machine row revision (coalesce) | Per-machine EventBus order; session publication remains ledger ordered. | The next metadata change or client bootstrap rebuilds the projection. Owner: sessions derived-field maintainer | system; actor=system; scope=acted-on entity | Projection refresh retains every session owner and visibility. | machine.metadataChanged |
| `sessions.feed-cursor` | feed.published | in-memory | none — Client bootstrap reads the durable authority cursor after restart. | feed sequence (coalesce) | Global ledger sequence; cursors only advance monotonically. | The next publication advances to an equal or newer sequence. Owner: session publication coordinator | system; actor=system; scope=acted-on entity | Cursor maintenance is system-derived and performs no human-attributed write. | feed.published |
| `issues.session-derived-projection` | session.listChanged | durable | startup reconcile from durable issue and session rows; re-authorize at apply | issue id + projection value hash (deduplicate) | Session event order; resulting change rows use global ledger sequence. | Issue boot reconciliation and the next session change retry the projection. Owner: issue publisher | system; actor=system; scope=acted-on entity | Derived rows inherit issue ownership and visibility; actor remains system. | session.listChanged, oplog.appended |
| `sessions.auto-continue-settings` | settings.changed | in-memory | none — Live session state is re-seeded and settings are read at boot. | owner + sessionId + settings revision (coalesce) | Settings events are processed synchronously in emission order. | No independent retry; the auto-continue controller owns later turn retries. Owner: sessions auto-continue controller | delegated; live delegation; re-authorize at apply | The session delegation is resolved live and remains bounded by its owner. | settings.changed |
| `attention.issue-mail-nudge` | issue.mailSent | durable | startup reconcile from unread issue mail and durable session inbox rows; re-authorize at apply | mail message id + recipient session id (deduplicate) | Per target session FIFO through the durable inbox. | Session inbox sweep owns retry and dead-lettering. Owner: message delivery service | delegated; live delegation; re-authorize at apply | Routes to the owner of the issue/mail target, never an ambient operator. | issue.mailSent, message delivery attempts |
| `issues.session-derived-maintenance` | issue.sessionDerived | in-memory | none — Boot rebuilds row-derived projections; live-only daemon facts retry on the next event. | event kind + issue/session id + derived payload hash (safe-repeat) | Synchronous EventBus emission order per session; issue writes retain funnel order. | The next session event or boot projection reconciliation is the retry backstop. Owner: issue derived-field maintainer | system; actor=system; scope=acted-on entity | Writes inherit the acted-on issue scope and stamp no human actor. | issue.sessionDerived, oplog.appended |
| `sessions.delegated-wake-on-queue` | session.wakeRequested | in-memory | none — Queued input remains durable; restart does not replay execution, and a later resume or send retries. | session id + queued principal reference (coalesce) | After durable enqueue; before the inbox attempts ordered FIFO delivery. | Best-effort drop; the durable inbox row remains pending for an explicit later resume. Owner: session inbox delivery | delegated; live delegation; re-authorize at apply | Resolve the queued delegation live at apply; revocation prevents process wake and delivery. | session.wakeRequested, message delivery attempts |
| `sessions.system-wake-on-queue` | session.wakeRequested | in-memory | none — Queued input remains durable; restart does not replay execution, and a later system action retries. | session id + system job reference (coalesce) | After durable enqueue; before the inbox attempts ordered FIFO delivery. | Best-effort drop; the durable inbox row remains pending for an explicit later resume. Owner: session inbox delivery | system; actor=system; scope=acted-on entity | Acts only on the existing session binding and never stamps or impersonates a human. | session.wakeRequested, message delivery attempts |
| `messages.dead-letter-nudge` | message.deadLettered | in-memory | none — The durable dead-letter row remains visible; the sender nudge is best-effort and is not replayed after restart. | message id (deduplicate) | After the guarded queued-to-dead-letter transition for that message. | Best-effort drop; the durable ledger row is the restart-visible failure record. Owner: message delivery service | delegated; live delegation; re-authorize at apply | Routes only to the persisted sender attribution and never to an ambient operator. | message.deadLettered, message.dead_letter, message dead letters |
| `messages.eligibility` | oplog.appended | durable | startup reconcile from messages outbox plus current session/issue rows; re-authorize at apply | message id + delivery attempt state (deduplicate) | Ledger sequence, then per-target message FIFO. | Delivery sweep retries; permanent refusal dead-letters to the sender. Owner: message delivery service | delegated; live delegation; re-authorize at apply | Apply-time authorization resolves the stored delegation reference live. | oplog.appended, message delivery attempts |
| `messages.turn-boundary-drain` | session.stateChanged | durable | startup reconcile from messages outbox plus restored session state; re-authorize at apply | message id (deduplicate) | Per-session FIFO after the observed state transition. | Delivery sweep is the retry backstop; dead letters belong to messaging. Owner: message delivery service | delegated; live delegation; re-authorize at apply | Every queued sender is re-authorized before injection. | session.stateChanged, message delivery attempts |
| `messages.transcript-confirmation` | transcript.delta | durable | startup reconcile from message ledger and durable transcript tail; re-authorize at apply | message id + transcript frame (deduplicate) | Transcript item order for one session. | The next transcript delta or delivery sweep retries reconciliation. Owner: message delivery service | delegated; live delegation; re-authorize at apply | Confirmation changes status only; it does not widen message visibility. | transcript.delta, message confirmations |
| `locks.session-exit-release` | session.exited | durable | startup reconcile from lock leases and restored live-session set; re-authorize at apply | lock name + holder session id + lease generation (safe-repeat) | Per-lock repository transaction order. | Lease expiry/janitor sweep is the backstop. Owner: lock service | system; actor=system; scope=acted-on entity | System cleanup changes only the lease it acted on and stamps no human. | session.exited, lock expiry/release |
| `conversations.discovery-index` | daemon.discovery | durable | startup reconcile from conversation registry, transcript lake, and mirror checkpoints; re-authorize at apply | machineId + native conversation id + source revision (deduplicate) | Per-machine discovery order; ledger assigns global publication order. | Mirror/index checkpoints retry on the next sweep; diagnostics remain visible. Owner: conversation memory service | system; actor=system; scope=acted-on entity | The derived row inherits the indexed conversation scope and never widens it. | conversations.changed, mirror/index diagnostics |
| `publisher.ordered-fanout` | oplog.appended | durable | startup reconcile from durable change ledger and subscriber cursor; re-authorize at apply | feed epoch + sequence + subscriber id (deduplicate) | Strict global ledger sequence with per-subscriber watermarks. | Reconnect/bootstrap heals gaps; serving edge owns slow-consumer failure. Owner: feed serving edge | system; actor=system; scope=acted-on entity | Visibility is evaluated for each authenticated subscriber before delivery. | oplog.appended, feed cursor and lag |
| `messaging.telegram-attention` | notification.telegramRequested | in-memory | none — Attention pushes are best-effort and are never replayed after restart. | owner user + session or notification fact + attention transition (deduplicate) | Per-owner EventBus request order; topic routing is resolved at send time. | Best-effort drop; the durable phase or steward breadcrumb remains observable. Owner: Telegram messaging bridge | delegated; live delegation; re-authorize at apply | Route comes from the owner of the triggering session/subscriber; unknown or ambiguous chats fail closed. | notification.telegramRequested, session.phase, steward.notify, Telegram send failures |
| `messaging.telegram-outbound` | superagent.turnEnded | in-memory | none — Completed turns are not replayed to external chat after restart. | owner user + thread id + completed turn id (deduplicate) | Per-user, per-conversation completion order. | Adapter-local bounded retry; final failure is owned by the messaging bridge. Owner: Telegram messaging bridge | delegated; live delegation; re-authorize at apply | Outbound route is resolved from the thread owner; inbound identity depends on POD-1080 and unknown chats fail closed. | superagent.turnEnded, Telegram send failures |
| `messaging.telegram-configure` | settings.changed | in-memory | none — Startup configures adapters from current settings and binding rows. | user + bot-token fingerprint + route revision (safe-repeat) | Settings event order; the latest configuration replaces the prior adapter. | The next settings/binding change or restart retries configuration. Owner: Telegram messaging bridge | system; actor=system; scope=acted-on entity | Configuration may read server secrets but never exposes them or invents a user binding. | settings.changed, Telegram configuration failures |
| `messaging.ambient-typing` | session.stateChanged | in-memory | none — Typing presence is transient and is cleared on process restart. | owner user + conversation + lease owner (coalesce) | Latest session phase wins per owner/conversation lease. | Best-effort drop; the next state transition refreshes or clears presence. Owner: Telegram messaging bridge | delegated; live delegation; re-authorize at apply | Lease key is per-conversation-per-user; no cross-user refcounting. | session.stateChanged, session.exited, typing send failures |
| `messaging.topic-entry-recap` | superagent.turnEnded | in-memory | none — Topic-entry recaps are never replayed after restart. | owner user + chat binding + topic entry (deduplicate) | At-least-once in per-user topic-entry order. | Best-effort send; the bridge owns failures and suppresses duplicate re-entry spam. Owner: Telegram messaging bridge | delegated; live delegation; re-authorize at apply | Transcript source and destination must have the same owner user. | superagent.turnEnded, Telegram recap failures |
| `automations.scheduled-runs` | janitor.tick | durable | startup reconcile from automation definitions, occurrence ids, and durable run rows; re-authorize at apply | automation id + scheduled occurrence (deduplicate) | Chronological occurrence order per automation; ledger orders resulting rows globally. | Janitor retries unmaterialized occurrences; terminal failures are recorded as run outcomes. Owner: automation scheduler/service | delegated; live delegation; re-authorize at apply | Creator delegation is resolved live on every fire/replay; revoked creators cannot run. | automation run rows, automation outcomes |
| `maintenance.steward` | janitor.tick | durable | startup reconcile from event log, notification facts, messages, issues, and sessions; re-authorize at apply | notification fact + target session (deduplicate) | Event-log cursor order; deliveries commit before cursor advance. | Janitor owns retry and leaves the cursor before a failed fact. Owner: steward janitor job | system; actor=system; scope=acted-on entity | Writes are system-attributed and remain in the target fact/session scope. | steward event cursor, steward delivery failures |
| `maintenance.expiry` | janitor.tick | durable | startup reconcile from durable expiry timestamps and current entity state; re-authorize at apply | entity id + expiry boundary (safe-repeat) | Per repository transaction; ledger orders visible entity changes. | The next fenced janitor tick retries. Owner: maintenance janitor | system; actor=system; scope=acted-on entity | System expiry never changes ownership or attributes a human actor. | maintenance run records, prune/expiry duration |
| `startup.boot-reconcile` | startup | durable | startup reconcile from authoritative repositories and durable outboxes; re-authorize at apply | entity kind + entity id + durable revision (safe-repeat) | Dependency order declared by the composition graph, then ledger sequence. | Component-specific sweep/janitor backstops; boot logs retain failures. Owner: server composition root | system; actor=system; scope=acted-on entity | Boot writes are system-attributed, preserve entity scope, and never stamp a human. | boot diagnostics, reconcile duration/failures |

## Startup reconciliation

Every durable entry names its durable source of truth above. Reconciliation re-reads that source, derives pending work, and re-authorizes the declared system scope or live delegation before each apply; persisted effective capabilities are never trusted.
