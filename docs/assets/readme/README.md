# README product images

The original capture files are unchanged copies of the illustrative Acme Launch project captures published on
the Podium website, retrieved on 2026-09-23. They show development UI, not a guarantee of
feature availability in the latest stable release. This file records the capture and composition provenance.

| Local file | Published source |
| --- | --- |
| `team-view.png` | https://podium.do/marketing/captures/flightdeck.png |
| `coordinator-chat.png` | https://podium.do/marketing/captures/chat.png |
| `task-board.png` | https://podium.do/marketing/captures/board.png |

Store replacements here so GitHub and cloned copies do not depend on the website's asset
URLs. Prefer readable task and conversation views that demonstrate the same project.

## Additional assets

- `proposed-task.png`: unchanged https://podium.do/marketing/captures/proposals.png capture.
- `agent-offer.png`: unchanged https://podium.do/marketing/captures/offers.png capture.
- `task-board-framed.png`: AI-assisted illustrative composition, generated with the built-in
  image-editing tool from `task-board.png`, Till's supplied `artistic_bg_foto_tf.jpg`, and the
  existing `../readme-hero.jpg` as a framing reference. This is not a pixel-exact raw capture;
  retain `task-board.png` as the original.

Composition prompt: Place the entire task-board screenshot flat and centered over the supplied
mountain painting, with narrow margins, rounded outer corners and a soft shadow matching the
existing hero. Preserve UI text, layout, cards, colors and proportions; add no window chrome or
captions. Landscape, high-resolution output. The built-in image tool was used, not the CLI fallback.
