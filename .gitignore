node_modules/
dist/
coverage/
*.tsbuildinfo
*.log
.DS_Store
.env
.env.*
!.env.example
.turbo/

# Playwright
tests/e2e/test-results/
tests/e2e/playwright-report/
/e2e/

# Brainstorming visual companion
.superpowers/

# Agent harness session state + nested worktrees (not part of this repo's lint/test scope)
.claude/*
# ...but track shared team settings (e.g. disabled commit attribution)
!.claude/settings.json
.claire/
.worktrees/
test-results/

# bun build --compile artifacts
dist-bun/

# Headless self-update DEV signing key (private half; pubkey is committed). Never commit.
scripts/.podium-update-dev.key
# Headless update artifacts produced by build-bun.ts
podium-headless-*.tar.gz
podium-headless-*.tar.gz.sig

# postgres experiment scratch
.exp/
dist-bun/

# experiment: staged Postgres blob (60M, regenerated from node_modules) + Rust target
apps/desktop/src-tauri/resources/postgres/
apps/desktop/src-tauri/target/
