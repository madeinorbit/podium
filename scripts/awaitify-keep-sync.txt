# The await pass refusal set — functions that CANNOT become async.
#
# GENERATED by `bun scripts/awaitify-derive-keep-sync.ts`. Do not hand-edit and
# never resolve a merge conflict here textually: re-derive it. Each entry is a
# function addressed by BYTE OFFSET, so any edit that moves a function makes its
# entry a silent no-op — the pass stops refusing there and proposes the edit
# again. `awaitify.ts` reports such entries as UNUSED and the idempotence check
# fails on them, which is the signal to re-run this script.
#
# Format: <repo-relative file>|<byte offset of the function>  # <category>: <the
# compiler error that proved it>. The category is what the flip has to decide
# about; the error is the evidence. Everything from `#` is commentary.
#
# Derived at a077d40fc8a45823b6013c723b0ff23029a065f2
# 5 functions in 3 files
#   2  compiler-refused
#   2  promise-reaches-a-member-read
#   1  caller-type-cannot-absorb-a-promise

# apps/server/src/modules/sessions/command-plane.test.ts (2)
apps/server/src/modules/sessions/command-plane.test.ts|8349  # promise-reaches-a-member-read: TS2739 Type 'Oracle' is missing the following properties from type 'Promise<Oracle>': then, catch, [Symbol.toStringTag], finally
apps/server/src/modules/sessions/command-plane.test.ts|13202  # promise-reaches-a-member-read: TS2739 Type 'Oracle' is missing the following properties from type 'Promise<Oracle>': then, catch, [Symbol.toStringTag], finally

# apps/server/src/relay.draft-reap.test.ts (2)
apps/server/src/relay.draft-reap.test.ts|7665  # compiler-refused: TS1109 Expression expected.
apps/server/src/relay.draft-reap.test.ts|14609  # compiler-refused: TS1109 Expression expected.

# apps/server/src/router.auth.test.ts (1)
apps/server/src/router.auth.test.ts|1335  # caller-type-cannot-absorb-a-promise: TS2322 Type '() => Promise<boolean>' is not assignable to type '() => boolean'.
