{
  "$schema": "Router `.mutation(` census — see scripts/audit-router-mutations.ts.",
  "$note": "Every top-level `<name>: t.router(` literal in apps/server/src/router.ts is accounted for exactly once, either as `migrated` (writes derived from a contract table) or in `pending` with the hand-written `.mutation(` keys it still carries. `total` may only go DOWN. A key in the router and not here is a finding; a key here and not in the router is a finding too — the census is not allowed to rot. A router in NEITHER list is a finding, so a whole new router cannot arrive unnoticed.",
  "total": 25,
  "migrated": {
    "$note": "Writes DERIVED from their contract tables — POD-380/381/382 (sessions), POD-731/732 (workflows), POD-729 (messages), POD-383 (superagent), POD-384 (machines/repos/discovery), POD-386 (specs). Zero hand-written mutations, with the single documented exception below. Adding one here is the regression these cutovers exist to prevent.",
    "routers": [
      "sessions",
      "pins",
      "snoozes",
      "tabs",
      "superagent",
      "repos",
      "machines",
      "discovery",
      "messages",
      "workflows",
      "specs",
      "approvals"
    ],
    "allowed": {
      "$note": "`discovery.scan` scans CONVERSATIONS (`rpc.scan()` returns `{ conversations, diagnostics }`) and belongs to the session-discovery family; it shares the fleet router's name and nothing else, so POD-384 deliberately left it out of the fleet contract table. Allowlisted BY KEY and only on `discovery` — the same key under `repos` is still a finding.",
      "discovery": [
        "scan"
      ]
    }
  },
  "pending": {
    "$note": "Routers whose writes are still hand-written, each with the issue that owns the migration. NOT this issue's scope; recorded so a new hand-written mutation cannot arrive unnoticed anywhere in router.ts. `keys: []` means the router serves reads only and must keep serving reads only.",
    "cloud": {
      "owner": "POD-314",
      "keys": [
        "createMachine",
        "createAgent",
        "moveSession",
        "stop",
        "wake"
      ]
    },
    "sync": {
      "owner": "POD-314",
      "keys": []
    },
    "conversations": {
      "owner": "POD-314",
      "keys": [
        "setMeta"
      ]
    },
    "search": {
      "owner": "POD-314",
      "keys": []
    },
    "settings": {
      "owner": "POD-352",
      "guard": true,
      "$note": "THE GUARD. POD-313's title carves settings out of 3.3 explicitly (`settings via #352`), so this issue's obligation is that settings is UNTOUCHED — not that it shrinks. An added, removed or renamed key here is a finding in BOTH directions, and apps/server/src/router.settings-guard.test.ts checks the same claim against the RUNNING router.",
      "keys": [
        "set",
        "telegramSetupStart",
        "telegramSetupPoll"
      ]
    },
    "perf": {
      "owner": "POD-314",
      "keys": [
        "report",
        "reset"
      ]
    },
    "features": {
      "owner": "POD-314",
      "keys": []
    },
    "telemetry": {
      "owner": "POD-314",
      "keys": [
        "set",
        "resetId"
      ]
    },
    "accounts": {
      "owner": "POD-314",
      "keys": [
        "connect",
        "disconnect"
      ]
    },
    "usage": {
      "owner": "POD-314",
      "keys": []
    },
    "quota": {
      "owner": "POD-314",
      "keys": []
    },
    "models": {
      "owner": "POD-314",
      "keys": [
        "refresh"
      ]
    },
    "hosts": {
      "owner": "POD-314",
      "keys": [
        "memoryBreakdown"
      ]
    },
    "setup": {
      "owner": "POD-314",
      "keys": [
        "complete",
        "join",
        "connect",
        "setChannel"
      ]
    },
    "auth": {
      "owner": "POD-314",
      "keys": [
        "setPassword",
        "clearPassword"
      ]
    },
    "git": {
      "owner": "POD-314",
      "keys": []
    },
    "files": {
      "owner": "POD-314",
      "keys": [
        "write"
      ]
    },
    "automations": {
      "owner": "POD-314",
      "keys": []
    }
  }
}
