{
  "$schema": "https://turbo.build/schema.json",
  "globalDependencies": ["tooling/tsconfig/**"],
  "tasks": {
    "typecheck": {
      "dependsOn": ["^typecheck"],
      "inputs": ["$TURBO_DEFAULT$", "tsconfig.json", "package.json"],
      "outputs": []
    }
  }
}
