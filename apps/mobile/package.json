{
  "name": "@podium/mobile",
  "version": "0.0.0",
  "private": true,
  "license": "Apache-2.0",
  "main": "expo-router/entry",
  "dependencies": {
    "@expo-google-fonts/geist": "^0.4.2",
    "@expo-google-fonts/geist-mono": "^0.4.3",
    "@expo/metro-runtime": "~57.0.14",
    "@expo/ui": "~57.0.14",
    "@podium/client-core": "workspace:*",
    "@podium/commands": "workspace:*",
    "@podium/logger": "workspace:*",
    "@podium/model": "workspace:*",
    "@podium/protocol": "workspace:*",
    "@podium/sync": "workspace:*",
    "@podium/terminal-client": "workspace:*",
    "@podium/terminal-client-react": "workspace:*",
    "@react-native-async-storage/async-storage": "2.2.0",
    "@react-native-community/netinfo": "12.0.1",
    "@trpc/client": "^11.18.0",
    "expo": "~57.0.17",
    "expo-blur": "~57.0.2",
    "expo-camera": "~57.0.4",
    "expo-clipboard": "~8.0.8",
    "expo-crypto": "~57.0.2",
    "expo-font": "~57.0.1",
    "expo-haptics": "~57.0.2",
    "expo-linear-gradient": "~57.0.1",
    "expo-modules-core": "^57.0.14",
    "expo-router": "~57.0.17",
    "expo-secure-store": "~57.0.2",
    "expo-sqlite": "~57.0.2",
    "expo-status-bar": "~57.0.1",
    "expo-symbols": "~57.0.0",
    "marked": "^18.0.5",
    "react": "19.2.3",
    "react-dom": "19.2.3",
    "react-native": "0.86.0",
    "react-native-gesture-handler": "~3.0.2",
    "react-native-keyboard-controller": "1.21.9",
    "react-native-reanimated": "4.5.0",
    "react-native-safe-area-context": "~5.7.0",
    "react-native-screens": "4.26.2",
    "react-native-svg": "15.15.4",
    "react-native-web": "^0.21.2",
    "react-native-worklets": "0.10.0"
  },
  "devDependencies": {
    "@testing-library/react": "^16.3.2",
    "@types/react": "~19.2.2",
    "happy-dom": "^20.0.0",
    "typescript": "~6.0.3",
    "vitest": "5.0.0-beta.6"
  },
  "scripts": {
    "dev": "expo start --web --port 8082",
    "start": "expo start",
    "android": "expo start --android",
    "ios": "expo start --ios",
    "web": "expo start --web --port 8082",
    "//build:web": "Same shape as apps/web's `build`, and for the same reasons. Metro's production graph optimization and tree shaking remove unused barrel exports such as Lucide's full icon set; keep both flags together. Pre-compression (POD-1991) keeps the LIVE server out of it: /mobile is served by the same serveFile that prefers a build-time .br/.gz sibling, so an export without them made the server brotli 6.7 MB on demand at its own interactive tier. The stamp stays LAST (POD-1986): podium-build.json is what Update reads to decide the phone is at HEAD, so nothing may write after it.",
    "build:web": "EXPO_UNSTABLE_METRO_OPTIMIZE_GRAPH=1 EXPO_UNSTABLE_TREE_SHAKING=1 expo export -p web && bun scripts/patch-web-html.ts && bun ../../scripts/precompress-dist.ts dist && bun --conditions=@podium/source ../../scripts/write-web-build-stamp.ts dist",
    "typecheck": "tsc --noEmit",
    "typecheck:tsc": "tsc --noEmit",
    "test": "bun ../../scripts/validation-admission.ts focused --label @podium/mobile:test -- vitest run"
  }
}
