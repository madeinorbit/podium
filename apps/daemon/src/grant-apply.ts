/** Compatibility import for daemon-local tests; the workflow is parent/shared. */
export {
  applyGrant,
  createGrantRunner,
  type GrantApplyDeps,
  type GrantArtifact,
  type GrantProgress,
} from '@podium/runtime/update-participant'
