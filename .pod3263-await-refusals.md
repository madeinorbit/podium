# Await pass refusals — 1731 sites

Generated by `bun scripts/awaitify.ts --pass=awaits
--keep-sync=scripts/awaitify-keep-sync.txt --report=<path>`. Line numbers are
pinned to the commit it was generated at and go stale as tests move; the
CATEGORIES do not. Regenerate rather than hand-edit.

## assertion-must-change-at-the-flip (888)

### apps/server/src/relay.ts (174)
- L568 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.sessions.getSession(asSessionId(candidate))`
- L571 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.sessions.getSession(asSessionId(candidate))`
- L661 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.users.get(userId)`
- L664 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `machinesForPrincipal( { machines: machineService }, userCommandPrincip`
- L685 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `machines.machineName(id)`
- L692 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `machines.listMachines()`
- L734 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `machines.updateChannel(machineId)`
- L762 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `operations?.engine.active(LIFECYCLE_EXCLUSION_GROUP)`
- L767 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `operations?.engine.active(LIFECYCLE_EXCLUSION_GROUP)`
- L779 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.events.appendEvent( { ts: new Date(record.at).toISOString`
- L793 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `machines.defaultMachine()`
- L826 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.users.list()`
- L829 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `settings.getSettingsFor(userId)`
- L830 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `machines.listMachines()`
- L832 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `settings.updatePreferences(userId, values)`
- L895 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `ledger.authority.snapshot('issueEvent')`
- L897 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `issueEventFeed?.publish(id, event)`
- L949 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.sync.minChangeSeq()`
- L983 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.repos.listRepoPaths()`
- L1034 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `machines.defaultMachine()`
- L1035 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `machines.resolveMachine(requested, cwd)`
- L1037 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `machines.machineName(id)`
- L1081 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `feedServing.identity()`
- L1090 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `targetStateResolver()`
- L1172 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.users.roleOf(userId)`
- L1232 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `queuedMessageApply.authorize(messageId)`
- L1233 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `queuedMessageApply.reject(messageId, reason)`
- L1251 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `loginPropagation.trigger({ targetMachineId: machineId, agentKind, `
- L1280 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.users.get(ownerUserId)`
- L1296 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.repos.listRepoPaths(machineId)`
- L1300 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `sessionsSvc.sessionById(asSessionId(event.podiumSessionId))`
- L1303 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `loginPropagation.trigger({ targetMachineId: session.machineId, agentKind: `
- L1328 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.settings.getSettings()`
- L1330 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `machines.machineName(id)`
- L1348 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `closedIssueIdsInScope()`
- L1368 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `sessionsSvc.hibernateSession(input)`
- L1372 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.automations.lastSpawnedSessions()`
- L1373 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.automations.list()`
- L1379 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `sessionsSvc.hasValidTerminalProof(sessionId)`
- L1380 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `sessionsSvc.terminalProofMissing(sessionId)`
- L1403 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.settings.getSettingsFor(ownerUserId)`
- L1405 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.secrets.getOrEmpty('notifications.telegramBotToken')`
- L1407 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.telegramBindings.listForUser(ownerUserId)`
- L1409 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.events.appendEvent(e)`
- L1433 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `locks.releaseForSession(sessionId)`
- L1459 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `sessionsSvc.listSessions()`
- L1461 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `sessionsSvc.sessionById(sessionId)`
- L1465 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `sessionsSvc.listSessionsForIssue(worktreePath, issueId)`
- L1469 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.settings.getSettingsFor(FIRST_ADMIN_USER_ID)`
- L1471 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `sessionsSvc.createSession({ ...(o.sessionId ? { sessionId: o.sessionId } : {}), `
- L1484 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `machines.resolveMachine(requested, cwd)`
- L1486 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `machines.requireMachineForRepo(machineId, repoPath)`
- L1487 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `machines.requireRepoHostStructure(machineId)`
- L1510 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `sessionsSvc.workspace.findRepoOnMachine(repoPath, machineId)`
- L1542 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `issues.ensureCoordinator(issueId, sessionId, { onlyMember: true })`
- L1612 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `issueSessionLifecycle.stopClosedIssue({ ...input, reason: 'close' })`
- L1633 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `messagesSvc.onWakeUnavailable(sessionId, `refused: ${reason}`)`
- L1676 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.transact(fn)`
- L1679 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.repos.resolveRepoIdForPath(repoPath)`
- L1693 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `issues.sendMail(issueId, from, body)`
- L1696 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.events.appendEvent(e)`
- L1726 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `funnel.run({ write: async () => await this.store.issues.addIssueMessage(row) })`
- L1728 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `funnel.run({ write: async () => await this.store.issues.markIssueMes`
- L1737 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.transact(fn)`
- L1745 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `sessionsSvc.createSession(o)`
- L1749 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `machines.listMachines()`
- L1753 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `messagesSvc.onQueuedInputApplied(messageId, sessionId)`
- L1755 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `messagesSvc.onQueuedInputInjected(messageId, sessionId)`
- L1757 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `messagesSvc.onQueueDrainAbandoned(sessionId, turnIds, reason)`
- L1760 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `messagesSvc.onSessionExited(sessionId)`
- L1763 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `messagesSvc.cancel(messageId)`
- L1770 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `messagesSvc.cancelPendingOperatorMessage(sessionId, messageId)`
- L1777 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `messagesSvc.notifyQueuedInputRejected(messageId, reason)`
- L1794 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `messagesSvc.onSessionEligibilityChanged( // `EntityChangeSpec.id` is polymorph`
- L1805 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `messagesSvc.onIssuesEligibilityChanged(changedIssueIds)`
- L1824 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `issues?.getMeta(issueId)`
- L1829 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.repos.resolveRepoIdForPath(path)`
- L1831 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `messagesSvc.send( { kind: 'system', name: 'workflow' }, { `
- L1854 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.workflows.ownerOf(entity.kind, entity.id)`
- L1856 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.grants .listForResource(entity.kind, entity.id)`
- L1877 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.users.roleOf(userId)`
- L1888 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.sync.getAppliedMutation(asMutationId(key))`
- L1890 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.sync.recordAppliedMutation( asMutationId(key), 'wor`
- L1912 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `workflows.prepareExistingSession({ sessionId, ...(issueId ? { `
- L1916 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `workflows.prepareStart({ sessionId, cwd, ...(iss`
- L1928 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `workflows.startRun({ sessionId, onBehalfOf: sessio`
- L1945 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `sessionsSvc.listSessions()`
- L1946 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `sessionsSvc.sessionById(sessionId)`
- L1952 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `sessionsSvc.createSession({ ownerUserId: o.ownerUserId, cwd: o.cwd`
- L1970 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `workflows.executionProfileForLaunch({ ...profileInput, ...(caller `
- L1974 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `workflowCallerForCapability(caller.capability, caller.overrideScope)`
- L1979 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `issues.create({ ...o, startNow: false })`
- L1980 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.events.appendEvent(e)`
- L1994 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.notificationFacts.retire(factKey, target, new Date(this.now()).toISOString())`
- L2000 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `sessionsSvc.listSessions()`
- L2016 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.transact(work)`
- L2029 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `sessionsSvc.createSession(o)`
- L2038 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `sessionsSvc.receiptSend('queue', o)`
- L2039 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `sessionsSvc.receiptSend('wake', o)`
- L2041 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `issues.create({ ...o, startNow: false, origin: 'agent', `
- L2047 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `issues.update(issue.id, { stage: 'in_progress' })`
- L2052 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `sessionsSvc .listSessions()`
- L2058 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.users.roleOf(ownerUserId)`
- L2062 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `machines.defaultMachine()`
- L2076 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `sessionsSvc.sessionById(sessionId)`
- L2080 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `issues.getMeta(issueId)`
- L2082 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.repos.prefixForPath(row.repoPath)`
- L2089 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `machines.listMachines()`
- L2090 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `issues.sendMail(issueId, 'approval-broker', body)`
- L2092 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `workflowCallerForCapability(sessionsSvc.capabilityForSession(sessionId))`
- L2119 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `sessionsSvc.sessionById(existingSessionId)`
- L2126 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `sessionsSvc.sessionSpawnedBy(candidate)`
- L2135 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.settings.getSettingsFor(FIRST_ADMIN_USER_ID)`
- L2142 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `automations.create( { name: op.name, scheduleKind:`
- L2166 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.events.appendEvent({ ts: new Date().toISOString(), kind`
- L2196 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `issueSessionLifecycle.deleteIssue(id)`
- L2197 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `issueSessionLifecycle.restoreIssue(id)`
- L2199 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `sessionsSvc.listSessions()`
- L2201 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `sessionsSvc.sessionById(sessionId)`
- L2202 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.repos.listRepoPaths()`
- L2203 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.repos.listRepoPaths()`
- L2205 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `messagesSvc.send(from, input)`
- L2212 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `sessionsSvc.sessionById(id)`
- L2238 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `machines.ownershipRows()`
- L2241 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.users .list()`
- L2248 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.repos.listRepoPaths(machineId)`
- L2249 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.issues.getIssue(id)`
- L2250 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `issues.create(input)`
- L2251 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `issues.sendMail(issueId, 'machine-diagnostic', body)`
- L2278 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.settings.getSettings()`
- L2283 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `settings.getSettingsFor(userId)`
- L2284 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `settings.getModelCatalog(machineId)`
- L2287 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `machines.nativeAccountIdForMachine(machineId, agent, requested)`
- L2349 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `issues.get(id)`
- L2353 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `issues.children(id, recursive)`
- L2354 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `issues.shippingCommit(id, mutation, write)`
- L2397 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.users.roleOf(humanId)`
- L2425 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.users.roleOf(userId)`
- L2474 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.shipping.rootIntegrationReceipt(rootIssueId, approvedHeadSha)`
- L2489 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `locks.acquire(caller, { repoPath: issue.repoPath, name, `
- L2504 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `locks.cancel(caller, { repoPath: issue.repoPath, name,`
- L2512 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `locks.release(caller, { repoPath: issue.repoPath, name`
- L2534 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `locks.renew(caller, { repoPath: issue.repoPath, name, `
- L2552 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `locks.release(caller, { repoPath: issue.repoPath, name, `
- L2562 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `machines.pickMachineForRepo(undefined, issue.repoPath)`
- L2564 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `machines.listMachines()`
- L2609 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.events.appendEvent({ ts: new Date(this.now()).toISOString(), `
- L2613 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.issues.getIssue(issueId)`
- L2623 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `shipping .reconcile()`
- L2722 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `operationsModule.engine.active(LIFECYCLE_EXCLUSION_GROUP)`
- L2747 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `ledger.authority.snapshot('pendingInteraction')`
- L2753 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `interactionFeed.publish(row)`
- L2760 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `sessionsSvc.sessionById(sessionId)`
- L2779 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.events.runtimeEventCheckpoint(sessionId)`
- L2781 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.events.hasCausalTurnFailure(sessionId, checkpoint.turnEpoch)`
- L2786 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `sessionsSvc.sessionById(id)`
- L2811 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `sessionsSvc.sessionById(id)`
- L2856 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `interactions .ask({ interaction: { ...msg.interaction, sessionId: msg.sessionId } `
- L2898 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `interactions.onStateChanged({ sessionId: e.sessionId, prev: e.prev, next: e.next })`
- L2900 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `interactions.onSessionExited(e.sessionId)`
- L2960 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.repos.listRepos()`
- L3011 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `sessionsSvc.listSessions()`
- L3013 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `sessionsSvc.sessionById(sessionId)`
- L3030 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `sessionsSvc.receiptSend( 'queue', { sessionId, `
- L3044 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.events.appendEvent({ ts: new Date().toISOString(), `
- L3061 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.settings.getSettings()`
- L3097 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `sessionsSvc.sessionById(sessionId)`
- L3101 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `messagesSvc.onSessionIdle(meta, { priorPhase: prev?.phase })`
- L3102 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `messagesSvc.onSessionEligibilityChanged(sessionId)`
- L3112 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `messagesSvc.onTranscriptDelta(sessionId, items)`
- L3114 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `messagesSvc.sweep()`
- L3129 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `approvals.sweepStalledExecutions()`
- L3177 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `approvals.listPending()`
- L3193 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `agentRelayGate.run(machineId, msg)`

### apps/server/src/issues.test.ts (30)
- L182 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `svc.resolveRef('#1')`
- L633 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `svc.setIssueTucked(open.id, true)`
- L1335 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `svc.addSession(w.id)`
- L1793 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `svc.addSession(created.id)`
- L1841 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `svc.addSession(created.id)`
- L1846 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `svc.addSession(created.id)`
- L2831 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `svc.addDep(a.id, a.id)`
- L2832 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `svc.addDep(b.id, a.id)`
- L2969 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `svc.update(child.id, { color: 'teal' })`
- L3016 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `svc.update(a.id, { parentId: b.id })`
- L3025 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `svc.update(x.id, { parentId: nw.id })`
- L3042 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `svc.addDep(root.id, child.id, 'blocks')`
- L3055 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `svc.addDep(c.id, a.id)`
- L3064 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `svc.addDep(a.id, b.id, 'parent-child')`
- L3071 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `svc.removeDep(c.id, e.id, 'parent-child')`
- L3213 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `svc.tree('iss_nope')`
- L3316 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `svc.supersede(a.id, 'iss_missing')`
- L3564 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `svc.purgeEmptyDraft('iss_missing')`
- L3644 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `svc.resolveRef('1')`
- L3680 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `svc.resolveRef('podium#1')`
- L3840 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `h.svc.update(issue.id, { worktreePath: '/r' })`
- L4283 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `h.svc.create({ repoPath: '/r', title: `K${i}`, parentId: epic.id, startNow: false })`
- L4284 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `h.svc.update(c.id, { branch: k.branch ?? `issue/${c.seq}-k${i}` })`
- L4285 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `h.svc.close(c.id)`
- L4286 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `h.svc.get(c.id)`
- L4374 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `h.store.shipping.rootIntegrationReceipt( epic.id, 'aaaaaaaaaaaaaaaaaaa`
- L4862 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `svc.children('iss_nope')`
- L4915 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `svc.panelApply(w.id, { op: 'todo-done', index: 9 })`
- L5169 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `svc.update(w.id, { stage: 'review' })`
- L5412 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `svc.mailClaim('msg_nope', 'x')`

### apps/server/src/modules/lock/service.ts (27)
- L340 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.sweepExpired(repoId)`
- L341 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.deps.locks.getLock(repoId, input.name)`
- L343 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.grantTo(repoId, input.name, caller, ttl, input.note ?? null)`
- L344 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.toWire(row)`
- L354 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.deps.locks.upsertLock(row)`
- L355 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.toWire(row)`
- L360 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.deps.locks .listWaiters(repoId, input.name)`
- L364 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.findSibling(existing, caller)`
- L383 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.deps.locks.enqueueWaiter({ repoId, name: input.name, `
- L393 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.toWire(existing)`
- L410 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.sweepExpired(repoId)`
- L411 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.deps.locks.getLock(repoId, input.name)`
- L418 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.advanceQueue(repoId, input.name)`
- L439 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.sweepExpired(repoId)`
- L440 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.deps.locks.getLock(repoId, input.name)`
- L445 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.deps.locks .listWaiters(repoId, input.name)`
- L449 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.deps.locks.removeWaiterBySession(repoId, input.name, key)`
- L492 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.toWire(l)`
- L509 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.sweepExpired(repoId)`
- L510 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.deps.locks.getLock(repoId, input.name)`
- L519 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.deps.locks.removeWaiterBySession(repoId, input.name, this.sessionKey(caller))`
- L520 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.grantTo(repoId, input.name, caller, ttl, input.note ?? null)`
- L538 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.toWire(row)`
- L550 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.deps.locks.listWaitsBySession(sessionId)`
- L551 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.deps.locks.removeWaiter(w.id)`
- L553 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.deps.locks.listLocksHeldBySession(sessionId)`
- L554 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.advanceQueue(lock.repoId, lock.name)`

### apps/server/src/modules/shipping/service.ts (27)
- L398 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.tick()`
- L674 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.requiredOrder(member.orderId)`
- L689 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.projectionSpecs()`
- L715 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.deps.repository.openHoldForOrder(member.id)`
- L881 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.requiredOrder(member.orderId)`
- L1157 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.deps.repository.getOrder(covered.id)`
- L1218 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.projectionSpecs()`
- L1389 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.requiredOrder(member.orderId)`
- L1411 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.requiredOrder(member.id)`
- L1412 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.deps.repository.openHoldForOrder(member.id)`
- L1567 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.requiredOrder(member.orderId)`
- L1586 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.deps.repository.openHoldForOrder(member.id)`
- L1815 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.projectionSpecs()`
- L1985 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.deps.repository.completeCoveredOrder(receipt, covering.id, effectEnvelopeKey)`
- L2023 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.requiredOrder(member.orderId)`
- L2146 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.deps.repository.raiseHold(hold)`
- L2244 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.projectionSpecs()`
- L2254 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.deps.repository.openHoldForOrder(member.id)`
- L2349 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.projectionSpecs()`
- L2384 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.projectionSpecs()`
- L2516 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.projectionSpecs()`
- L2585 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.requiredOrder(member.orderId)`
- L2715 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.deps.repository.openHoldForOrder(member.id)`
- L2904 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.projectionSpecs()`
- L3054 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.requiredOrder(member.orderId)`
- L3064 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.deps.repository.openHoldForOrder(member.id)`
- L3234 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.deps.resolveBranchTip(child)`

### apps/server/src/store/json-column-corruption-oracle.test.ts (26)
- L592 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `f.store.issues.getIssue('iss_1')`
- L603 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `f.store.issues.getIssue('iss_1')`
- L614 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `f.store.events.listEventsSince(0)`
- L626 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `f.store.shipping.rootIntegrationReceipt(asIssueId('iss_1'), 'approved-head')`
- L637 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `f.store.observationCheckpoints.get(SESSION)`
- L648 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `f.store.observationCheckpoints.getTerminalCandidate(SESSION)`
- L659 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `f.store.settingsAudit.list()`
- L670 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `f.store.settingsAudit.list()`
- L681 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `f.store.shipping.openHoldForOrder(f.orderId)`
- L692 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `f.store.shipping.openHoldForOrder(f.orderId)`
- L704 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `f.store.shipping.getOrder(f.stackedOrderId)`
- L715 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `f.store.shipping.getOrder(f.orderId)`
- L727 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `f.store.shipping.getOrder(f.orderId)`
- L739 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `f.store.shipping.getOrder(f.stackedOrderId)`
- L750 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `f.store.shipping.getOrder(f.stackedOrderId)`
- L761 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `f.store.shipping.getOrder(f.orderId)`
- L772 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `f.store.shipping.stepById(f.stepId)`
- L783 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `f.store.shipping.trainManifestForAttempt(f.attemptId)`
- L794 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `f.store.shipping.trainManifestForAttempt(f.attemptId)`
- L805 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `f.store.shipping.trainManifestForAttempt(f.attemptId)`
- L826 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `f.store.workflows.getRevision(f.revisionId)`
- L837 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `f.store.workflows.getRunSteps(f.runId)`
- L851 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `f.store.workflows.getRunSteps(f.runId)`
- L871 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `f.store.superagent.loadSuperagentMessages()`
- L882 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `f.store.superagent.listQueuedInputs()`
- L893 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `f.store.superagent.listPendingTurns()`

### apps/server/src/store-issues.test.ts (26)
- L175 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `s.issues.upsertIssue({ ...base(), id: asIssueId('t4'), repoPath: '`
- L254 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `s.issues.upsertIssue({ ...base(), stage: 'bogus' })`
- L257 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `s.issues.upsertIssue({ ...base(), defaultAgent: 'auto' })`
- L278 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `s.issues.listIssueRows()`
- L420 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `s.shipping.createOrder( shipOrder({ id: asShipOrderId('order-not-queued'), state: `
- L424 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `s.shipping.createOrder(shipOrder({ id: asShipOrderId('order-other') }))`
- L502 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `s.shipping.appendStep({ ...plannedStep, id: asShipStepId('step-collision')`
- L512 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `s.shipping.appendStep({ ...plannedStep, id: asShipStepId('step-stale-gener`
- L521 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `s.shipping.raiseHold({ id: asShipHoldId('hold-stale'), orderId: order.id, `
- L565 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `s.shipping.raiseHold({ id: asShipHoldId('hold-second-open'), orderId: orde`
- L578 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `s.shipping.resolveHold(order.id, 2, 'retry', 'queued', '2026-08-12T10:05:00.000Z')`
- L581 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `s.shipping.resolveHold(order.id, 1, 'retry', 'repairing', '2026-08-12T10:05:00.000Z')`
- L588 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `s.shipping.finishAttempt(attempt.id, 2, { finishedAt: '2026-08-12T10:06:00.000Z', `
- L594 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `s.shipping.transitionOrder(order.id, 'queued', 'verifying', '2026-08-12T10:06:30.000Z')`
- L623 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `s.shipping.completeVerifiedOrder(receipt)`
- L647 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `s.shipping.completeVerifiedOrder({ ...receipt, destinationSha: 'different-`
- L652 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `s.shipping.transitionOrder(order.id, 'shipped', 'queued', 'later')`
- L679 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `s.shipping.createOrder(shipOrder({ id: asShipOrderId('order-2') }))`
- L939 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `s.issues.transitionShippingStage(issue.id, 'review', 'shipping', 't2')`
- L972 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `s.shipping.recordRootIntegrationReceipt({ ...receipt, descendants: [childA`
- L1000 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `s.issues.upsertIssue({ ...base(), id, seq: 70 + index, bra`
- L1026 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `s.shipping.claimTrain({ leaderOrderId: b.id, startedAt: '2026-08-12T10:03:`
- L1033 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `s.shipping.claimTrain({ leaderOrderId: c.id, startedAt: '2026-08-12T10:03:`
- L1076 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `cyclic.shipping.claimTrain({ leaderOrderId: upperId, startedAt: '2026-08-1`
- L1148 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `s.shipping.createOrder({ ...evidenced, currentIntegrationReceipt: { `
- L1157 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `s.shipping.createOrder( shipOrder({ id: asShipOrderId('order-missing-rec`

### apps/server/src/server.ts (22)
- L716 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `registry.modules.machines.listMachines()`
- L722 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `store.repos.listRepos()`
- L734 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `registry.modules.machines.machineName(id)`
- L753 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `store.telegramBindings.listForUser(userId)`
- L758 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `store.secrets.getOrEmpty('notifications.telegramBotToken')`
- L767 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `store.superagent.getSuperagentThread(threadId)`
- L828 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `store.machines .listMachines()`
- L834 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `store.machines.listMachines()`
- L1079 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `store.machines.getMachineByToken(hostMachineId, token)`
- L1086 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `store.settings.getSettings()`
- L1089 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `registry.modules.sessions .listSessions(undefined, 'steward')`
- L1196 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `repos.inferFromPath(root, machineId ?? registry.modules.machines.defaultMachine())`
- L1216 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `superagent.mcpToolSpecs(threadId)`
- L1436 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `store.users.roleOf(principal.user)`
- L1471 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `ws.close()`
- L1501 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `superagent.threadOwner(threadId)`
- L1502 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `store.users.get(ownerUserId)`
- L1516 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `superagent.setMcpEndpoint( `http://127.0.0.1:${server.port}/mcp`, mcpToken, `
- L1519 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `superagent.mcpToolSpecs()`
- L1699 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `refreshTargetsOnBoot({ refresh: async (channel) => await registry.modules.updates.re`
- L1748 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `registry.modules.logs.close()`
- L1750 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `registry.modules.fleetLogs.close()`

### apps/server/src/modules/issues/registry.ts (19)
- L269 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `ctx.reports.list(input.repoPath)`
- L286 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `ctx.mayReadIssue(id)`
- L291 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `ctx.reports.readyList(input.repoPath)`
- L295 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `ctx.reports.blockedList(input.repoPath)`
- L304 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `ctx.mayReadIssue(id)`
- L310 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `ctx.mayReadIssue(id)`
- L322 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `ctx.mayReadIssue(id)`
- L327 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `ctx.mayReadIssue(id)`
- L332 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `ctx.mayReadIssue(id)`
- L337 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `ctx.mayReadIssue(id)`
- L342 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `ctx.mayReadIssue(id)`
- L346 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `ctx.mayReadIssue(id)`
- L350 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `ctx.mayReadIssue(id)`
- L354 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `ctx.mayReadIssue(id)`
- L370 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `ctx.mayReadIssue(id)`
- L374 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `ctx.mayReadIssue(id)`
- L378 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `ctx.mayReadIssue(id)`
- L382 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `ctx.mayReadIssue(id)`
- L1302 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `ctx.attention .subscriptionList({ subscriberId: subscriber.id })`

### apps/server/src/modules/lock/service.test.ts (16)
- L25 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `store.transact(fn)`
- L78 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `svc.release(stale, { repoPath: REPO, name: 'merge:main' })`
- L99 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `svc.renew(restarted, { repoPath: REPO, name: 'merge:main' })`
- L100 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `svc.release(restarted, { repoPath: REPO, name: 'merge:main' })`
- L108 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `svc.renew(first, { repoPath: REPO, name: 'merge:main' })`
- L109 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `svc.release(first, { repoPath: REPO, name: 'merge:main' })`
- L261 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `svc.acquire(sibling, { repoPath: REPO, name: 'l' })`
- L270 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `svc.acquire(sibling, { repoPath: REPO, name: 'l' })`
- L289 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `svc.acquire(third, { repoPath: REPO, name: 'l' })`
- L323 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `svc.acquire(on539, { repoPath: REPO, name: 'test:heavy' })`
- L388 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `svc.release(agent(2), { repoPath: REPO, name: 'l' })`
- L401 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `svc.release(agent(2), { repoPath: REPO, name: 'l' })`
- L423 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `svc.renew(agent(2), { repoPath: REPO, name: 'l' })`
- L424 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `svc.renew(agent(1), { repoPath: REPO, name: 'nope' })`
- L511 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `svc.cancel(agent(1), { repoPath: REPO, name: 'l' })`
- L513 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `svc.cancel(agent(2), { repoPath: REPO, name: 'l' })`

### apps/server/src/modules/issues/registry.test.ts (15)
- L309 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `guardIssueCommand(viewer, reg.issues, 'list', issueRegistry.defs.list, {})`
- L312 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `guardIssueCommand(viewer, reg.issues, 'create', issueRegistry.defs.create, { repoP`
- L328 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `guardIssueCommand(scoped, reg.issues, 'update', issueRegistry.defs.update, { id: a`
- L334 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `guardIssueCommand(scoped, reg.issues, 'update', issueRegistry.defs.update, { id: b`
- L340 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `guardIssueCommand( { ...scoped, overrideScope: true }, reg.issues, `
- L358 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `guardIssueCommand(scoped, reg.issues, 'update', issueRegistry.defs.update, { id: S`
- L401 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `guardIssueCommand(scoped, reg.issues, name, definition, insideInput)`
- L403 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `guardIssueCommand(scoped, reg.issues, name, definition, outsideInput)`
- L407 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `guardIssueCommand( { ...scoped, overrideScope: true }, reg.issues, `
- L415 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `guardIssueCommand(viewer, reg.issues, name, definition, insideInput)`
- L430 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `guardIssueCommand(scoped, reg.issues, 'mailSend', issueRegistry.defs.mailSend, { i`
- L437 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `guardIssueCommand(scoped, reg.issues, 'delete', issueRegistry.defs.delete, { id: a.id })`
- L441 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `guardIssueCommand({ capability: OPERATOR }, reg.issues, 'delete', issueRegistry.defs.delet`
- L566 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `dispatcher.run( { capability: agentCaller(root.id).capability }, 'ship', `
- L593 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `guardIssueCommand( { capability: { role: 'viewer', scope: { kind: 'all' `

### apps/server/src/modules/workflows/engine.test.ts (14)
- L1452 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `h.service.resolveRevision({ sessionId: asSessionId('s1'), cwd: '/r`
- L1464 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `h.service.resolveRevision({ sessionId: asSessionId('s1'), cwd: '/r`
- L1674 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `h.service.executionProfileForLaunch({ caller: operator, profileId:`
- L1685 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `h.service.executionProfileForLaunch({ caller: operator, profileId:`
- L1697 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `h.service.executionProfileForLaunch({ caller: operator, profileId:`
- L1707 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `h.service.executionProfileForLaunch({ caller: operator, profileId:`
- L1717 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `h.service.executionProfileForLaunch({ caller: operator, profileId: 'wfp_nope' })`
- L1818 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `h.service.executionProfileForLaunch({ caller: operator, profileId:`
- L2324 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `h.service.startRun({ sessionId: asSessionId('s3'), cwd: '/repo-b/w`
- L2902 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `h.service.prepareStart({ sessionId: asSessionId('s2'), cwd: '/repo`
- L3328 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `dispatchWorkflowRpc(h.service, operator, 'get', {})`
- L3360 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `dispatchWorkflowRpc( h.service, operator, 'publish', `
- L3369 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `dispatchWorkflowRpc(h.service, operator, 'get', { id: created.workflow.id }, 'outbox')`
- L3647 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `h.service.startRun({ sessionId: asSessionId('s1'), cwd: '/repo-a/w`

### apps/server/src/modules/machines/service.test.ts (14)
- L194 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `serviceListing([{ ...runnable, online: true }]).requireAgent(MACHINE, 'codex')`
- L238 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `serviceListing([{ id: MACHINE, name: 'vmi', online: true }]).requireAgent(MACHINE, 'shell'`
- L241 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `serviceListing([{ id: MACHINE, name: 'vmi', online: true, use: 'denied' }]).requireAgent( `
- L432 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `svc.requireAgent(MACHINE, 'claude-code')`
- L466 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `svc.resolveMachineForAgent(MACHINE, '/repo', 'codex')`
- L557 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `svc.ownershipRows()`
- L645 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `svc.transferMachineOwnership(MACHINE, asUserId(OWNER_A), asUserId(OWNER_B))`
- L660 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `svc.transferMachineOwnership(MACHINE, asUserId('user:typo'), asUserId(OWNER_A))`
- L677 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `svc.transferMachineOwnership(MACHINE, asUserId(OWNER_A), asUserId(OWNER_A))`
- L815 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `svc.adoptMachine(MACHINE, asUserId(BOB))`
- L816 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `svc.adoptMachine(MACHINE, asUserId(ALICE))`
- L841 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `svc.adoptMachine(MACHINE, asUserId(BOB))`
- L850 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `svc.adoptMachine(MACHINE, asUserId('user:typo'))`
- L866 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `svc.adoptMachine(asMachineId('ghost'), asUserId(ALICE))`

### apps/server/src/modules/messages/service.ts (13)
- L542 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.queueDeliveryTarget(target)`
- L563 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.send(from, input, opts)`
- L569 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.emitTransition(message, kind, extra)`
- L570 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.render.fromLabel(message)`
- L771 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.drainPreferred(session, messages, nowMs)`
- L773 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.prepareQueuedAttemptSafely(message, nowMs)`
- L775 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.scheduleQueuedWakeRetry(message)`
- L794 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.prepareQueuedAttemptSafely(message, nowMs)`
- L1451 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `settleReceipt(receipt, true)`
- L1453 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `settleReceipt(receipt, false)`
- L1740 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.onSessionEligibilityChanged(session.sessionId, session, { preferThisIdleSessi`
- L1916 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.reconcileReceipt(reconcile, session.sessionId, receipt, recorded.has(reconcile))`
- L2052 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.withQueuePosition(message)`

### apps/server/src/modules/issues/service/reads.ts (13)
- L99 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.toWire(r, commentCounts, batch)`
- L110 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.toWire(r, commentCounts, batch)`
- L123 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.toWire(r, commentCounts, batch)`
- L179 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.toWire(r, commentCounts, batch)`
- L321 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.computeBlocked(row, batch)`
- L361 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.epicStatus(r.id, mayRead)`
- L362 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.toWire(r, commentCounts, batch)`
- L408 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.toWire(r, commentCounts, batch)`
- L498 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.toWire(r, commentCounts, batch)`
- L545 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.toWire(r, commentCounts, batch)`
- L700 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.niceRef(b)`
- L754 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.niceRef(k)`
- L777 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.niceRef(i)`

### apps/server/src/modules/issues/service/crud.ts (12)
- L244 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.draftOrThrow(this.store.resolveRef(id))`
- L1421 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.deps.store.issues.upsertIssue(row, { expectedRevision: current.revisi`
- L1424 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.toWire(row)`
- L1429 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.emitEvent('issue.deleted', row.id, { seq: row.seq, deletedAt })`
- L1431 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.broadcastList()`
- L1535 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.deps.store.issues.upsertIssue(row, { expectedRevision: current.revisi`
- L1538 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.toWire(row)`
- L1543 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.emitEvent('issue.restored', row.id, { seq: row.seq, restoredAt })`
- L1545 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.broadcastList()`
- L1553 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.deps.store.issues.setIssueLabels(asIssueId(id), labels)`
- L1575 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.deps.store.grants.upsert({ resourceKind: 'issue', resourceId: r`
- L1596 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.deps.store.grants.remove('issue', row.id, grantee, verb)`

### apps/server/src/modules/sessions/command-plane.ts (11)
- L650 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `ctx.target(input.sessionId, 'sessions.kill')`
- L655 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `ctx.target(input.sessionId, 'sessions.hibernate')`
- L656 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `ctx.sessions.hibernateSession(input)`
- L660 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `ctx.target(input.sessionId, 'sessions.interrupt')`
- L672 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `ctx.sessions.interruptTurn({ sessionId: input.sessionId, ...(input.message`
- L697 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `ctx.target(input.sessionId, 'sessions.configure')`
- L700 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `ctx.sessions.configureSession(input)`
- L704 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `ctx.target(input.sessionId, 'sessions.resurrect')`
- L716 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `ctx.target(input.sessionId, 'sessions.answerAskUserQuestion')`
- L724 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `ctx.target(input.sessionId, 'sessions.continue')`
- L736 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `ctx.target(input.sessionId, 'sessions.stop')`

### apps/server/src/modules/messages/service.test.ts (10)
- L259 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `store.issues.addIssueMessage(row)`
- L261 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `store.issues.markIssueMessagesRead(FIRST_ADMIN_USER_ID, issueId, ids, 'tr')`
- L2207 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `svc.send( { kind: 'operator' }, { to: { kind: 'session', id: asSessionId('`
- L2213 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `svc.send( { kind: 'operator' }, { to: { kind: 'session', id: asS`
- L2841 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `svc.dismiss(dismissed.message.id, 's1')`
- L4408 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `store.messages.addMessage( queuedDeliveryRow( 'msg_reentrant`
- L4415 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `svc.onSessionEligibilityChanged(asSessionId('s1'), idle)`
- L4678 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `svc.onIssueEligibilityChanged(id)`
- L4681 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `svc.onIssuesEligibilityChanged(batchIds)`
- L4760 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `svc.reconcileQueued()`

### apps/server/src/modules/settings/service.commands.test.ts (9)
- L127 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `service.setSettingsFor(USER, { ...current, apiKeys: { ...current.apiKeys, `
- L134 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `service.setSettingsFor(USER, { ...current, apiKeys: { ...current.apiKeys, `
- L156 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `service.setSettingsFor(USER, { ...current, apiKeys: { ...current.apiKeys, anthropic: '' } `
- L169 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `service.setSettingsFor(USER, { ...current, apiKeys: { ...current.apiKeys, `
- L193 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `service.setSettingsFor(USER, next)`
- L277 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `service.updatePreferences(USER, { 'hibernation.memoryPct': 'not a number' })`
- L279 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `service.updatePreferences(USER, { 'gitWorkflow.mergeStyle': 'octopus' })`
- L286 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `service.updatePreferences(USER, { 'apiKeys.openai': 'sk-via-patch' })`
- L292 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `service.updatePreferences(USER, { [key]: 'x' })`

### apps/server/src/modules/issues/service/issue-registry-model.test.ts (8)
- L111 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `svc.update(id, { title: 'from the inner write' })`
- L113 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `svc.update(id, { title: 'from the outer write' })`
- L132 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `svc.get(id)`
- L135 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `svc.update(id, { title: 'never committed' })`
- L167 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `svc.update(id, { title: 'winner' })`
- L169 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `svc.update(id, { title: 'loser' })`
- L183 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `svc.get(id)`
- L184 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `svc.list()`

### apps/server/src/modules/superagent/registry.ts (8)
- L68 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `s.sendTurn({ ...input, ownerUserId })`
- L73 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `s.interruptTurn({ ...input, ownerUserId })`
- L78 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `s.openInTerminal({ ...input, ownerUserId })`
- L83 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `s.clear(ownerUserId, input.threadId)`
- L88 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `s.restartThread({ ...input, ownerUserId })`
- L93 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `s.ensureSession({ ...input, ownerUserId })`
- L98 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `s.startBtwTurn({ ...input, ownerUserId })`
- L103 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `s.conciergeTurn({ ...input, ownerUserId })`

### apps/server/src/modules/superagent/service.ts (8)
- L269 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.reapStaleTurns()`
- L293 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `service.resumePendingTurns(machineId)`
- L901 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.superagent .listPendingTurns()`
- L904 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.dispatchPendingTurn(current)`
- L1123 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.superagent .listPendingTurns()`
- L1126 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.finishPendingTurn(still, { ok: false, error: 'stopped' })`
- L1514 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.sessionInfo(s.sessionId)`
- L1571 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.sessionInfo(id)`

### apps/server/src/modules/shipping/service.test.ts (7)
- L114 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `issues.children(id, recursive)`
- L647 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `service.deliveryReceipt({ orderId, principal: approval.principal, `
- L1158 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `service.cancel({ orderId: input.order.id, principal: approval.`
- L1422 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `store.shipping.claimAttempt({ orderId: order.id, expectedState: 'preflight`
- L1436 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `store.shipping.finishAttempt(claimed.attempt.id, 2, { finishedAt: '2026-08-13T10:0`
- L1442 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `store.shipping.finishAttempt(claimed.attempt.id, 1, { finishedAt: '2026-08-13T10:0`
- L1524 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `store.shipping.claimAttempt({ orderId: order.id, expectedState: 'preflight`

### apps/server/src/issues.attach.test.ts (7)
- L169 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `svc.attachSession({ sessionId: asSessionId('s1'), targetId: other.id })`
- L234 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `svc.attachSession({ sessionId: asSessionId('s1'), newSubissue: { title: 'x`
- L333 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `svc.attachSession({ sessionId: asSessionId('s1'), newSpinoff: { title: 'x'`
- L339 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `svc.attachSession({ sessionId: asSessionId('s1'), newSubissue: { title: 'a`
- L349 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `svc.attachSession({ sessionId: asSessionId('s1'), newSpinoff: { title: 'x'`
- L437 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `svc.attachSession({ sessionId: asSessionId('s1') })`
- L439 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `svc.attachSession({ sessionId: asSessionId('s1'), targetId: 'iss_nope' })`

### apps/server/src/modules/settings/registry.ts (7)
- L76 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `svc.updatePreferences(actor, input.values)`
- L87 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `svc.updatePreferences(actor, input.values)`
- L94 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `svc.setSecret(input.key, input.value)`
- L101 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `svc.clearSecret(input.key)`
- L118 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `svc.secretPresenceList()`
- L125 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `svc.startTelegramSetup(actor)`
- L132 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `svc.pollTelegramSetup(input.setupId)`

### apps/server/src/store/shipping.ts (7)
- L1020 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.getOrder(id)`
- L1053 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.db .select({ branch: issues.branch, machineId`
- L1184 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.latestAttemptForOrder(order.id)`
- L1185 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.claimAttempt({ orderId: order.id, expectedState: 'queued', `
- L1650 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.getOrder(member.orderId)`
- L1654 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.getAttempt(member.attemptId)`
- L1716 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.db .select({ repoPath: issues.repoPath, machineId: issues.machineId }) `

### apps/server/src/modules/updates/operation.test.ts (6)
- L1289 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `driver()?.active(LIFECYCLE_EXCLUSION_GROUP)`
- L1291 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `driver()?.active(LIFECYCLE_EXCLUSION_GROUP)`
- L1317 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `driver().recordDetails(id, patch)`
- L1330 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `driver().recordProgress(id, stepId, patch)`
- L1335 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `driver().watching(id, stepId)`
- L1380 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `store.get(id)`

### apps/server/src/store.test.ts (6)
- L64 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `store.repos.addRepo('/home/u/fenced', store.hostMachineId)`
- L66 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `store.repos.addRepo('/home/u/reopened', store.hostMachineId)`
- L198 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `s.sessions.upsertSession(row({ agentKind: 'auto' }))`
- L514 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `store.sessions.markSessionRead(asUserId(''), asSessionId('s_read'), 't')`
- L740 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `store.sessions.setTabOrder(asUserId(SOLE_USER_ID), ' ', ['s1'])`
- L1043 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `s.superagent.loadSuperagentMessages('global')`

### apps/server/src/issues.ledger.test.ts (6)
- L97 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `ledger.commit({ write: async () => await store.issues.upsertIssue({ ...row, title:`
- L167 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `svc.create({ repoPath: '/r', title: 'phantom', startNow: false })`
- L196 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `svc.update(wire.id, { title: 'phantom' })`
- L236 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `svc.setLabels(wire.id, ['urgent'])`
- L257 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `svc.purgeEmptyDraft(wire.id)`
- L388 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `ledger.commit({ write: async () => await store.issues.upsertIssue({ ...row, title:`

### apps/server/src/modules/sessions/session-state/service.ts (6)
- L340 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.ports.store.sessions.markSessionRead( principal.userId, sessionId, `
- L351 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.ports.store.sessions.markSessionUnread(principal.userId, sessionId)`
- L363 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.ports.store.sessions.setSnooze(principal.userId, sessionId, until)`
- L370 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.ports.store.sessions.clearSnooze(principal.userId, sessionId)`
- L378 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.ports.store.sessions.clearAllSnoozes(sessionId)`
- L676 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.writeDraftDoc(this.draftDocs.get(sessionId) ?? doc)`

### apps/server/src/gateway/feed-serving.ts (6)
- L304 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.serveWorld(peer, principal, routingPrincipal, cause)`
- L307 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.canResume(peer, resumeFrom)`
- L311 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.serveWorld(peer, principal, routingPrincipal, 'cursor-rejected')`
- L314 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.serveResume(peer, principal, routingPrincipal, resumeFrom)`
- L658 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.serveWorld(peer, principal, routingPrincipal, 'version-change')`
- L702 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.publish(principal, delivery)`

### apps/server/src/modules/messages/characterization-support.ts (5)
- L248 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `openTestStore(':memory:')`
- L403 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `issues.create({ ...input, startNow: false })`
- L465 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `issues.create({ repoPath: input.repoPath ?? '/repo', title: input.title, `
- L474 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `issues.update(issueId, { worktreePath })`
- L477 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `issues.update(issueId, { archived: true })`

### apps/server/src/modules/issues/service/row-install-fold.test.ts (5)
- L48 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `store.transact(async () => { await issues.update(created.id, { title: 'renamed ins`
- L100 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `store.transact(async () => { await issues.update(created.id, { title: 'touched bef`
- L125 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `store.transact(async () => { await issues.create({ repoPath: '/repo', title: 'orph`
- L153 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `store.transact(async () => { await issues.create({ repoPath: '/repo', title: 'neve`
- L211 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `store.transact(async () => { await issues.create({ repoPath: '/repo', title: 'or`

### apps/server/src/modules/lifecycle-runtime-fold.test.ts (5)
- L50 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `store.transact(() => { registry.modules.sessions.killSession({ sessionId }) `
- L88 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `store.transact(async () => { await registry.modules.issueSessionLifecycle.deleteIs`
- L120 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `store.transact(async () => { await registry.modules.issueSessionLifecycle.deleteIs`
- L156 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `store.transact(async () => { await registry.modules.issueSessionLifecycle.restoreI`
- L186 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `store.transact(async () => { await registry.modules.issueSessionLifecycle.restoreI`

### apps/server/src/modules/maintenance/service.ts (5)
- L121 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.maintenance.getLease(LEASE_NAME)`
- L131 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.maintenance.putLease({ name: LEASE_NAME, generationId: request.`
- L463 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.maintenance.getLease(LEASE_NAME)`
- L470 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.maintenance.getCommand(command.jobKind, command.runKey)`
- L478 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.maintenance.recordCommand( { status: 'applied', jobKind: command.jobKin`

### apps/server/src/modules/fleet/authz.ts (5)
- L355 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `sessions.sessionSpawnedBy(sessionId)`
- L361 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `machines.defaultMachine()`
- L362 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `machines.ownershipRows()`
- L363 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `machines.ownershipRows()`
- L364 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `machines.effectiveOwner(machineId)`

### apps/server/src/modules/sessions/command-ctx.ts (5)
- L81 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `sessions.sessionSpawnedBy(sessionId)`
- L116 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `issues.createDraftFor(repoPath, agentKind, issueId, ownership)`
- L120 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `issues.discardUnlaunchedDraft(issueId)`
- L123 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `sessions.listSessions()`
- L124 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `sessions.sessionById(sessionId)`

### apps/server/src/feed-visibility.ts (5)
- L682 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `store.grants.visibilityAudienceFor('issue', ref.entityId)`
- L684 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `currentBootstrapReadCache()`
- L687 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `sessionsForIssue(cache, ref.entityId)`
- L714 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `durableChangeValueOf(ref)`
- L722 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `store.grants.visibilityRevision()`

### apps/server/src/modules/sessions/session-state/registry.ts (5)
- L336 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `deps.state.listSnoozes(principal)`
- L343 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `deps.state.listSnoozes(principal)`
- L349 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `deps.state.setPin(principal, input.kind as never, str(input.id), input.pinned === true)`
- L355 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `deps.state.setTabOrder( principal, str(input.worktree), (input.ses`
- L378 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `deps.state.setDraft( { sessionId: sessionIdOf(input.sessionId), text: edit.text },`

### apps/server/src/modules/issues/service/mail.ts (5)
- L44 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.deps.store.issues.addIssueComment({ id: `cmt_${randomUUID()}`, `
- L138 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.deps.store.issues.markIssueMessagesRead( this.store.broadcastView`
- L155 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.deps.store.messages.markDeliveredByPull(mid, reader ?? null, at)`
- L159 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.deps.store.messages.recordRead(mid, reader, at)`
- L230 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.reports().get(fromIssue)`

### apps/server/src/modules/issues/service/core.ts (5)
- L812 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.toWireMemo(r, commentCounts, batch, prefixFor(r.repoPath))`
- L1185 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.projectionChanges(row)`
- L1260 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.toWire(row, undefined, batch)`
- L1262 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.deps.store.events.appendEvent( { ts: this.now(), `
- L1286 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.projectionChanges(row)`

### apps/server/src/modules/messages/gate-agent.test.ts (4)
- L133 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `store.notificationFacts.retire(factKey, target, opts?.now?.() ?? new Date().toISOString())`
- L152 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `store.events.appendEvent(e)`
- L955 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `svc.onSessionIdle(target({}))`
- L995 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `svc.onSessionIdle(target({}))`

### apps/server/src/modules/issues/service/hard-delete-fold.test.ts (4)
- L47 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `store.transact(async () => { await registry.issues.purgeEmptyDraft(doomed.id) `
- L92 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `store.transact(async () => { await registry.issues.update(bystander.id, { title: '`
- L121 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `store.transact(async () => { await registry.issues.purgeEmptyDraft(parent.id) `
- L142 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `store.transact(async () => { await registry.issues.purgeEmptyDraft(doomed.id) `

### apps/server/src/modules/automations/scheduler.test.ts (4)
- L14 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `super.create(input, TEST_PRINCIPAL)`
- L18 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `super.update(id, patch, TEST_PRINCIPAL)`
- L22 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `super.setEnabled(id, enabled, TEST_PRINCIPAL)`
- L761 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `h.service.tick()`

### apps/server/src/migrations/restore.test.ts (4)
- L85 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `registry.current()`
- L332 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `restoreDatabase({ backupPath, dbPath, freeBytes: () => 1 })`
- L345 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `restoreDatabase({ backupPath: `${dbPath}.nope`, dbPath, freeBytes: PLENTY })`
- L347 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `restoreDatabase({ backupPath: dbPath, dbPath, freeBytes: PLENTY })`

### apps/server/src/transcript-indexer.test.ts (4)
- L75 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `indexer.onBytes(m, n, p)`
- L76 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `indexer.onTruncate(m, n)`
- L209 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `indexer.onBytes(asMachineId('m1'), 'nofts', '/nonexistent/lake.jsonl')`
- L213 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `indexer.onTruncate(asMachineId('m1'), 'nofts')`

### apps/server/src/machine-identity.test.ts (4)
- L85 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `openTestStore(path, HOST)`
- L106 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `openTestStore(path, HOST)`
- L190 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `openTestStore(path, HOST)`
- L323 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `store.sessions.upsertSession(row)`

### apps/server/src/ledger.commit-application.test.ts (4)
- L89 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `store.transact(async () => { await ledger.commit({ write: async () => { `
- L138 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `ledger.authority.snapshot('conversation')`
- L170 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `ledger.commit({ arbitrate: { rowId: expRevRow.id, attempt: { e`
- L193 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `ledger.commit({ write: () => { throw new Error('the write failed') `

### apps/server/src/ledger.baseline-fold.test.ts (4)
- L45 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `ledger.authority.snapshot('conversation')`
- L53 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `store.transact(async () => { // A nested ledger.commit: its own transact span degr`
- L88 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `store.transact(async () => { await ledger.commit({ write: () => {}, `
- L169 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `store.transact(async () => { await ledger.commit({ write: () => {}, `

### apps/server/src/enrollment-durability.test.ts (4)
- L63 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `store.users.get(id)`
- L248 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `afterReconcile.store.users.get(id)`
- L348 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `store.users.get(id)`
- L415 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `restarted.store.users.get(id)`

### apps/server/src/modules/automations/registry.ts (4)
- L62 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `service.create(input, principal)`
- L67 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `service.update(input.id, input.patch, principal)`
- L72 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `service.setEnabled(input.id, input.enabled, principal)`
- L77 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `service.remove(input.id, principal)`

### apps/server/src/modules/messages/mailbox.ts (4)
- L425 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.deps.messages.listMessagesFor(p, { limit: opts?.limit ?? 50 })`
- L450 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.deps.messages.recordRead(m.id, opts.consume, at)`
- L452 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.deps.messages.markRead(m.id, opts.consume ?? null, at)`
- L453 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.retireNotificationFact(m, at)`

### apps/server/src/modules/sessions/workspace.ts (4)
- L190 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `rpc.repoOp('revParseVerify', sourceRepoPath, { ref }, sourceMachineId)`
- L195 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `rpc.repoOp('revParseVerify', input.targetRepoPath, { ref }, input.targetMachineId)`
- L436 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.ports.rpc.repoOp('revParseVerify', sourceRepo.path, { ref }, source.machineId)`
- L442 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.ports.rpc.repoOp('revParseVerify', fetcherRepo.path, { ref }, caller.machineId)`

### apps/server/src/modules/sessions/handoff/preflight.ts (4)
- L110 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.ports.rpc.repoOp('revParseVerify', sourceRepo.path, { ref }, session.machineId)`
- L116 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.ports.rpc.repoOp('revParseVerify', targetRepo.path, { ref }, input.machineId)`
- L139 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.ports.rpc.repoOp('revParseVerify', targetRepo.path, { ref }, input.machineId)`
- L145 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.ports.rpc.repoOp('revParseVerify', sourceRepo.path, { ref }, session.machineId)`

### apps/server/src/modules/sessions/daemon-lifecycle.ts (4)
- L598 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.observationCheckpoints.rebindExact({ sessionId: session.sessionId`
- L754 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.observationCheckpoints.save(outcome.checkpoint)`
- L757 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.observationCheckpoints.recordTerminalCandidate( candidateFacts,`
- L762 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.observationCheckpoints.cancelTerminalCandidate(session.sessionId)`

### apps/server/src/modules/settings/service.ts (4)
- L285 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.getModelCatalog(machineId)`
- L286 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.setModelCatalog(snapshot)`
- L375 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.secrets.getOrEmpty(key)`
- L567 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.secrets.getOrEmpty(row.key)`

### apps/server/src/modules/messages/handlers/context.ts (4)
- L271 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.deps.issues.resolveRef(ref)`
- L272 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.deps.issues.has(id)`
- L286 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.deps.issues.resolveRef(r)`
- L287 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.deps.issues.has(id)`

### apps/server/src/modules/memory/search.ts (4)
- L83 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `scopedVisibility.mayRead(reader, { class: 'conversation', machineI`
- L127 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `visibility.mayRead(reader, { class: 'issue', id: row.id })`
- L209 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `visibility.mayRead(reader, { class: 'transcript', machineId: row.machi`
- L226 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `visibility.mayRead(reader, { class: 'session', id: candidate.id })`

### apps/server/src/modules/memory/lake.ts (4)
- L86 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `indexer.onBytes(machineId, nativeId, lakePath)`
- L87 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `indexer.onTruncate(machineId, nativeId)`
- L88 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `indexer.onIncarnation(machineId, nativeId)`
- L228 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.requestRead(machineId, request)`

### apps/server/src/store/executor/span-side-effects.test.ts (3)
- L75 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `store.transact(async () => { await ledger.commit({ write: () => 'ok', changes: () `
- L115 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `ledger.commit({ write: () => 'ok', changes: () => [...upsert('second', { v: 1 })] })`
- L182 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `store.transact(async () => { await store.events.appendEvent({ ts: new Da`

### apps/server/src/store/user-preferences.test.ts (3)
- L158 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `settings.userPreferences.set(ALICE, 'gitWorkflow.mergeStyle', 'pr', AT)`
- L161 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `settings.userPreferences.set(ALICE, 'apiKeys.openai', 'sk-x', AT)`
- L167 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `settings.userPreferences.set(ALICE, 'sidebar.repoSort', 'custom', AT)`

### apps/server/src/store/user-layout.test.ts (3)
- L37 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `layout.setMany(ALICE, { dockTab: 'mail', 'not.a.key': 1 }, AT)`
- L57 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `layout.set(ALICE, 'view', 'workspace', AT)`
- L58 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `layout.set(ALICE, 'podium.view', 'workspace', AT)`

### apps/server/src/store.refs.test.ts (3)
- L32 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `s.repos.addRepo('/b/thing', asMachineId('__local__'), undefined, 'lower')`
- L34 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `s.repos.addRepo('/c/thing', asMachineId('__local__'), undefined, 'PDM')`
- L56 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `s.repos.setRepoPrefix(asMachineId('__local__'), '/a/podium', otherPrefix)`

### apps/server/src/session-cutover.audit.test.ts (3)
- L183 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `modules.issues.createDraftFor(repoPath, agentKind, issueId, ownership)`
- L187 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `modules.issues.discardUnlaunchedDraft(issueId)`
- L190 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `modules.sessions.listSessions()`

### apps/server/src/feed-bootstrap-scaling.test.ts (3)
- L404 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `internals(small).ledger.authority.bootstrap(feedPrincipal)`
- L407 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `internals(large).ledger.authority.bootstrap(feedPrincipal)`
- L439 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `ledger.capture([ { entity: 'issue', id: 'iss_shared_0', op: 'upsert', value: { i`

### apps/server/src/modules/messaging/service.ts (3)
- L219 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.onTurnEnded(ev)`
- L220 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.sendUserNotice(request)`
- L277 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.onInbound(msg)`

### apps/server/src/modules/updates/trpc.ts (3)
- L331 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `input.operations.engine.recordDetails(operationId, patch)`
- L340 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `input.operations.engine.recordProgress(operationId, stepId, patch)`
- L344 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `input.operations.engine.watching(operationId, stepId)`

### apps/server/src/modules/updates/dev-bundle.ts (3)
- L2299 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `fs.remove(artifact.path)`
- L2300 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `fs.remove(artifact.path + DEV_BUNDLE_SIGNATURE_SUFFIX)`
- L2301 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `fs.remove(artifact.path + DEV_BUNDLE_METADATA_SUFFIX)`

### apps/server/src/store.ts (3)
- L320 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.observationCheckpoints.purge(id)`
- L323 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.repos.resolveRepoIdForPath(repoPath)`
- L327 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.issues.assignRepoIdToIssuesUnder(repoId, repoPath)`

### apps/server/src/modules/sessions/session-teardown.ts (3)
- L338 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.ports.store.observationCheckpoints.cancelTerminalCandidate(input.sessionId)`
- L572 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.ports.store.observationCheckpoints.get(sessionId)`
- L579 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.ports.store.observationCheckpoints.consumeTerminalCandidate( curren`

### apps/server/src/modules/sessions/session-revival.ts (3)
- L248 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.ports.store.repos.listRepos()`
- L249 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.ports.machines.listMachines()`
- L267 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.ports.store.events.appendEvent(event)`

### apps/server/src/modules/sessions/view.ts (3)
- L238 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.wire(session, principal, memo)`
- L313 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.ports.store.issues.allocateSessionLetter(birthIssueId)`
- L321 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.ports.store.repos.nextDraftSeq(repoId)`

### apps/server/src/modules/sessions/runtime-event-gate.ts (3)
- L182 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.ports.events.saveRuntimeEventCheckpoint({ ...current, obs`
- L233 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.ports.events.appendEvent( { ts: event.at, kind: RUN`
- L242 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.ports.events.saveRuntimeEventCheckpoint(next)`

### apps/server/src/modules/messages/scheduler.ts (3)
- L179 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.flushDeliveryTriggers()`
- L337 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.runReconcilePage(next)`
- L396 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.runRetryBackstopPage(next)`

### apps/server/src/modules/issues/command-ctx.ts (3)
- L135 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `tracker.reports.has(id)`
- L137 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `tracker.hierarchy.ancestorIds(id)`
- L223 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.mayReadIssue(row.id)`

### apps/server/src/modules/issues/authority-arbitration.ts (3)
- L50 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.commit(op)`
- L51 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.source.capture(specs)`
- L52 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.source.reconcile(entity, rows)`

### apps/server/src/modules/machines/service.ts (3)
- L652 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.deps.store.repos .listRepos(machine.id)`
- L837 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.deps.store.repos .listRepos(id)`
- L944 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.effectiveOwner(m.id)`

### apps/server/src/store/maintenance.golden.test.ts (2)
- L156 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `store.maintenance.pruneCommandsBatch('2026-09-02T00:00:00.000Z', bad)`
- L178 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `store.maintenance.getCommand('message-expiry', 'run-1')`

### apps/server/src/modules/shipping/lease-projection-fold.test.ts (2)
- L42 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `store.transact(() => { const pinned = leases.pin(['order-1']) expect(lease`
- L82 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `store.transact(() => { leases.delete('order-1') expect(leases.get('order-1`

### apps/server/src/modules/sessions/repository.durable-baseline-fold.test.ts (2)
- L82 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `f.store.transact(() => { // A NESTED persist: the ledger's span degrades to a save`
- L107 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `f.store.transact(() => { f.repo.persist(f.session) throw new Error('enclos`

### apps/server/src/modules/sessions/oracle-decomposition.test.ts (2)
- L523 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `o.reg.modules.sessions.createSession({ agentKind: 'claude-code', cwd: `
- L535 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `o.reg.modules.sessions.createSession({ agentKind: 'claude-code', cwd: `

### apps/server/src/modules/memory/conversation-list-fold.test.ts (2)
- L56 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `store.transact(async () => { await memory.onDiscovery(machine, [conversation('c-ro`
- L88 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `store.transact(async () => { await memory.setConversationMeta(SYSTEM_READER, { id:`

### apps/server/src/modules/approvals/service.test.ts (2)
- L44 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `svc.request({ op, sessionId: asSessionId('s1'), machineId: 'm1' })`
- L107 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `svc.approve(id)`

### apps/server/src/modules/funnel.test.ts (2)
- L127 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `funnel.run({ authorize: () => { throw new Error('forbidden') }, `
- L320 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `ledger.commit({ write: () => {}, changes: () => [{ entity: 'issue', id: 'i`

### apps/server/src/gateway/feed-test-plumbing.ts (2)
- L100 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `store.sync.minChangeSeq()`
- L102 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `store.grants.visibilityRevision()`

### apps/server/src/superagent-headless.test.ts (2)
- L386 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `h.sa.clear(FIRST_ADMIN_USER_ID, asThreadId('global'))`
- L1014 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `h.sa.interruptTurn({ ownerUserId: FIRST_ADMIN_USER_ID, threadId: asThreadId('btw_none') })`

### apps/server/src/store.repo-id.test.ts (2)
- L186 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `openTestStore(file)`
- L206 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `openTestStore(file)`

### apps/server/src/store.issues.test.ts (2)
- L147 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `store.issues.upsertIssue(baseRow({ id: asIssueId(id), seq: 100 + i }))`
- L266 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `store.issues.setIssueUserState(asUserId(''), asIssueId('iss_read'), { readAt: 't' })`

### apps/server/src/sessions.ledger.test.ts (2)
- L88 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `ledger.commit({ write: async () => await store.sessions.upsertSession({ `
- L631 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `registry.modules.machines.renameMachine( registry.modules.machines.hostMachineId`

### apps/server/src/relay.test.ts (2)
- L336 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `reg.modules.sessions.createSession({ agentKind: 'claude-code', cwd: '/othe`
- L7105 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `SessionRegistry.create(undefined, undefined, { instanceId: 'default' })`

### apps/server/src/relay.issue-session-delete.test.ts (2)
- L104 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `registry.modules.issueSessionLifecycle.deleteIssue(issue.id)`
- L137 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `registry.modules.issueSessionLifecycle.restoreIssue(issue.id)`

### apps/server/src/machine-capability-guard.test.ts (2)
- L99 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `registry.modules.machines.requireRepoHostStructure(coordinator)`
- L152 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `registry.modules.issues.create({ title: 'homed wrongly', repoPath: '/home/`

### apps/server/src/issues.boot-quarantine.test.ts (2)
- L67 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `rebooted.init()`
- L89 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `rebooted.init()`

### apps/server/src/event-log.test.ts (2)
- L229 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `store.events.appendEvent({ ts: daysAgo(0), kind: 'k', subject: 's' })`
- L313 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `svc.create({ repoPath: '/r', title: 'A', startNow: false })`

### apps/server/src/conversations.ledger.test.ts (2)
- L120 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `ledger.commit({ // BOTH store methods open their own transaction() internally — in`
- L272 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `registry.modules.memory.setConversationMeta( { kind: 'user', id: FIRST_ADMIN_USER_`

### apps/server/src/modules/models/registry.ts (2)
- L55 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `state.settings.refreshModelCatalog( input?.machineId ?? state.defaultMachine(), `
- L82 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `modules.machines.defaultMachine()`

### apps/server/src/modules/files/registry.ts (2)
- L71 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `assertAllowedRoot(state, input.root)`
- L72 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `state.rpc.writeFile(input)`

### apps/server/src/modules/approvals/registry.ts (2)
- L51 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `svc.approve(input.id)`
- L58 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `svc.deny(input.id)`

### apps/server/src/modules/accounts/queries.ts (2)
- L42 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `state.settings.apiKeyFor(provider)`
- L49 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `state.machineService .listMachines()`

### apps/server/src/modules/accounts/registry.ts (2)
- L64 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `state.accounts.upsert({ id: asAccountId(id), provider: input.provider, `
- L80 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `state.accounts.remove(input.id)`

### apps/server/src/modules/sessions/session-wiring.ts (2)
- L185 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `runtimeEventGate?.ready(sessionId)`
- L563 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `projectMachinesForPrincipal( { machines: bag.machines }, userCommandPrinci`

### apps/server/src/modules/sessions/concurrency-history.ts (2)
- L122 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.capture()`
- L123 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.capture()`

### apps/server/src/modules/sessions/activity-history.ts (2)
- L73 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.record(sessionId, next.phase, prev?.phase)`
- L78 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.record(sessionId, 'ended', undefined)`

### apps/server/src/modules/superagent/tools.ts (2)
- L134 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `issues.getMeta(issueId)`
- L779 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `issues.getMeta(r.id)`

### apps/server/src/modules/operations/engine.ts (2)
- L1010 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.enqueue(operationId, async () => { await this.expireWaiting(operationId, `
- L1237 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.enqueue(operationId, async () => await this.onDeadline(operationId))`

### apps/server/src/modules/workflows/service.ts (2)
- L338 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.toRun(row)`
- L458 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.deps.store.getProfile(step.executionProfileId)`

### apps/server/src/modules/messages/handlers/inbox-consume.ts (2)
- L45 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `access.wire(m)`
- L52 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `access.wire(m)`

### apps/server/src/modules/issues/git-watch.ts (2)
- L37 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.sweep()`
- L38 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.sweep()`

### apps/server/src/modules/issues/auto-archive.ts (2)
- L29 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.sweep()`
- L30 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.sweep()`

### apps/server/src/modules/quota-history/service.ts (2)
- L114 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.sampleNow()`
- L115 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.sampleNow()`

### apps/server/src/modules/events/retention.ts (2)
- L43 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.schedulePrune()`
- L44 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.schedulePrune()`

### apps/server/src/modules/automations/scheduler.ts (2)
- L33 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.tick()`
- L34 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.tick()`

### apps/server/src/modules/machines/rpc.ts (2)
- L620 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.request( QUOTA_HISTORY, 120_000, () => ({ samples: [] }`
- L1366 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.transcriptPathHint(reader, session)`

### apps/server/src/modules/issues/service/assistant.ts (2)
- L56 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.refreshAssistant(row.id)`
- L102 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.d.store.secrets.apiKeyFor(provider)`

### apps/server/src/modules/issues/service/hierarchy.ts (2)
- L61 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.deps.store.issues.addIssueDep(fromId, toId, type)`
- L80 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.deps.store.issues.removeIssueDep(fromId, toId, type)`

### apps/server/src/modules/issues/service/attention.ts (2)
- L296 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.deps.store.issues .listIssueDeps(row.id)`
- L491 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.deps.store.events.setSubscriptionEnabled(id, enabled)`

### apps/server/src/store/repos.ts (2)
- L102 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.invalidateRegistry()`
- L219 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.isPrefixTaken(p)`

### apps/server/src/store/spike/turso-append/run-lease.ts (1)
- L123 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `take(Date.now())`

### apps/server/src/store/executor/synchronous-span.test.ts (1)
- L382 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `store.transact(() => { postCommit().effect(() => void ran.push('effect'), 'e') `

### apps/server/src/store/executor/executor.test.ts (1)
- L518 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `scheduler.run('read', async () => { order.push(`read${i}:start`) await par`

### apps/server/src/store/user-read-position.test.ts (1)
- L89 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `cursors.advance(ALICE, 'notAStream', { lastEventId: 1, seenAt: null }, AT)`

### apps/server/src/store/shipping-uncovered.test.ts (1)
- L352 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `s.shipping.isolateTrainFailure({ trainId: claim.manifest.id, leaderOrderId`

### apps/server/src/store/session-oom-death.test.ts (1)
- L81 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `sessions.upsertSession(row('sess-oom', { stopReason: 'oom', oomKilledAt: DIED_AT }))`

### apps/server/src/store/runtime-events.test.ts (1)
- L939 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `gate.replayBoardProjection()`

### apps/server/src/store/repos-read-cost.test.ts (1)
- L82 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `repos.resolveRepoIdForPath(p)`

### apps/server/src/store/quota-history.test.ts (1)
- L49 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `store.quotaHistory.record( sample({ resetsAtMs: Date.parse(`2026-08-25T${t}Z`), at`

### apps/server/src/store/observation-checkpoints.test.ts (1)
- L51 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `store.observationCheckpoints.save(checkpoint)`

### apps/server/src/store/locks.golden.test.ts (1)
- L198 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `store.locks.deleteLock(repo, 'build')`

### apps/server/src/modules/workflows/service.test.ts (1)
- L439 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `service.prepareStart({ sessionId: asSessionId('s2'), cwd: '/repo/wt', `

### apps/server/src/modules/workflows/multi-user.test.ts (1)
- L578 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `h.service.executionProfileForLaunch({ profileId: profile.id, caller: p`

### apps/server/src/modules/settings/wiring.test.ts (1)
- L215 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `store.settingsAudit.append({ command: 'settings.updateInstance', outcome: `

### apps/server/src/modules/sessions/spawn-name.test.ts (1)
- L52 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `reg.modules.sessions.createSession({ agentKind: 'shell', cwd: '/proj', `

### apps/server/src/modules/sessions/session-start.test.ts (1)
- L402 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `reg.modules.sessions.createSession({ agentKind: 'shell', cwd: '/other', `

### apps/server/src/modules/sessions/repository.volatile-slice.test.ts (1)
- L125 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `repo.drainVolatileCaptureSlice()`

### apps/server/src/modules/sessions/rename-shadow.test.ts (1)
- L74 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `sessions.listSessions()`

### apps/server/src/modules/sessions/oracle-session-state.test.ts (1)
- L467 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `o.store.sessions.loadDrafts()`

### apps/server/src/modules/sessions/oracle-handoff.test.ts (1)
- L238 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `store.repos.listRepos(asMachineId('m1'))`

### apps/server/src/modules/sessions/oracle-ask-upload.test.ts (1)
- L166 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `o.store.messages.listLedger({ sessionId: target.sessionId })`

### apps/server/src/modules/sessions/model-validation-wiring.test.ts (1)
- L64 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `reg.modules.sessions.createSession({ agentKind: 'codex', cwd: '/tmp/x', `

### apps/server/src/modules/operations/engine.test.ts (1)
- L1511 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `store.get(row.id)`

### apps/server/src/modules/messages/scheduler.single-flight.test.ts (1)
- L78 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `h.scheduler.sweep()`

### apps/server/src/modules/messages/characterization.delivery.test.ts (1)
- L1003 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `h.svc.onTranscriptDelta(asSessionId('s1'), [ { role: 'user', text: `podium message`

### apps/server/src/modules/messages/authz.test.ts (1)
- L711 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `h.svc.send( fromKind === 'system' ? { kind: 'system', name: 'steward' } : { kind`

### apps/server/src/modules/issue-events/feed.test.ts (1)
- L151 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `publisher.publish(1, event())`

### apps/server/src/modules/approvals/service.single-flight.test.ts (1)
- L86 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `h.svc.sweepStalledExecutions()`

### apps/server/src/gateway/feed-serving.test.ts (1)
- L401 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `p.authority.cursor()`

### apps/server/src/composition/reactions.runtime.test.ts (1)
- L29 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `SessionRegistry.create(undefined, undefined, { instanceId: 'default', `

### apps/server/src/terminal-hibernation-proof.test.ts (1)
- L423 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `registry.modules.sessions.hibernateSession({ sessionId, requireTerminalProof: true })`

### apps/server/src/superagent-concierge.test.ts (1)
- L296 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `h.sa.mcpToolSpecs(threadId)`

### apps/server/src/store-issues-frame-cache.test.ts (1)
- L178 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `store.transact(async () => { await store.issues.upsertIssue(issue('iss_a', { stage`

### apps/server/src/steward.test.ts (1)
- L795 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `issues.comments(w.id)`

### apps/server/src/steward.single-flight.test.ts (1)
- L83 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `h.steward.tick()`

### apps/server/src/sessions.refs.test.ts (1)
- L97 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `reg.modules.sessions.createSession({ agentKind: 'shell', cwd: '/r/podium' })`

### apps/server/src/relay.outbox.test.ts (1)
- L152 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `reg.sessionStore.sync.listQueuedMessages(asSessionId(sessionId))`

### apps/server/src/relay.machines.test.ts (1)
- L512 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `store.repos.listRepos(asMachineId('m1'))`

### apps/server/src/modules/updates/target-refresh.ts (1)
- L125 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `tick()`

### apps/server/src/modules/updates/dev-web-build.ts (1)
- L321 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `start()`

### apps/server/src/gateway/daemon-socket.ts (1)
- L314 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `registry.modules.loginPropagation.trigger({ targetMachineId: outcome.principal`

### apps/server/src/characterization.test.ts (1)
- L362 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `reg.issues.update(w.id, { stage: 'in_progress', closedReason: 'wontfix' })`

### apps/server/src/modules/fleet/trpc.ts (1)
- L120 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `fleetAuthzDeps(ctx)`

### apps/server/src/modules/settings/authz.ts (1)
- L166 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `sessions.sessionSpawnedBy(sessionId)`

### apps/server/src/modules/read-position/authz.ts (1)
- L70 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `sessions.sessionSpawnedBy(sessionId)`

### apps/server/src/modules/misc-queries.ts (1)
- L238 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `s.modules.settings.getSettings()`

### apps/server/src/modules/layout/authz.ts (1)
- L65 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `sessions.sessionSpawnedBy(sessionId)`

### apps/server/src/modules/interactions/registry.ts (1)
- L58 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `svc.answer({ id: input.id, ...(input.text !== undefined ? { text: input.te`

### apps/server/src/modules/fleet/queries.ts (1)
- L33 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `s.repos.inferFromPath(input.path)`

### apps/server/src/modules/conversations/registry.ts (1)
- L35 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `svc.setConversationMeta(input)`

### apps/server/src/modules/accounts/native-login.ts (1)
- L36 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.onInventory(machineId)`

### apps/server/src/store/superagent.ts (1)
- L163 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.mapSuperagentThread(r)`

### apps/server/src/modules/messages/render.ts (1)
- L232 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.fromLabel(m)`

### apps/server/src/modules/sessions/lifecycle.ts (1)
- L704 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.sessionSpawnedBy(sessionId)`

### apps/server/src/modules/sessions/session-kill.ts (1)
- L142 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.removeSessionRuntime(input.sessionId, { retiredAt: deletedAt })`

### apps/server/src/modules/sessions/repository.ts (1)
- L524 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.view.wire(session, undefined, undefined, draft)`

### apps/server/src/steward.ts (1)
- L447 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.tick()`

### apps/server/src/modules/operations/trpc.ts (1)
- L26 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `operationsModule(ctx).engine.active(input?.group)`

### apps/server/src/store/users.ts (1)
- L145 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.get(row.id)`

### apps/server/src/repo-discovery.ts (1)
- L220 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.scan(machineId, { deep: false })`

### apps/server/src/modules/messages/handlers/ledger.ts (1)
- L42 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `access.wire(m)`

### apps/server/src/modules/issues/dispatcher.ts (1)
- L71 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.deps.issues.reports.get(issue.id)`

### apps/server/src/modules/issue-session-lifecycle.ts (1)
- L161 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.sweepClosedIssues('periodic')`

### apps/server/src/modules/quota-history/backfill.ts (1)
- L106 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.runOnce()`

### apps/server/src/modules/automations/service.ts (1)
- L466 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.deps.store.getRun(result.runId)`

### apps/server/src/modules/memory/visibility.ts (1)
- L263 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.mayReadSessionRow(userId, row)`

### apps/server/src/modules/issues/service/workflow.ts (1)
- L1700 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.refreshGitState(id)`

### apps/server/src/store/observation-checkpoints.ts (1)
- L193 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.mapRow(row)`

### apps/server/src/store/issues.ts (1)
- L1191 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.mapIssueMessage(r)`

### apps/server/src/migrations/snapshot-verifier.ts (1)
- L540 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.verify(candidate.path)`

### packages/sync/src/outbox/outbox.ts (1)
- L588 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.drainPartition(bucket)`

### apps/server/src/store/executor/executor.ts (1)
- L782 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `runner.effectsSettled()`

## would-change-what-the-caller-reads (824)

### apps/server/src/relay.ts (157)
- L566 — passed where (sessionId: string & BRAND<"SessionId">) => (string & BRAND<"SessionId">) | undefined is expected — that caller reads the value, not a promise
  `(candidate) => spawnedByParentSessionId( this.store.sessions.getSess`
- L570 — passed where ((sessionId: string & BRAND<"SessionId">) => (string & BRAND<"UserId">) | undefined) | undefined is expected — that caller reads the value, not a promise
  `(candidate) => this.store.sessions.getSession(asSessionId(candidate))?.ownerUser`
- L661 — passed where ((userId: string & BRAND<"UserId">) => boolean) | undefined is expected — that caller reads the value, not a promise
  `(userId) => this.store.users.get(userId) !== undefined`
- L663 — passed where (principal: ClientPrincipal, machines: MachinesService) => { id: string & BRAND<"MachineId">; name: string; hostname: string; lastSeenAt: string; ... 16 more ...; targetUnavailableReason?: string | ... 1 more ... | undefined; }[] is expected — that caller reads the value, not a promise
  `(principal, machineService) => machinesForPrincipal( { machines: machine`
- L685 — passed where (id: string) => string is expected — that caller reads the value, not a promise
  `(id) => machines.machineName(id)`
- L691 — passed where () => readonly WaveMachine[] is expected — that caller reads the value, not a promise
  `() => machines.listMachines().map((machine) => ({ id: machine.id, `
- L734 — passed where ((machineId: string & BRAND<"MachineId">) => "stable" | "edge" | "dev" | undefined) | undefined is expected — that caller reads the value, not a promise
  `(machineId) => machines.updateChannel(machineId)`
- L761 — passed where (() => boolean) | undefined is expected — that caller reads the value, not a promise
  `() => operations?.engine.active(LIFECYCLE_EXCLUSION_GROUP) !== undefined`
- L766 — passed where ((channel: "stable" | "edge" | "dev") => string | undefined) | undefined is expected — that caller reads the value, not a promise
  `(channel) => exclusiveUpdateVersion(operations?.engine.active(LIFECYCLE_EXCLUSION_`
- L778 — passed where RecordGrant | undefined is expected — that caller reads the value, not a promise
  `(record) => { this.store.events.appendEvent( { ts: new Date(`
- L793 — passed where () => string & BRAND<"MachineId"> is expected — that caller reads the value, not a promise
  `() => machines.defaultMachine()`
- L825 — passed where () => (string & BRAND<"UserId">)[] is expected — that caller reads the value, not a promise
  `() => { const rows = this.store.users.list().map((row) => asUserId(row.id)) `
- L829 — passed where (userId: string & BRAND<"UserId">) => { issues: { assistantEnabled: boolean; }; steward: { enabled: boolean; }; notifications: { web: boolean; ntfyTopic: string; telegramChatId: string; telegramBotToken: string; }; ... 9 more ...; transcripts: { ...; }; } is expected — that caller reads the value, not a promise
  `(userId) => settings.getSettingsFor(userId)`
- L830 — passed where () => HandoffMachine[] is expected — that caller reads the value, not a promise
  `() => machines.listMachines()`
- L831 — passed where (userId: string & BRAND<"UserId">, values: Record<string, unknown>) => void is expected — that caller reads the value, not a promise
  `(userId, values) => { settings.updatePreferences(userId, values) }`
- L895 — passed where () => readonly { kind: string; id: string; repoPath: string | null; ts: string; subject: string; eventId: number; payload?: unknown; }[] is expected — that caller reads the value, not a promise
  `() => ledger.authority.snapshot('issueEvent') as IssueEventWire[]`
- L897 — passed where EventAppendListener is expected — that caller reads the value, not a promise
  `(id, event) => issueEventFeed?.publish(id, event)`
- L949 — passed where () => number | null is expected — that caller reads the value, not a promise
  `() => this.store.sync.minChangeSeq()`
- L983 — passed where () => string[] is expected — that caller reads the value, not a promise
  `() => this.store.repos.listRepoPaths()`
- L1034 — passed where () => string & BRAND<"MachineId"> is expected — that caller reads the value, not a promise
  `() => machines.defaultMachine()`
- L1035 — passed where (requested: string | undefined, cwd: string) => string is expected — that caller reads the value, not a promise
  `(requested, cwd) => machines.resolveMachine(requested, cwd)`
- L1037 — passed where (id: string & BRAND<"MachineId">) => string is expected — that caller reads the value, not a promise
  `(id) => machines.machineName(id)`
- L1080 — passed where () => { feedId: string; feedEpoch: string; } is expected — that caller reads the value, not a promise
  `() => { const identity = feedServing.identity() return { feedId: identity.`
- L1090 — passed where (machineId: string & BRAND<"MachineId">) => ServerTransferTargetState is expected — that caller reads the value, not a promise
  `(machineId) => targetStateResolver()(machineId)`
- L1161 — passed where (message: MessageRow) => CommandPrincipal | undefined is expected — that caller reads the value, not a promise
  `(message) => { if (message.fromKind === 'system') return systemPrincipal(message.f`
- L1232 — passed where ((messageId: string) => { ok: true; } | { ok: false; reason: string; }) | undefined is expected — that caller reads the value, not a promise
  `(messageId) => queuedMessageApply.authorize(messageId)`
- L1233 — passed where ((messageId: string, reason: string) => void) | undefined is expected — that caller reads the value, not a promise
  `(messageId, reason) => queuedMessageApply.reject(messageId, reason)`
- L1250 — passed where ((input: { machineId: string & BRAND<"MachineId">; agentKind: "claude-code" | "codex" | "grok" | "opencode" | "cursor" | "pi" | "shell"; ownerUserId: string & BRAND<"UserId">; }) => void) | undefined is expected — that caller reads the value, not a promise
  `({ machineId, agentKind, ownerUserId }) => loginPropagation.trigger({ ta`
- L1279 — passed where (ownerUserId: string & BRAND<"UserId">) => (machineId: string & BRAND<"MachineId">) => string | undefined is expected — that caller reads the value, not a promise
  `(ownerUserId) => { const user = this.store.users.get(ownerUserId) if (user`
- L1296 — passed where (machineId: string & BRAND<"MachineId">) => string is expected — that caller reads the value, not a promise
  `(machineId) => this.store.repos.listRepoPaths(machineId)[0] ?? '/'`
- L1298 — passed where Listener<"superagent.turnEnded"> is expected — that caller reads the value, not a promise
  `(event) => { if (event.ok || event.harnessErrorKind !== 'provider-auth' || !event.ha`
- L1328 — passed where () => { issues: { assistantEnabled: boolean; }; steward: { enabled: boolean; }; notifications: { web: boolean; ntfyTopic: string; telegramChatId: string; telegramBotToken: string; }; roles: { superagent: { ...; }; shipwright: { ...; }; coding: { ...; }; background: { ...; }; }; ... 8 more ...; transcripts: { ...; }; } is expected — that caller reads the value, not a promise
  `() => this.store.settings.getSettings()`
- L1330 — passed where (id: string) => string is expected — that caller reads the value, not a promise
  `(id) => machines.machineName(id)`
- L1331 — passed where () => Iterable<HostSessionView> is expected — that caller reads the value, not a promise
  `() => { // ONE statement per projection, not one per session (POD-568): the `
- L1368 — passed where (input: { sessionId: string & BRAND<"SessionId">; requireTerminalProof?: boolean | undefined; }) => { ok: boolean; reason?: string | undefined; } is expected — that caller reads the value, not a promise
  `(input) => sessionsSvc.hibernateSession(input)`
- L1371 — passed where (sessionId: string & BRAND<"SessionId">, now: number) => boolean is expected — that caller reads the value, not a promise
  `(sessionId, now) => { const lastSpawned = this.store.automations.lastSpawnedSess`
- L1379 — passed where (sessionId: string & BRAND<"SessionId">) => boolean is expected — that caller reads the value, not a promise
  `(sessionId) => sessionsSvc.hasValidTerminalProof(sessionId)`
- L1380 — passed where (sessionId: string & BRAND<"SessionId">) => boolean is expected — that caller reads the value, not a promise
  `(sessionId) => sessionsSvc.terminalProofMissing(sessionId)`
- L1402 — passed where (ownerUserId?: (string & BRAND<"UserId">) | undefined) => { issues: { assistantEnabled: boolean; }; steward: { enabled: boolean; }; notifications: { web: boolean; ntfyTopic: string; telegramChatId: string; telegramBotToken: string; }; ... 9 more ...; transcripts: { ...; }; } is expected — that caller reads the value, not a promise
  `(ownerUserId = FIRST_ADMIN_USER_ID) => this.store.settings.getSettingsFor(ownerU`
- L1405 — passed where () => string is expected — that caller reads the value, not a promise
  `() => this.store.secrets.getOrEmpty('notifications.telegramBotToken')`
- L1406 — passed where ((ownerUserId: string & BRAND<"UserId">) => boolean) | undefined is expected — that caller reads the value, not a promise
  `(ownerUserId) => this.store.telegramBindings.listForUser(ownerUserId).length ===`
- L1409 — passed where (e: { ts: string; kind: string; subject: string; repoPath?: string | null | undefined; payload?: unknown; }) => void is expected — that caller reads the value, not a promise
  `(e) => this.store.events.appendEvent(e)`
- L1433 — passed where Listener<"session.exited"> is expected — that caller reads the value, not a promise
  `({ sessionId }) => locks.releaseForSession(sessionId)`
- L1459 — passed where () => { status: "live" | "starting" | "reconnecting" | "hibernated" | "exited"; geometry: { rows: number; cols: number; }; agentKind: "claude-code" | "codex" | "grok" | "opencode" | "cursor" | "pi" | "shell"; ... 67 more ...; upstreamStale?: boolean | undefined; }[] is expected — that caller reads the value, not a promise
  `() => sessionsSvc.listSessions()`
- L1461 — passed where ((sessionId: string & BRAND<"SessionId">) => { status: "live" | "starting" | "reconnecting" | "hibernated" | "exited"; geometry: { rows: number; cols: number; }; agentKind: "claude-code" | ... 5 more ... | "shell"; ... 67 more ...; upstreamStale?: boolean | undefined; } | undefined) | undefined is expected — that caller reads the value, not a promise
  `(sessionId) => sessionsSvc.sessionById(sessionId)`
- L1464 — passed where ((worktreePath: string | null, issueId: string & BRAND<"IssueId">) => { status: "live" | "starting" | "reconnecting" | "hibernated" | "exited"; geometry: { rows: number; cols: number; }; ... 68 more ...; upstreamStale?: boolean | undefined; }[]) | undefined is expected — that caller reads the value, not a promise
  `(worktreePath, issueId) => sessionsSvc.listSessionsForIssue(worktreePath, issueId)`
- L1469 — passed where () => { issues: { assistantEnabled: boolean; }; steward: { enabled: boolean; }; notifications: { web: boolean; ntfyTopic: string; telegramChatId: string; telegramBotToken: string; }; roles: { superagent: { ...; }; shipwright: { ...; }; coding: { ...; }; background: { ...; }; }; ... 8 more ...; transcripts: { ...; }; } is expected — that caller reads the value, not a promise
  `() => this.store.settings.getSettingsFor(FIRST_ADMIN_USER_ID)`
- L1470 — passed where (o: { sessionId?: (string & BRAND<"SessionId">) | undefined; cwd: string; issueId?: (string & BRAND<"IssueId">) | undefined; agentKind?: string | undefined; model?: string | undefined; ... 6 more ...; ownerUserId?: (string & BRAND<...>) | undefined; }) => { ...; } is expected — that caller reads the value, not a promise
  `(o) => sessionsSvc.createSession({ ...(o.sessionId ? { sessionId: o.sess`
- L1484 — passed where ((requested: string | undefined, cwd: string) => string & BRAND<"MachineId">) | undefined is expected — that caller reads the value, not a promise
  `(requested, cwd) => machines.resolveMachine(requested, cwd)`
- L1485 — passed where ((machineId: string & BRAND<"MachineId">, repoPath: string) => void) | undefined is expected — that caller reads the value, not a promise
  `(machineId, repoPath) => machines.requireMachineForRepo(machineId, repoPath)`
- L1487 — passed where ((machineId: string & BRAND<"MachineId">) => void) | undefined is expected — that caller reads the value, not a promise
  `(machineId) => machines.requireRepoHostStructure(machineId)`
- L1509 — passed where ((repoPath: string, machineId: string & BRAND<"MachineId">) => string | null) | undefined is expected — that caller reads the value, not a promise
  `(repoPath, machineId) => sessionsSvc.workspace.findRepoOnMachine(repoPath, machine`
- L1541 — passed where Listener<"session.created"> is expected — that caller reads the value, not a promise
  `({ sessionId, issueId }) => { if (issueId) issues.ensureCoordinator(issueId, session`
- L1611 — passed where ((input: { issueId: string & BRAND<"IssueId">; }) => void) | undefined is expected — that caller reads the value, not a promise
  `(input) => issueSessionLifecycle.stopClosedIssue({ ...input, reason: 'close' })`
- L1614 — passed where Listener<"session.wakeRequested"> is expected — that caller reads the value, not a promise
  `({ sessionId, principal }) => { const authorized = sessionsSvc.authorizeQueuedInputA`
- L1676 — passed where <T>(fn: () => T) => T is expected — that caller reads the value, not a promise
  `(fn) => this.store.transact(fn)`
- L1679 — passed where (repoPath: string) => string & BRAND<"RepoId"> is expected — that caller reads the value, not a promise
  `(repoPath) => this.store.repos.resolveRepoIdForPath(repoPath)`
- L1691 — passed where (issueId: string & BRAND<"IssueId">, from: string, body: string) => void is expected — that caller reads the value, not a promise
  `(issueId, from, body) => { try { issues.sendMail(issueId, from, body) `
- L1696 — passed where (e: { ts: string; kind: string; subject: string; payload?: unknown; }) => void is expected — that caller reads the value, not a promise
  `(e) => this.store.events.appendEvent(e)`
- L1726 — passed where ((row: IssueMessageRow) => void) | undefined is expected — that caller reads the value, not a promise
  `(row) => funnel.run({ write: async () => await this.store.issues.addIssueMessage(row) })`
- L1727 — passed where ((issueId: string & BRAND<"IssueId">, ids: string[]) => void) | undefined is expected — that caller reads the value, not a promise
  `(issueId, ids) => funnel.run({ write: async () => await this`
- L1737 — passed where (<T>(fn: () => T) => T) | undefined is expected — that caller reads the value, not a promise
  `(fn) => this.store.transact(fn)`
- L1745 — passed where (input: { cwd: string; agentKind?: "claude-code" | "codex" | "grok" | "opencode" | "cursor" | "pi" | "shell" | undefined; model?: string | undefined; effort?: string | undefined; issueId?: (string & BRAND<...>) | undefined; spawnedBy?: string | undefined; machineId?: (string & BRAND<...>) | undefined; ownerUserId: s... is expected — that caller reads the value, not a promise
  `(o) => sessionsSvc.createSession(o)`
- L1749 — passed where ((id: string) => string) | undefined is expected — that caller reads the value, not a promise
  `(id) => machines.listMachines().find((m) => m.id === id)?.name ?? id`
- L1752 — passed where ((messageId: string, sessionId: string & BRAND<"SessionId">) => void) | undefined is expected — that caller reads the value, not a promise
  `(messageId, sessionId) => messagesSvc.onQueuedInputApplied(messageId, sessionId)`
- L1754 — passed where ((messageId: string, sessionId: string & BRAND<"SessionId">) => void) | undefined is expected — that caller reads the value, not a promise
  `(messageId, sessionId) => messagesSvc.onQueuedInputInjected(messageId, sessionId)`
- L1756 — passed where ((input: { sessionId: string & BRAND<"SessionId">; turnIds: readonly string[]; reason: "never-live" | "teardown" | "delivery-failed"; }) => void) | undefined is expected — that caller reads the value, not a promise
  `({ sessionId, turnIds, reason }) => messagesSvc.onQueueDrainAbandoned(sessionId, tur`
- L1760 — passed where Listener<"session.exited"> is expected — that caller reads the value, not a promise
  `({ sessionId }) => messagesSvc.onSessionExited(sessionId)`
- L1761 — passed where ((messageId: string) => void) | undefined is expected — that caller reads the value, not a promise
  `(messageId) => { try { messagesSvc.cancel(messageId) } catch { `
- L1768 — passed where ((sessionId: string & BRAND<"SessionId">, messageId?: string | undefined) => void) | undefined is expected — that caller reads the value, not a promise
  `(sessionId, messageId) => { try { messagesSvc.cancelPendingOperatorMessage(s`
- L1776 — passed where Listener<"message.deadLettered"> is expected — that caller reads the value, not a promise
  `({ messageId, reason }) => messagesSvc.notifyQueuedInputRejected(messageId, reason)`
- L1783 — passed where Listener<"oplog.appended"> is expected — that caller reads the value, not a promise
  `({ changes }) => { // ISSUE CHANGES GO IN ONE BATCH (POD-1597). Per change, the reco`
- L1823 — passed where (issueId: string & BRAND<"IssueId">) => IssueInfo | undefined is expected — that caller reads the value, not a promise
  `(issueId) => { const issue = issues?.getMeta(issueId) // Only `worktre`
- L1829 — passed where (path: string) => string | null is expected — that caller reads the value, not a promise
  `(path) => this.store.repos.resolveRepoIdForPath(path)`
- L1830 — passed where ((sessionId: string & BRAND<"SessionId">, text: string) => void) | undefined is expected — that caller reads the value, not a promise
  `(sessionId, text) => { messagesSvc.send( { kind: 'system', name: 'wo`
- L1854 — passed where (entity: WorkflowEntityRef) => string | null is expected — that caller reads the value, not a promise
  `(entity) => this.store.workflows.ownerOf(entity.kind, entity.id)`
- L1855 — passed where (user: string, entity: WorkflowEntityRef, verb: WorkflowVerb) => boolean is expected — that caller reads the value, not a promise
  `(user, entity, verb) => this.store.grants .listForResource(entit`
- L1860 — passed where ((principal: WorkflowPrincipal) => WorkflowMachineAccess) | undefined is expected — that caller reads the value, not a promise
  `(workflowPrincipal) => { let principal: CommandPrincipal | undefined i`
- L1888 — passed where (key: string) => string | undefined is expected — that caller reads the value, not a promise
  `(key) => this.store.sync.getAppliedMutation(asMutationId(key))`
- L1889 — passed where (key: string, result: string) => void is expected — that caller reads the value, not a promise
  `(key, result) => this.store.sync.recordAppliedMutation( asMutati`
- L1909 — passed where (context: SessionInstructionContext) => SessionInstructionContribution | null is expected — that caller reads the value, not a promise
  `({ sessionId, cwd, issueId, workflowRevisionId, existingOnly }) => { if (!featureE`
- L1927 — passed where (() => void) | undefined is expected — that caller reads the value, not a promise
  `() => { workflows.startRun({ sessionId, `
- L1945 — passed where () => { status: "live" | "starting" | "reconnecting" | "hibernated" | "exited"; geometry: { rows: number; cols: number; }; agentKind: "claude-code" | "codex" | "grok" | "opencode" | "cursor" | "pi" | "shell"; ... 67 more ...; upstreamStale?: boolean | undefined; }[] is expected — that caller reads the value, not a promise
  `() => sessionsSvc.listSessions()`
- L1946 — passed where ((sessionId: string & BRAND<"SessionId">) => { status: "live" | "starting" | "reconnecting" | "hibernated" | "exited"; geometry: { rows: number; cols: number; }; agentKind: "claude-code" | ... 5 more ... | "shell"; ... 67 more ...; upstreamStale?: boolean | undefined; } | undefined) | undefined is expected — that caller reads the value, not a promise
  `(sessionId) => sessionsSvc.sessionById(sessionId)`
- L1951 — passed where ((input: { ownerUserId: string & BRAND<"UserId">; cwd: string; agentKind?: string | undefined; initialPrompt?: string | undefined; model?: string | undefined; effort?: string | undefined; ... 8 more ...; executionProfileId?: string | undefined; }) => { ...; }) | undefined is expected — that caller reads the value, not a promise
  `(o) => sessionsSvc.createSession({ ownerUserId: o.ownerUserId, `
- L1968 — passed where ((input: { profileId: string; runId?: string | undefined; stepId?: string | undefined; caller?: MailCaller | undefined; }) => { id: string; accountId: string & BRAND<"AccountId">; machineId: (string & BRAND<...>) | null; harness: string; model: string; effort: string; }) | undefined is expected — that caller reads the value, not a promise
  `(input) => { const { caller, ...profileInput } = input return workflow`
- L1979 — passed where ((input: { ownerUserId: string & BRAND<"UserId">; createdByActor: string; createdByOnBehalfOf: string & BRAND<"UserId">; repoPath: string; title: string; description?: string | undefined; parentId?: (string & BRAND<...>) | undefined; origin: "human" | "agent"; }) => { ...; }) | undefined is expected — that caller reads the value, not a promise
  `(o) => issues.create({ ...o, startNow: false })`
- L1980 — passed where ((e: { ts: string; kind: string; subject: string; payload: unknown; }) => void) | undefined is expected — that caller reads the value, not a promise
  `(e) => this.store.events.appendEvent(e)`
- L1993 — passed where ((factKey: string, target: string) => void) | undefined is expected — that caller reads the value, not a promise
  `(factKey, target) => { this.store.notificationFacts.retire(factKey, target, new `
- L2000 — passed where () => { status: "live" | "starting" | "reconnecting" | "hibernated" | "exited"; geometry: { rows: number; cols: number; }; agentKind: "claude-code" | "codex" | "grok" | "opencode" | "cursor" | "pi" | "shell"; ... 67 more ...; upstreamStale?: boolean | undefined; }[] is expected — that caller reads the value, not a promise
  `() => sessionsSvc.listSessions()`
- L2016 — passed where <T>(work: () => T) => T is expected — that caller reads the value, not a promise
  `(work) => this.store.transact(work)`
- L2029 — passed where (input: { cwd: string; agentKind?: "claude-code" | "codex" | "grok" | "opencode" | "cursor" | "pi" | "shell" | undefined; model?: string | undefined; effort?: string | undefined; spawnedBy?: string | undefined; title?: string | undefined; issueId?: (string & BRAND<...>) | undefined; ownerUserId: string & BRAND<...>;... is expected — that caller reads the value, not a promise
  `(o) => sessionsSvc.createSession(o)`
- L2038 — passed where (input: { sessionId: string & BRAND<"SessionId">; text: string; mutationId?: (string & BRAND<"MutationId">) | undefined; inputOrigin?: "system" | undefined; }) => { ...; } is expected — that caller reads the value, not a promise
  `(o) => sessionsSvc.receiptSend('queue', o)`
- L2039 — passed where (input: { sessionId: string & BRAND<"SessionId">; text: string; mutationId?: (string & BRAND<"MutationId">) | undefined; }) => { ok: boolean; reason?: string | undefined; } is expected — that caller reads the value, not a promise
  `(o) => sessionsSvc.receiptSend('wake', o)`
- L2040 — passed where (input: { repoPath: string; title: string; description: string; defaultAgent: string; defaultModel: string; defaultEffort: string; type: "automation"; ownerUserId: string & BRAND<"UserId">; createdByActor: string; createdByOnBehalfOf: string & BRAND<...>; }) => { ...; } is expected — that caller reads the value, not a promise
  `(o) => { const issue = issues.create({ ...o, startNow: false, `
- L2050 — passed where () => Set<string & BRAND<"SessionId">> is expected — that caller reads the value, not a promise
  `() => new Set( sessionsSvc .listSessions() .filt`
- L2057 — passed where (ownerUserId: string & BRAND<"UserId">) => CommandPrincipal | undefined is expected — that caller reads the value, not a promise
  `(ownerUserId) => { const role = this.store.users.roleOf(ownerUserId) retur`
- L2061 — passed where (principal: CommandPrincipal) => boolean is expected — that caller reads the value, not a promise
  `(principal) => checkMachineUse(principal, machines.defaultMachine(), ownershipFrom`
- L2075 — passed where (sessionId: string & BRAND<"SessionId">) => (string & BRAND<"IssueId">) | null is expected — that caller reads the value, not a promise
  `(sessionId) => { const s = sessionsSvc.sessionById(sessionId) return s ? (`
- L2079 — passed where (issueId: string & BRAND<"IssueId">) => { seq: number; title: string; displayRef?: string | undefined; } | null is expected — that caller reads the value, not a promise
  `(issueId) => { const row = issues.getMeta(issueId) if (!row) return null `
- L2089 — passed where (machineId: string & BRAND<"MachineId">) => string | undefined is expected — that caller reads the value, not a promise
  `(machineId) => machines.listMachines().find((m) => m.id === machineId)?.name`
- L2090 — passed where (issueId: string & BRAND<"IssueId">, body: string) => void is expected — that caller reads the value, not a promise
  `(issueId, body) => void issues.sendMail(issueId, 'approval-broker', body)`
- L2091 — passed where ((op: { kind: "update"; } | { kind: "channel"; target: "stable" | "edge" | "dev"; } | { kind: "stop"; } | { kind: "set-server"; target: string; } | { kind: "workflow-publish"; revisionId: string; } | { ...; } | { ...; }, sessionId: string & BRAND<...>) => string | null) | undefined is expected — that caller reads the value, not a promise
  `(op, sessionId) => { const caller = workflowCallerForCapability(sessionsSvc.capabi`
- L2125 — passed where (sessionId: string & BRAND<"SessionId">) => (string & BRAND<"SessionId">) | undefined is expected — that caller reads the value, not a promise
  `(candidate) => spawnedByParentSessionId(sessionsSvc.sessionSpawnedBy(candida`
- L2164 — passed where (kind: string, issueId: (string & BRAND<"IssueId">) | null, payload: Record<string, unknown>) => void is expected — that caller reads the value, not a promise
  `(kind, issueId, payload) => { try { this.store.events.appendEvent({ `
- L2196 — passed where (id: string) => unknown is expected — that caller reads the value, not a promise
  `(id) => issueSessionLifecycle.deleteIssue(id)`
- L2197 — passed where (id: string) => unknown is expected — that caller reads the value, not a promise
  `(id) => issueSessionLifecycle.restoreIssue(id)`
- L2199 — passed where () => { status: "live" | "starting" | "reconnecting" | "hibernated" | "exited"; geometry: { rows: number; cols: number; }; agentKind: "claude-code" | "codex" | "grok" | "opencode" | "cursor" | "pi" | "shell"; ... 67 more ...; upstreamStale?: boolean | undefined; }[] is expected — that caller reads the value, not a promise
  `() => sessionsSvc.listSessions()`
- L2201 — passed where ((sessionId: string & BRAND<"SessionId">) => { status: "live" | "starting" | "reconnecting" | "hibernated" | "exited"; geometry: { rows: number; cols: number; }; agentKind: "claude-code" | ... 5 more ... | "shell"; ... 67 more ...; upstreamStale?: boolean | undefined; } | undefined) | undefined is expected — that caller reads the value, not a promise
  `(sessionId) => sessionsSvc.sessionById(sessionId)`
- L2202 — passed where () => string[] is expected — that caller reads the value, not a promise
  `() => this.store.repos.listRepoPaths()`
- L2203 — passed where (path: string) => string | undefined is expected — that caller reads the value, not a promise
  `(path) => inferRepoFromRoots(this.store.repos.listRepoPaths(), path)`
- L2205 — passed where ((from: MessageSender, input: MessageSendInput) => MessageSendResult) | undefined is expected — that caller reads the value, not a promise
  `(from, input) => messagesSvc.send(from, input)`
- L2212 — passed where (sessionId: string & BRAND<"SessionId">) => AnswerTargetSession | undefined is expected — that caller reads the value, not a promise
  `(id) => sessionsSvc.sessionById(id)`
- L2237 — passed where (machineId: string & BRAND<"MachineId">) => (string & BRAND<"UserId">)[] is expected — that caller reads the value, not a promise
  `(machineId) => { const owner = machines.ownershipRows().find((row) => row.id ===`
- L2247 — passed where (machineId: string & BRAND<"MachineId">) => string | undefined is expected — that caller reads the value, not a promise
  `(machineId) => this.store.repos.listRepoPaths(machineId)[0] ?? this.store.repos.`
- L2249 — passed where (id: string & BRAND<"IssueId">) => boolean is expected — that caller reads the value, not a promise
  `(id) => this.store.issues.getIssue(id) !== null`
- L2250 — passed where (input: CreateIssueInput) => void is expected — that caller reads the value, not a promise
  `(input) => void issues.create(input)`
- L2251 — passed where (issueId: string & BRAND<"IssueId">, body: string) => void is expected — that caller reads the value, not a promise
  `(issueId, body) => void issues.sendMail(issueId, 'machine-diagnostic', body)`
- L2278 — passed where () => string is expected — that caller reads the value, not a promise
  `() => this.store.settings.getSettings().gitWorkflow.defaultParentBranch || 'main'`
- L2283 — passed where (userId: string & BRAND<"UserId">) => { issues: { assistantEnabled: boolean; }; steward: { enabled: boolean; }; notifications: { web: boolean; ntfyTopic: string; telegramChatId: string; telegramBotToken: string; }; ... 9 more ...; transcripts: { ...; }; } is expected — that caller reads the value, not a promise
  `(userId) => settings.getSettingsFor(userId)`
- L2284 — passed where (machineId: string & BRAND<"MachineId">) => ModelCatalogSnapshot is expected — that caller reads the value, not a promise
  `(machineId) => settings.getModelCatalog(machineId)`
- L2286 — passed where (machineId: string & BRAND<"MachineId">, agent: "claude-code" | "codex" | "grok" | "opencode" | "cursor" | "pi", requested: string & BRAND<"AccountId">) => (string & BRAND<...>) | null is expected — that caller reads the value, not a promise
  `(machineId, agent, requested) => machines.nativeAccountIdForMachine(machineId, age`
- L2348 — passed where (id: string) => { type: "task" | "automation" | "bug" | "feature" | "chore" | "epic" | "decision" | "spike" | "story" | "milestone"; id: string & BRAND<"IssueId">; title: string; ... 72 more ...; comments?: { ...; }[] | undefined; } is expected — that caller reads the value, not a promise
  `(id) => { const issue = issues.get(id) if (!issue) throw new Error(`un`
- L2353 — passed where (id: string, recursive?: boolean | undefined) => { type: "task" | "automation" | "bug" | "feature" | "chore" | "epic" | "decision" | "spike" | "story" | "milestone"; id: string & BRAND<...>; ... 73 more ...; comments?: { ...; }[] | undefined; }[] is expected — that caller reads the value, not a promise
  `(id, recursive) => issues.children(id, recursive)`
- L2354 — passed where <T>(id: string & BRAND<"IssueId">, mutation: ShippingIssueMutation, write: () => T) => { issue: { type: "task" | "automation" | "bug" | "feature" | ... 5 more ... | "milestone"; ... 74 more ...; comments?: { ...; }[] | undefined; }; result: T; } is expected — that caller reads the value, not a promise
  `(id, mutation, write) => issues.shippingCommit(id, mutation, write)`
- L2391 — passed where (input: { principal: CommandPrincipal; action: "cancel" | "enqueue" | "resolve-hold" | "read-receipt"; issue: { type: "task" | "automation" | "bug" | "feature" | "chore" | ... 4 more ... | "milestone"; ... 74 more ...; comments?: { ...; }[] | undefined; }; overrideScope: boolean; }) => void is expected — that caller reads the value, not a promise
  `({ principal, action, issue, overrideScope }) => { if (principal.kind === 'syste`
- L2421 — passed where (input: { order: { state: "queued" | "cancelled" | "preflight" | "composing" | "validating" | "repairing" | "landing" | "publishing" | "verifying" | "shipped" | "held"; id: string & BRAND<...>; ... 20 more ...; holdCode?: string | undefined; }; issue: { ...; }; machineId: string & BRAND<...>; effect: "preflight" | .... is expected — that caller reads the value, not a promise
  `({ order, issue, machineId, effect }) => { const attribution = order.requestedBy`
- L2473 — passed where (rootIssueId: string & BRAND<"IssueId">, approvedHeadSha: string) => { rootIssueId: string & BRAND<"IssueId">; approvedHeadSha: string; descendants: { issueId: string & BRAND<...>; approvedHeadSha: string; }[]; } | null is expected — that caller reads the value, not a promise
  `(rootIssueId, approvedHeadSha) => this.store.shipping.rootIntegrationReceipt(roo`
- L2483 — passed where (input: { order: { state: "queued" | "cancelled" | "preflight" | "composing" | "validating" | "repairing" | "landing" | "publishing" | "verifying" | "shipped" | "held"; id: string & BRAND<...>; ... 20 more ...; holdCode?: string | undefined; }; attempt: { ...; }; issue: { ...; }; names: readonly string[]; ttlSeconds... is expected — that caller reads the value, not a promise
  `({ order, attempt, issue, names, ttlSeconds }) => { const key = shippingLockKey(`
- L2524 — passed where (input: { order: { state: "queued" | "cancelled" | "preflight" | "composing" | "validating" | "repairing" | "landing" | "publishing" | "verifying" | "shipped" | "held"; id: string & BRAND<...>; ... 20 more ...; holdCode?: string | undefined; }; attempt: { ...; }; issue: { ...; }; names: readonly string[]; ttlSeconds... is expected — that caller reads the value, not a promise
  `({ order, attempt, issue, names, ttlSeconds }) => { const key = shippingLockKey(`
- L2546 — passed where (input: { order: { state: "queued" | "cancelled" | "preflight" | "composing" | "validating" | "repairing" | "landing" | "publishing" | "verifying" | "shipped" | "held"; id: string & BRAND<...>; ... 20 more ...; holdCode?: string | undefined; }; attempt: { ...; }; issue: { ...; }; names: readonly string[]; }) => void is expected — that caller reads the value, not a promise
  `({ order, attempt, issue, names }) => { const key = shippingLockKey(order.id, at`
- L2561 — passed where (issue: { type: "task" | "automation" | "bug" | "feature" | "chore" | "epic" | "decision" | "spike" | "story" | "milestone"; id: string & BRAND<"IssueId">; title: string; createdAt: string; ... 71 more ...; comments?: { ...; }[] | undefined; }) => string & BRAND<...> is expected — that caller reads the value, not a promise
  `(issue) => issue.machineId ?? machines.pickMachineForRepo(undefined, issue.repoPat`
- L2563 — passed where ((machineId: string & BRAND<"MachineId">) => readonly string[]) | undefined is expected — that caller reads the value, not a promise
  `(machineId) => machines.listMachines().find((machine) => machine.id === machineId)`
- L2607 — passed where ((kind: string, issueId: string & BRAND<"IssueId">, payload: Record<string, unknown>) => void) | undefined is expected — that caller reads the value, not a promise
  `(kind, issueId, payload) => { try { this.store.events.appendEvent({ `
- L2622 — passed where Listener<"machine.connected"> is expected — that caller reads the value, not a promise
  `() => { void shipping .reconcile() .catch((error) => log.warn('shipp`
- L2721 — passed where () => boolean is expected — that caller reads the value, not a promise
  `() => operationsModule.engine.active(LIFECYCLE_EXCLUSION_GROUP) !== undefined`
- L2746 — passed where () => readonly { readonly id: string; }[] is expected — that caller reads the value, not a promise
  `() => ledger.authority.snapshot('pendingInteraction') as readonly { readonly id: s`
- L2753 — passed where (row: InteractionRow) => void is expected — that caller reads the value, not a promise
  `(row) => interactionFeed.publish(row)`
- L2760 — passed where ((sessionId: string & BRAND<"SessionId">) => "terminal" | "server" | "embedded" | undefined) | undefined is expected — that caller reads the value, not a promise
  `(sessionId) => sessionsSvc.sessionById(sessionId)?.driverFamily`
- L2778 — passed where ((sessionId: string & BRAND<"SessionId">) => boolean) | undefined is expected — that caller reads the value, not a promise
  `(sessionId) => { const checkpoint = this.store.events.runtimeEventCheckpoint(sessi`
- L2786 — passed where (sessionId: string & BRAND<"SessionId">) => AnswerTargetSession | undefined is expected — that caller reads the value, not a promise
  `(id) => sessionsSvc.sessionById(id)`
- L2811 — passed where (sessionId: string & BRAND<"SessionId">) => { phase: "unknown" | "working" | "idle" | "needs_user" | "errored" | "compacting" | "ended"; since: string; nativeSubagentCount: number; ... 9 more ...; stateObservedAt?: string | undefined; } | null | undefined is expected — that caller reads the value, not a promise
  `(id) => sessionsSvc.sessionById(id)?.agentState`
- L2849 — passed where ((msg: { sessionId: string & BRAND<"SessionId">; interaction: { kind: "permission"; id: string; sessionId: string & BRAND<"SessionId">; source: "hook" | "protocol" | "sdk-callback" | "screen-classifier"; ... 4 more ...; policyVerdict?: "auto-allowed" | ... 2 more ... | undefined; } | ... 4 more ... | { ...; }; }) =>... is expected — that caller reads the value, not a promise
  `(msg) => { // LOGGED, NOT SWALLOWED (POD-2023 review, 7.2). This frame is classified`
- L2897 — passed where Listener<"session.stateChanged"> is expected — that caller reads the value, not a promise
  `(e) => { void interactions.onStateChanged({ sessionId: e.sessionId, prev: e.prev, ne`
- L2900 — passed where Listener<"session.exited"> is expected — that caller reads the value, not a promise
  `(e) => interactions.onSessionExited(e.sessionId)`
- L2960 — passed where () => FleetRepoRow[] is expected — that caller reads the value, not a promise
  `() => this.store.repos.listRepos()`
- L3011 — passed where () => { status: "live" | "starting" | "reconnecting" | "hibernated" | "exited"; geometry: { rows: number; cols: number; }; agentKind: "claude-code" | "codex" | "grok" | "opencode" | "cursor" | "pi" | "shell"; ... 67 more ...; upstreamStale?: boolean | undefined; }[] is expected — that caller reads the value, not a promise
  `() => sessionsSvc.listSessions()`
- L3013 — passed where ((sessionId: string & BRAND<"SessionId">) => { status: "live" | "starting" | "reconnecting" | "hibernated" | "exited"; geometry: { rows: number; cols: number; }; agentKind: "claude-code" | ... 5 more ... | "shell"; ... 67 more ...; upstreamStale?: boolean | undefined; } | undefined) | undefined is expected — that caller reads the value, not a promise
  `(sessionId) => sessionsSvc.sessionById(sessionId)`
- L3029 — passed where (sessionId: string & BRAND<"SessionId">, text: string, mutationId?: (string & BRAND<"MutationId">) | undefined) => void is expected — that caller reads the value, not a promise
  `(sessionId, text, mutationId) => { const result = sessionsSvc.receiptSend( `
- L3038 — passed where ((receipt: { at: string; outcome: "accepted"; turnEpoch: number; deliveredAs: "interrupt" | "when-ready" | "queue" | "steer"; provenBy: "hook" | "sdk-callback" | "protocol-ack" | "transcript-echo"; } | { ...; } | { ...; } | { ...; }) => void) | undefined is expected — that caller reads the value, not a promise
  `(receipt) => { // LEDGER-VISIBLE, NEVER A RESEND — the uniform `unverified` po`
- L3061 — passed where () => { issues: { assistantEnabled: boolean; }; steward: { enabled: boolean; }; notifications: { web: boolean; ntfyTopic: string; telegramChatId: string; telegramBotToken: string; }; roles: { superagent: { ...; }; shipwright: { ...; }; coding: { ...; }; background: { ...; }; }; ... 8 more ...; transcripts: { ...; }; } is expected — that caller reads the value, not a promise
  `() => this.store.settings.getSettings()`
- L3095 — passed where Listener<"session.stateChanged"> is expected — that caller reads the value, not a promise
  `({ sessionId, prev, next }) => { if (next.phase !== 'idle' || prev?.phase === 'idle'`
- L3108 — passed where Listener<"transcript.delta"> is expected — that caller reads the value, not a promise
  `({ sessionId, items, reset }) => { // A reset can replay an old interrupt marker whi`
- L3114 — passed where () => void is expected — that caller reads the value, not a promise
  `() => messagesSvc.sweep()`
- L3129 — passed where () => void is expected — that caller reads the value, not a promise
  `() => approvals.sweepStalledExecutions()`
- L3174 — passed where (conn: ClientConn) => void is expected — that caller reads the value, not a promise
  `(client) => { client.send({ type: 'approvalsChanged', pending:`
- L3193 — passed where (machineId: string & BRAND<"MachineId">, msg: { type: "agentRelayRequest"; sessionId: string & BRAND<"SessionId">; proc: string; requestId: string; router: string; input?: unknown; outsideScope?: boolean | undefined; }) => void is expected — that caller reads the value, not a promise
  `(machineId, msg) => void agentRelayGate.run(machineId, msg)`

### apps/server/src/issues.test.ts (27)
- L182 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => svc.resolveRef('#1')`
- L633 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => svc.setIssueTucked(open.id, true)`
- L1335 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => svc.addSession(w.id)`
- L1793 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => svc.addSession(created.id)`
- L1841 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => svc.addSession(created.id)`
- L1846 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => svc.addSession(created.id)`
- L2831 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => svc.addDep(a.id, a.id)`
- L2832 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => svc.addDep(b.id, a.id)`
- L2969 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => svc.update(child.id, { color: 'teal' })`
- L3016 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => svc.update(a.id, { parentId: b.id })`
- L3025 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => svc.update(x.id, { parentId: nw.id })`
- L3042 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => svc.addDep(root.id, child.id, 'blocks')`
- L3055 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => svc.addDep(c.id, a.id)`
- L3064 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => svc.addDep(a.id, b.id, 'parent-child')`
- L3071 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => svc.removeDep(c.id, e.id, 'parent-child')`
- L3213 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => svc.tree('iss_nope')`
- L3316 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => svc.supersede(a.id, 'iss_missing')`
- L3564 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => svc.purgeEmptyDraft('iss_missing')`
- L3644 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => svc.resolveRef('1')`
- L3680 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => svc.resolveRef('podium#1')`
- L3840 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => h.svc.update(issue.id, { worktreePath: '/r' })`
- L4282 — argument of .map() — the caller consumes the value synchronously
  `(k, i) => { const c = h.svc.create({ repoPath: '/r', title: `K${i}`, parentId: epic.`
- L4371 — passed where EventAppendListener is expected — that caller reads the value, not a promise
  `(_id, event) => { if (event.kind !== 'issue.integration') return visibleAtEven`
- L4862 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => svc.children('iss_nope')`
- L4915 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => svc.panelApply(w.id, { op: 'todo-done', index: 9 })`
- L5169 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => svc.update(w.id, { stage: 'review' })`
- L5412 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => svc.mailClaim('msg_nope', 'x')`

### apps/server/src/modules/shipping/service.ts (26)
- L397 — passed where () => void is expected — that caller reads the value, not a promise
  `() => { void this.tick().catch((err) => log.warn('scheduler tick failed', { err })) `
- L674 — argument of .map() — the caller consumes the value synchronously
  `(member) => this.requiredOrder(member.orderId)`
- L689 — passed where readonly EntityChangeSpec[] | ((result: unknown) => readonly EntityChangeSpec[]) is expected — that caller reads the value, not a promise
  `() => this.projectionSpecs()`
- L714 — passed where () => { kind: string; payload: { orderId: any; holdId: any; generation: any; reasonCode: any; actions: any; readonly reason?: undefined; }; } | { kind: string; payload: { orderId: any; reason: string; holdId?: undefined; generation?: undefined; reasonCode?: undefined; actions?: undefined; }; } is expected — that caller reads the value, not a promise
  `() => { const hold = this.deps.repository.openHoldForOrder(member.id) `
- L881 — argument of .map() — the caller consumes the value synchronously
  `(member) => this.requiredOrder(member.orderId)`
- L1156 — argument of .flatMap() — the caller consumes the value synchronously
  `(covered) => { const live = this.deps.repository.getOrder(covered.id) `
- L1218 — passed where readonly EntityChangeSpec[] | ((result: unknown) => readonly EntityChangeSpec[]) is expected — that caller reads the value, not a promise
  `() => this.projectionSpecs()`
- L1389 — argument of .map() — the caller consumes the value synchronously
  `(member) => this.requiredOrder(member.orderId)`
- L1410 — passed where () => { kind: string; payload: { orderId: any; holdId: any; generation: any; reasonCode: any; actions: any; cancelledPeer?: undefined; }; } | { kind: string; payload: { orderId: any; cancelledPeer: string & BRAND<...>; holdId?: undefined; generation?: undefined; reasonCode?: undefined; actions?: undefined; }; } is expected — that caller reads the value, not a promise
  `() => { const sibling = this.requiredOrder(member.id) `
- L1567 — argument of .map() — the caller consumes the value synchronously
  `(member) => this.requiredOrder(member.orderId)`
- L1585 — passed where () => { kind: string; payload: { orderId: any; holdId: any; generation: any; reasonCode: any; actions: any; cancelledPeer?: undefined; }; } | { kind: string; payload: { orderId: any; cancelledPeer: string & BRAND<...>; holdId?: undefined; generation?: undefined; reasonCode?: undefined; actions?: undefined; }; } is expected — that caller reads the value, not a promise
  `() => { const hold = this.deps.repository.openHoldForOrder(member.id) `
- L1815 — passed where (result: { manifest: { version: 1; id: string & BRAND<"ShipTrainId">; subsetId: string & BRAND<"ShipTrainSubsetId">; repairRound: 0; memberCount: number; leaderOrderId: string & BRAND<...>; members: { ...; }[]; lane: { ...; }; }; claimed: { ...; }[]; }) => EntityChangeSpec[] is expected — that caller reads the value, not a promise
  `() => this.projectionSpecs()`
- L1983 — passed where () => void is expected — that caller reads the value, not a promise
  `() => { for (const { receipt } of settlements) { this.deps.repository.co`
- L2023 — argument of .map() — the caller consumes the value synchronously
  `(member) => this.requiredOrder(member.orderId)`
- L2145 — passed where () => void is expected — that caller reads the value, not a promise
  `() => { for (const { hold } of pending) this.deps.repository.raiseHold(hold) `
- L2244 — passed where (result: void) => EntityChangeSpec[] is expected — that caller reads the value, not a promise
  `() => this.projectionSpecs()`
- L2253 — passed where () => { kind: string; payload: { orderId: string & BRAND<"ShipOrderId">; holdId: any; generation: any; reasonCode: any; actions: any; readonly reason?: undefined; }; } | { kind: string; payload: { ...; }; } is expected — that caller reads the value, not a promise
  `() => { const hold = this.deps.repository.openHoldForOrder(member.id) `
- L2349 — passed where (result: { order: { state: "queued" | "cancelled" | "preflight" | "composing" | "validating" | "repairing" | "landing" | "publishing" | "verifying" | "shipped" | "held"; id: string & BRAND<...>; ... 20 more ...; holdCode?: string | undefined; }; attempt: { ...; }; }) => EntityChangeSpec[] is expected — that caller reads the value, not a promise
  `() => this.projectionSpecs()`
- L2384 — passed where (result: { state: "queued" | "cancelled" | "preflight" | "composing" | "validating" | "repairing" | "landing" | "publishing" | "verifying" | "shipped" | "held"; id: string & BRAND<"ShipOrderId">; ... 20 more ...; holdCode?: string | undefined; }) => EntityChangeSpec[] is expected — that caller reads the value, not a promise
  `() => this.projectionSpecs()`
- L2516 — passed where (result: { state: "queued" | "cancelled" | "preflight" | "composing" | "validating" | "repairing" | "landing" | "publishing" | "verifying" | "shipped" | "held"; id: string & BRAND<"ShipOrderId">; ... 20 more ...; holdCode?: string | undefined; }) => EntityChangeSpec[] is expected — that caller reads the value, not a promise
  `() => this.projectionSpecs()`
- L2585 — argument of .map() — the caller consumes the value synchronously
  `(member) => this.requiredOrder(member.orderId)`
- L2714 — passed where () => { kind: string; payload: { orderId: any; holdId: any; generation: any; reasonCode: any; actions: any; isolatedFrom?: undefined; }; } | { kind: string; payload: { orderId: any; isolatedFrom: string & BRAND<...>; holdId?: undefined; generation?: undefined; reasonCode?: undefined; actions?: undefined; }; } is expected — that caller reads the value, not a promise
  `() => { const hold = this.deps.repository.openHoldForOrder(member.id) `
- L2904 — passed where (result: { state: "queued" | "cancelled" | "preflight" | "composing" | "validating" | "repairing" | "landing" | "publishing" | "verifying" | "shipped" | "held"; id: string & BRAND<"ShipOrderId">; ... 20 more ...; holdCode?: string | undefined; }) => EntityChangeSpec[] is expected — that caller reads the value, not a promise
  `() => this.projectionSpecs()`
- L3054 — argument of .map() — the caller consumes the value synchronously
  `(member) => this.requiredOrder(member.orderId)`
- L3063 — passed where () => { kind: string; payload: { orderId: any; holdId: any; generation: any; reasonCode: any; actions: any; heldPeer?: undefined; }; } | { kind: string; payload: { orderId: any; heldPeer: string & BRAND<"ShipOrderId">; holdId?: undefined; generation?: undefined; reasonCode?: undefined; actions?: undefined; }; } is expected — that caller reads the value, not a promise
  `() => { const liveHold = this.deps.repository.openHoldForOrder(member.id) `
- L3234 — argument of .map() — the caller consumes the value synchronously
  `(child) => this.deps.resolveBranchTip(child)`

### apps/server/src/store-issues.test.ts (26)
- L174 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => s.issues.upsertIssue({ ...base(), id: asIssueId('t4'), `
- L254 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => s.issues.upsertIssue({ ...base(), stage: 'bogus' })`
- L257 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => s.issues.upsertIssue({ ...base(), defaultAgent: 'auto' })`
- L278 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => s.issues.listIssueRows()`
- L419 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => s.shipping.createOrder( shipOrder({ id: asShipOrderId('order-not-queue`
- L424 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => s.shipping.createOrder(shipOrder({ id: asShipOrderId('order-other') }))`
- L501 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => s.shipping.appendStep({ ...plannedStep, id: asShipStepId('step`
- L511 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => s.shipping.appendStep({ ...plannedStep, id: asShipStepId('step`
- L520 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => s.shipping.raiseHold({ id: asShipHoldId('hold-stale'), orderId`
- L564 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => s.shipping.raiseHold({ id: asShipHoldId('hold-second-open'), o`
- L577 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => s.shipping.resolveHold(order.id, 2, 'retry', 'queued', '2026-08-12T10:05:00.00`
- L580 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => s.shipping.resolveHold(order.id, 1, 'retry', 'repairing', '2026-08-12T10:05:00`
- L587 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => s.shipping.finishAttempt(attempt.id, 2, { finishedAt: '2026-08-12T10:0`
- L593 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => s.shipping.transitionOrder(order.id, 'queued', 'verifying', '2026-08-12T10:06:`
- L623 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => s.shipping.completeVerifiedOrder(receipt)`
- L646 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => s.shipping.completeVerifiedOrder({ ...receipt, destinationSha:`
- L652 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => s.shipping.transitionOrder(order.id, 'shipped', 'queued', 'later')`
- L679 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => s.shipping.createOrder(shipOrder({ id: asShipOrderId('order-2') }))`
- L939 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => s.issues.transitionShippingStage(issue.id, 'review', 'shipping', 't2')`
- L971 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => s.shipping.recordRootIntegrationReceipt({ ...receipt, descenda`
- L999 — argument of .forEach() — the caller consumes the value synchronously
  `(id, index) => s.issues.upsertIssue({ ...base(), id, seq: 70`
- L1025 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => s.shipping.claimTrain({ leaderOrderId: b.id, startedAt: '2026-`
- L1032 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => s.shipping.claimTrain({ leaderOrderId: c.id, startedAt: '2026-`
- L1075 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => cyclic.shipping.claimTrain({ leaderOrderId: upperId, startedAt`
- L1147 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => s.shipping.createOrder({ ...evidenced, currentIntegrationRecei`
- L1156 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => s.shipping.createOrder( shipOrder({ id: asShipOrderId('order`

### apps/server/src/store/json-column-corruption-oracle.test.ts (26)
- L592 — passed where ((f: Fixture) => unknown) | undefined is expected — that caller reads the value, not a promise
  `(f) => f.store.issues.getIssue('iss_1')?.blockedBy`
- L603 — passed where ((f: Fixture) => unknown) | undefined is expected — that caller reads the value, not a promise
  `(f) => f.store.issues.getIssue('iss_1')?.humanQuestionOptions`
- L614 — passed where ((f: Fixture) => unknown) | undefined is expected — that caller reads the value, not a promise
  `(f) => f.store.events.listEventsSince(0)[0]?.payload`
- L625 — passed where ((f: Fixture) => unknown) | undefined is expected — that caller reads the value, not a promise
  `(f) => f.store.shipping.rootIntegrationReceipt(asIssueId('iss_1'), 'approved-head')?`
- L637 — passed where ((f: Fixture) => unknown) | undefined is expected — that caller reads the value, not a promise
  `(f) => f.store.observationCheckpoints.get(SESSION)?.checkpoint`
- L648 — passed where ((f: Fixture) => unknown) | undefined is expected — that caller reads the value, not a promise
  `(f) => f.store.observationCheckpoints.getTerminalCandidate(SESSION)`
- L659 — passed where ((f: Fixture) => unknown) | undefined is expected — that caller reads the value, not a promise
  `(f) => f.store.settingsAudit.list()[0]?.detail`
- L670 — passed where ((f: Fixture) => unknown) | undefined is expected — that caller reads the value, not a promise
  `(f) => f.store.settingsAudit.list()[0]?.redactedPaths`
- L681 — passed where ((f: Fixture) => unknown) | undefined is expected — that caller reads the value, not a promise
  `(f) => f.store.shipping.openHoldForOrder(f.orderId)?.actions`
- L692 — passed where ((f: Fixture) => unknown) | undefined is expected — that caller reads the value, not a promise
  `(f) => f.store.shipping.openHoldForOrder(f.orderId)?.evidenceRefs`
- L704 — passed where ((f: Fixture) => unknown) | undefined is expected — that caller reads the value, not a promise
  `(f) => f.store.shipping.getOrder(f.stackedOrderId)`
- L715 — passed where ((f: Fixture) => unknown) | undefined is expected — that caller reads the value, not a promise
  `(f) => f.store.shipping.getOrder(f.orderId)?.deliveryDependsOn`
- L727 — passed where ((f: Fixture) => unknown) | undefined is expected — that caller reads the value, not a promise
  `(f) => f.store.shipping.getOrder(f.orderId)?.descendantManifest`
- L739 — passed where ((f: Fixture) => unknown) | undefined is expected — that caller reads the value, not a promise
  `(f) => f.store.shipping.getOrder(f.stackedOrderId)`
- L750 — passed where ((f: Fixture) => unknown) | undefined is expected — that caller reads the value, not a promise
  `(f) => f.store.shipping.getOrder(f.stackedOrderId)?.providerRef`
- L761 — passed where ((f: Fixture) => unknown) | undefined is expected — that caller reads the value, not a promise
  `(f) => f.store.shipping.getOrder(f.orderId)`
- L772 — passed where ((f: Fixture) => unknown) | undefined is expected — that caller reads the value, not a promise
  `(f) => f.store.shipping.stepById(f.stepId)`
- L783 — passed where ((f: Fixture) => unknown) | undefined is expected — that caller reads the value, not a promise
  `(f) => f.store.shipping.trainManifestForAttempt(f.attemptId)`
- L794 — passed where ((f: Fixture) => unknown) | undefined is expected — that caller reads the value, not a promise
  `(f) => f.store.shipping.trainManifestForAttempt(f.attemptId)`
- L805 — passed where ((f: Fixture) => unknown) | undefined is expected — that caller reads the value, not a promise
  `(f) => f.store.shipping.trainManifestForAttempt(f.attemptId)`
- L826 — passed where ((f: Fixture) => unknown) | undefined is expected — that caller reads the value, not a promise
  `(f) => f.store.workflows.getRevision(f.revisionId)?.steps`
- L837 — passed where ((f: Fixture) => unknown) | undefined is expected — that caller reads the value, not a promise
  `(f) => f.store.workflows.getRunSteps(f.runId)[0]?.evidence`
- L851 — passed where ((f: Fixture) => unknown) | undefined is expected — that caller reads the value, not a promise
  `(f) => f.store.workflows.getRunSteps(f.runId)[0]?.warnings`
- L871 — passed where ((f: Fixture) => unknown) | undefined is expected — that caller reads the value, not a promise
  `(f) => f.store.superagent.loadSuperagentMessages()[0]?.toolCalls`
- L882 — passed where ((f: Fixture) => unknown) | undefined is expected — that caller reads the value, not a promise
  `(f) => f.store.superagent.listQueuedInputs()[0]?.focus`
- L893 — passed where ((f: Fixture) => unknown) | undefined is expected — that caller reads the value, not a promise
  `(f) => f.store.superagent.listPendingTurns()[0]?.payload`

### apps/server/src/modules/issues/registry.ts (19)
- L269 — passed where (ctx: IssueCommandCtx, input: { repoPath?: string | undefined; }) => { id: string; }[] is expected — that caller reads the value, not a promise
  `(ctx, input) => ctx.visibleRows(ctx.reports.list(input.repoPath))`
- L286 — passed where ((id: string) => boolean) | undefined is expected — that caller reads the value, not a promise
  `(id) => ctx.mayReadIssue(id)`
- L291 — passed where (ctx: IssueCommandCtx, input: { repoPath?: string | undefined; }) => { id: string; }[] is expected — that caller reads the value, not a promise
  `(ctx, input) => ctx.visibleRows(ctx.reports.readyList(input.repoPath))`
- L295 — passed where (ctx: IssueCommandCtx, input: { repoPath?: string | undefined; }) => { id: string; }[] is expected — that caller reads the value, not a promise
  `(ctx, input) => ctx.visibleRows(ctx.reports.blockedList(input.repoPath))`
- L304 — passed where ((id: string) => boolean) | undefined is expected — that caller reads the value, not a promise
  `(id) => ctx.mayReadIssue(id)`
- L310 — passed where ((id: string) => boolean) | undefined is expected — that caller reads the value, not a promise
  `(id) => ctx.mayReadIssue(id)`
- L322 — passed where ((id: string) => boolean) | undefined is expected — that caller reads the value, not a promise
  `(id) => ctx.mayReadIssue(id)`
- L327 — passed where ((id: string) => boolean) | undefined is expected — that caller reads the value, not a promise
  `(id) => ctx.mayReadIssue(id)`
- L332 — passed where ((id: string) => boolean) | undefined is expected — that caller reads the value, not a promise
  `(id) => ctx.mayReadIssue(id)`
- L337 — passed where ((id: string) => boolean) | undefined is expected — that caller reads the value, not a promise
  `(id) => ctx.mayReadIssue(id)`
- L342 — passed where ((id: string) => boolean) | undefined is expected — that caller reads the value, not a promise
  `(id) => ctx.mayReadIssue(id)`
- L346 — passed where ((id: string) => boolean) | undefined is expected — that caller reads the value, not a promise
  `(id) => ctx.mayReadIssue(id)`
- L350 — passed where ((id: string) => boolean) | undefined is expected — that caller reads the value, not a promise
  `(id) => ctx.mayReadIssue(id)`
- L354 — passed where ((id: string) => boolean) | undefined is expected — that caller reads the value, not a promise
  `(id) => ctx.mayReadIssue(id)`
- L370 — passed where ((id: string) => boolean) | undefined is expected — that caller reads the value, not a promise
  `(id) => ctx.mayReadIssue(id)`
- L374 — passed where ((id: string) => boolean) | undefined is expected — that caller reads the value, not a promise
  `(id) => ctx.mayReadIssue(id)`
- L378 — passed where ((id: string) => boolean) | undefined is expected — that caller reads the value, not a promise
  `(id) => ctx.mayReadIssue(id)`
- L382 — passed where ((id: string) => boolean) | undefined is expected — that caller reads the value, not a promise
  `(id) => ctx.mayReadIssue(id)`
- L1298 — passed where (ctx: IssueCommandCtx, input: { id: string; enabled: boolean; }) => { updated: boolean; } is expected — that caller reads the value, not a promise
  `(ctx, input) => { // Constrained callers may only toggle their OWN subscriptions. `

### apps/server/src/server.ts (18)
- L716 — passed where () => number is expected — that caller reads the value, not a promise
  `() => registry.modules.machines.listMachines().length`
- L722 — passed where () => RepoRow[] is expected — that caller reads the value, not a promise
  `() => store.repos.listRepos()`
- L734 — passed where (machineId: string & BRAND<"MachineId">) => string is expected — that caller reads the value, not a promise
  `(id) => registry.modules.machines.machineName(id)`
- L752 — passed where (userId: string & BRAND<"UserId">) => string | undefined is expected — that caller reads the value, not a promise
  `(userId) => { const routes = store.telegramBindings.listForUser(userId) re`
- L758 — passed where () => string is expected — that caller reads the value, not a promise
  `() => store.secrets.getOrEmpty('notifications.telegramBotToken')`
- L767 — passed where (threadId: string & BRAND<"ThreadId">) => { ownerUserId: string & BRAND<"UserId">; podiumSessionId?: (string & BRAND<"SessionId">) | null | undefined; originSessionId?: (string & BRAND<...>) | ... 1 more ... | undefined; } | undefined is expected — that caller reads the value, not a promise
  `(threadId) => store.superagent.getSuperagentThread(threadId)`
- L827 — passed where () => boolean is expected — that caller reads the value, not a promise
  `() => store.machines .listMachines() .some((machine) => isRemoteUpda`
- L834 — passed where (() => readonly string[]) | undefined is expected — that caller reads the value, not a promise
  `() => fleetHeadlessPlatforms(store.machines.listMachines())`
- L1079 — passed where (token: string) => boolean is expected — that caller reads the value, not a promise
  `(token) => store.machines.getMachineByToken(hostMachineId, token)`
- L1086 — passed where (() => { mode: "auto" | "off" | "propose"; afterDays: number; }) | undefined is expected — that caller reads the value, not a promise
  `() => store.settings.getSettings().worktreeGc`
- L1087 — passed where (() => Set<string>) | undefined is expected — that caller reads the value, not a promise
  `() => new Set( registry.modules.sessions .listSessions(undef`
- L1195 — passed where (root: string, machineId?: (string & BRAND<"MachineId">) | undefined) => boolean is expected — that caller reads the value, not a promise
  `(root, machineId) => repos.inferFromPath(root, machineId ?? registry.modules.machine`
- L1216 — passed where (threadId?: (string & BRAND<"ThreadId">) | undefined) => { name: string; description: string; inputSchema: unknown; }[] is expected — that caller reads the value, not a promise
  `(threadId) => superagent.mcpToolSpecs(threadId)`
- L1391 — passed where (resolve: (value: ServerHandle | PromiseLike<ServerHandle>) => void, reject: (reason?: any) => void) => void is expected — that caller reads the value, not a promise
  `(resolve, reject) => { let settled = false const failListen = (err: unknown): void`
- L1418 — passed where ((request: Request) => { userId: string & BRAND<"UserId">; userRole: "member" | "admin"; credentialId?: string | undefined; } | undefined) | undefined is expected — that caller reads the value, not a promise
  `(request) => { if ( request.headers.has('authorization') && `
- L1500 — passed where (threadId: string & BRAND<"ThreadId">) => IssueTrpc is expected — that caller reads the value, not a promise
  `(threadId) => { const ownerUserId = superagent.threadOwner(threadId) const acc`
- L1748 — passed where () => void is expected — that caller reads the value, not a promise
  `() => registry.modules.logs.close()`
- L1750 — passed where () => void is expected — that caller reads the value, not a promise
  `() => registry.modules.fleetLogs.close()`

### apps/server/src/modules/lock/service.test.ts (16)
- L25 — passed where <T>(fn: () => T) => T is expected — that caller reads the value, not a promise
  `(fn) => store.transact(fn)`
- L78 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => svc.release(stale, { repoPath: REPO, name: 'merge:main' })`
- L99 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => svc.renew(restarted, { repoPath: REPO, name: 'merge:main' })`
- L100 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => svc.release(restarted, { repoPath: REPO, name: 'merge:main' })`
- L108 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => svc.renew(first, { repoPath: REPO, name: 'merge:main' })`
- L109 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => svc.release(first, { repoPath: REPO, name: 'merge:main' })`
- L261 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => svc.acquire(sibling, { repoPath: REPO, name: 'l' })`
- L270 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => svc.acquire(sibling, { repoPath: REPO, name: 'l' })`
- L289 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => svc.acquire(third, { repoPath: REPO, name: 'l' })`
- L323 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => svc.acquire(on539, { repoPath: REPO, name: 'test:heavy' })`
- L388 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => svc.release(agent(2), { repoPath: REPO, name: 'l' })`
- L401 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => svc.release(agent(2), { repoPath: REPO, name: 'l' })`
- L423 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => svc.renew(agent(2), { repoPath: REPO, name: 'l' })`
- L424 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => svc.renew(agent(1), { repoPath: REPO, name: 'nope' })`
- L511 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => svc.cancel(agent(1), { repoPath: REPO, name: 'l' })`
- L513 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => svc.cancel(agent(2), { repoPath: REPO, name: 'l' })`

### apps/server/src/modules/issues/registry.test.ts (15)
- L308 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => guardIssueCommand(viewer, reg.issues, 'list', issueRegistry.defs.list, {})`
- L311 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => guardIssueCommand(viewer, reg.issues, 'create', issueRegistry.defs.create, { `
- L327 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => guardIssueCommand(scoped, reg.issues, 'update', issueRegistry.defs.update, { `
- L333 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => guardIssueCommand(scoped, reg.issues, 'update', issueRegistry.defs.update, { `
- L339 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => guardIssueCommand( { ...scoped, overrideScope: true }, reg.iss`
- L357 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => guardIssueCommand(scoped, reg.issues, 'update', issueRegistry.defs.update, { `
- L400 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => guardIssueCommand(scoped, reg.issues, name, definition, insideInput)`
- L403 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => guardIssueCommand(scoped, reg.issues, name, definition, outsideInput)`
- L406 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => guardIssueCommand( { ...scoped, overrideScope: true }, r`
- L415 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => guardIssueCommand(viewer, reg.issues, name, definition, insideInput)`
- L429 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => guardIssueCommand(scoped, reg.issues, 'mailSend', issueRegistry.defs.mailSend,`
- L436 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => guardIssueCommand(scoped, reg.issues, 'delete', issueRegistry.defs.delete, { i`
- L440 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => guardIssueCommand({ capability: OPERATOR }, reg.issues, 'delete', issueRegistr`
- L565 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => dispatcher.run( { capability: agentCaller(root.id).capability }, `
- L592 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => guardIssueCommand( { capability: { role: 'viewer', scope: { `

### apps/server/src/modules/machines/service.test.ts (14)
- L193 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => serviceListing([{ ...runnable, online: true }]).requireAgent(MACHINE, 'codex')`
- L237 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => serviceListing([{ id: MACHINE, name: 'vmi', online: true }]).requireAgent(MACH`
- L240 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => serviceListing([{ id: MACHINE, name: 'vmi', online: true, use: 'denied' }]).re`
- L432 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => svc.requireAgent(MACHINE, 'claude-code')`
- L466 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => svc.resolveMachineForAgent(MACHINE, '/repo', 'codex')`
- L556 — passed where (principal: ClientPrincipal, machines: MachinesService) => { id: string & BRAND<"MachineId">; name: string; hostname: string; lastSeenAt: string; ... 16 more ...; targetUnavailableReason?: string | ... 1 more ... | undefined; }[] is expected — that caller reads the value, not a promise
  `() => { broadcasts.push(svc.ownershipRows().find((r) => r.id === MACHINE)?.ownerUs`
- L644 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => svc.transferMachineOwnership(MACHINE, asUserId(OWNER_A), asUserId(OWNER_B))`
- L659 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => svc.transferMachineOwnership(MACHINE, asUserId('user:typo'), asUserId(OWNER_`
- L676 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => svc.transferMachineOwnership(MACHINE, asUserId(OWNER_A), asUserId(OWNER_A))`
- L815 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => svc.adoptMachine(MACHINE, asUserId(BOB))`
- L816 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => svc.adoptMachine(MACHINE, asUserId(ALICE))`
- L841 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => svc.adoptMachine(MACHINE, asUserId(BOB))`
- L850 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => svc.adoptMachine(MACHINE, asUserId('user:typo'))`
- L866 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => svc.adoptMachine(asMachineId('ghost'), asUserId(ALICE))`

### apps/server/src/modules/workflows/engine.test.ts (14)
- L1451 — passed where () => unknown is expected — that caller reads the value, not a promise
  `() => h.service.resolveRevision({ sessionId: asSessionId('s1'), `
- L1463 — passed where () => unknown is expected — that caller reads the value, not a promise
  `() => h.service.resolveRevision({ sessionId: asSessionId('s1'), `
- L1673 — passed where () => unknown is expected — that caller reads the value, not a promise
  `() => h.service.executionProfileForLaunch({ caller: operator, `
- L1684 — passed where () => unknown is expected — that caller reads the value, not a promise
  `() => h.service.executionProfileForLaunch({ caller: operator, `
- L1696 — passed where () => unknown is expected — that caller reads the value, not a promise
  `() => h.service.executionProfileForLaunch({ caller: operator, `
- L1706 — passed where () => unknown is expected — that caller reads the value, not a promise
  `() => h.service.executionProfileForLaunch({ caller: operator, `
- L1716 — passed where () => unknown is expected — that caller reads the value, not a promise
  `() => h.service.executionProfileForLaunch({ caller: operator, profileId: 'wfp_no`
- L1817 — passed where () => unknown is expected — that caller reads the value, not a promise
  `() => h.service.executionProfileForLaunch({ caller: operator, `
- L2323 — passed where () => unknown is expected — that caller reads the value, not a promise
  `() => h.service.startRun({ sessionId: asSessionId('s3'), `
- L2901 — passed where () => unknown is expected — that caller reads the value, not a promise
  `() => h.service.prepareStart({ sessionId: asSessionId('s2'), `
- L3328 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => dispatchWorkflowRpc(h.service, operator, 'get', {})`
- L3359 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => dispatchWorkflowRpc( h.service, operator, 'pub`
- L3368 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => dispatchWorkflowRpc(h.service, operator, 'get', { id: created.workflow.id },`
- L3646 — passed where () => unknown is expected — that caller reads the value, not a promise
  `() => h.service.startRun({ sessionId: asSessionId('s1'), `

### apps/server/src/modules/issues/service/reads.ts (13)
- L99 — argument of .map() — the caller consumes the value synchronously
  `(r) => this.store.toWire(r, commentCounts, batch)`
- L110 — argument of .map() — the caller consumes the value synchronously
  `(r) => this.store.toWire(r, commentCounts, batch)`
- L122 — argument of .map() — the caller consumes the value synchronously
  `(r) => { const w = this.store.toWire(r, commentCounts, batch) return { `
- L179 — argument of .map() — the caller consumes the value synchronously
  `(r) => this.store.toWire(r, commentCounts, batch)`
- L319 — argument of .map() — the caller consumes the value synchronously
  `(row) => { const closed = this.store.isClosed(row) const blocked = this.st`
- L361 — argument of .filter() — the caller consumes the value synchronously
  `(r) => this.epicStatus(r.id, mayRead).complete`
- L362 — argument of .map() — the caller consumes the value synchronously
  `(r) => this.store.toWire(r, commentCounts, batch)`
- L408 — argument of .map() — the caller consumes the value synchronously
  `(r) => this.store.toWire(r, commentCounts, batch)`
- L498 — argument of .map() — the caller consumes the value synchronously
  `(r) => this.store.toWire(r, commentCounts, batch)`
- L545 — argument of .map() — the caller consumes the value synchronously
  `(r) => this.store.toWire(r, commentCounts, batch)`
- L700 — argument of .map() — the caller consumes the value synchronously
  `(b) => `${this.niceRef(b)} (${b.title})``
- L754 — argument of .map() — the caller consumes the value synchronously
  `(k) => ` - ${this.niceRef(k)} (${k.title})``
- L777 — argument of .map() — the caller consumes the value synchronously
  `(i) => ` - ${this.niceRef(i)} (${i.title})``

### apps/server/src/modules/messages/service.ts (11)
- L541 — passed where (targets: readonly DeliveryTarget[]) => void is expected — that caller reads the value, not a promise
  `(targets) => { for (const target of targets) this.queueDeliveryTarget(target) `
- L563 — passed where (from: MessageSender, input: MessageSendInput, opts?: MessageSendOptions | undefined) => MessageSendResult is expected — that caller reads the value, not a promise
  `(from, input, opts) => this.send(from, input, opts)`
- L569 — passed where (message: MessageRow, kind: string, extra?: Record<string, unknown> | undefined) => void is expected — that caller reads the value, not a promise
  `(message, kind, extra) => this.emitTransition(message, kind, extra)`
- L570 — passed where (message: MessageRow) => string is expected — that caller reads the value, not a promise
  `(message) => this.render.fromLabel(message)`
- L771 — passed where (session: { status: "live" | "starting" | "reconnecting" | "hibernated" | "exited"; geometry: { rows: number; cols: number; }; agentKind: "claude-code" | "codex" | "grok" | "opencode" | "cursor" | "pi" | "shell"; ... 67 more ...; upstreamStale?: boolean | undefined; }, messages: readonly MessageRow[], nowMs: number)... is expected — that caller reads the value, not a promise
  `(session, messages, nowMs) => this.drainPreferred(session, messages, nowMs)`
- L772 — passed where (message: MessageRow, nowMs: number) => void is expected — that caller reads the value, not a promise
  `(message, nowMs) => { if (!this.prepareQueuedAttemptSafely(message, nowMs)) return`
- L794 — argument of .filter() — the caller consumes the value synchronously
  `(message) => this.prepareQueuedAttemptSafely(message, nowMs)`
- L1450 — passed where ((receipt: { at: string; outcome: "accepted"; turnEpoch: number; deliveredAs: "interrupt" | "when-ready" | "queue" | "steer"; provenBy: "hook" | "sdk-callback" | "protocol-ack" | "transcript-echo"; } | { ...; } | { ...; } | { ...; }) => void) | undefined is expected — that caller reads the value, not a promise
  `(receipt) => { if (recorded) settleReceipt(receipt, true) else if (awa`
- L1739 — passed where () => void is expected — that caller reads the value, not a promise
  `() => { this.onSessionEligibilityChanged(session.sessionId, session, { prefe`
- L1915 — passed where ((receipt: { at: string; outcome: "accepted"; turnEpoch: number; deliveredAs: "interrupt" | "when-ready" | "queue" | "steer"; provenBy: "hook" | "sdk-callback" | "protocol-ack" | "transcript-echo"; } | { ...; } | { ...; } | { ...; }) => void) | undefined is expected — that caller reads the value, not a promise
  `(receipt) => { this.reconcileReceipt(reconcile, session.sessionId, receipt, re`
- L2052 — argument of .map() — the caller consumes the value synchronously
  `(message) => this.withQueuePosition(message)`

### apps/server/src/modules/issues/service/crud.ts (10)
- L244 — argument of .map() — the caller consumes the value synchronously
  `({ id }) => this.store.draftOrThrow(this.store.resolveRef(id))`
- L1417 — passed where () => void is expected — that caller reads the value, not a promise
  `() => { // The prepare/apply pair has always been draft-then-install; POD-3259 `
- L1427 — passed where () => void is expected — that caller reads the value, not a promise
  `() => { this.store.installRow(row.id, row) this.store.emitEvent('issue.del`
- L1431 — passed where () => void is expected — that caller reads the value, not a promise
  `() => this.store.broadcastList()`
- L1531 — passed where () => void is expected — that caller reads the value, not a promise
  `() => { // The prepare/apply pair has always been draft-then-install; POD-3259 `
- L1541 — passed where () => void is expected — that caller reads the value, not a promise
  `() => { this.store.installRow(row.id, row) this.store.emitEvent('issue.res`
- L1545 — passed where () => void is expected — that caller reads the value, not a promise
  `() => this.store.broadcastList()`
- L1552 — passed where (() => void) | undefined is expected — that caller reads the value, not a promise
  `() => this.store.deps.store.issues.setIssueLabels(asIssueId(id), labels)`
- L1574 — passed where (() => void) | undefined is expected — that caller reads the value, not a promise
  `() => this.store.deps.store.grants.upsert({ resourceKind: 'issue', r`
- L1595 — passed where (() => void) | undefined is expected — that caller reads the value, not a promise
  `() => this.store.deps.store.grants.remove('issue', row.id, grantee, verb)`

### apps/server/src/modules/shipping/service.test.ts (10)
- L137 — passed where (rootIssueId: string & BRAND<"IssueId">, approvedHeadSha: string) => { rootIssueId: string & BRAND<"IssueId">; approvedHeadSha: string; descendants: { issueId: string & BRAND<...>; approvedHeadSha: string; }[]; } | null is expected — that caller reads the value, not a promise
  `(rootIssueId, approvedHeadSha) => ({ rootIssueId, approvedHead`
- L646 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => service.deliveryReceipt({ orderId, principal: approval.p`
- L929 — passed where () => void is expected — that caller reads the value, not a promise
  `() => { void provedShippingJob(input, machineId).then(resolve) }`
- L977 — passed where () => void is expected — that caller reads the value, not a promise
  `() => { void provedShippingJob(input, machineId).then(resolve) }`
- L1096 — passed where () => void is expected — that caller reads the value, not a promise
  `() => { void provedShippingJob(input, machineId).then(resolve) }`
- L1150 — passed where ((input: { order: { state: "queued" | "cancelled" | "preflight" | "composing" | "validating" | "repairing" | "landing" | "publishing" | "verifying" | "shipped" | "held"; id: string & BRAND<...>; ... 20 more ...; holdCode?: string | undefined; }; attempt: { ...; }; issue: { ...; }; names: readonly string[]; ttlSecond... is expected — that caller reads the value, not a promise
  `( input: Parameters< NonNullable< ConstructorParameter`
- L1421 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => store.shipping.claimAttempt({ orderId: order.id, expectedState`
- L1435 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => store.shipping.finishAttempt(claimed.attempt.id, 2, { finishedAt: '202`
- L1441 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => store.shipping.finishAttempt(claimed.attempt.id, 1, { finishedAt: '202`
- L1523 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => store.shipping.claimAttempt({ orderId: order.id, expectedState`

### apps/server/src/modules/messages/service.test.ts (9)
- L259 — passed where ((row: IssueMessageRow) => void) | undefined is expected — that caller reads the value, not a promise
  `(row) => store.issues.addIssueMessage(row)`
- L260 — passed where ((issueId: string & BRAND<"IssueId">, ids: string[]) => void) | undefined is expected — that caller reads the value, not a promise
  `(issueId, ids) => store.issues.markIssueMessagesRead(FIRST_ADMIN_USER_ID, issueId, i`
- L2206 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => svc.send( { kind: 'operator' }, { to: { kind: 'session', id: a`
- L2212 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => svc.send( { kind: 'operator' }, { to: { kind: 'sessi`
- L2841 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => svc.dismiss(dismissed.message.id, 's1')`
- L4405 — passed where ((i: { sessionId: string & BRAND<"SessionId">; text: string; }) => { ok: boolean; queued?: boolean | undefined; reason?: string | undefined; }) | undefined is expected — that caller reads the value, not a promise
  `() => { if (!retriggered) { retriggered = true store.mes`
- L4677 — passed where (svc: MessageDeliveryService) => void is expected — that caller reads the value, not a promise
  `(svc) => { for (const id of batchIds) svc.onIssueEligibilityChanged(id) }`
- L4680 — passed where (svc: MessageDeliveryService) => void is expected — that caller reads the value, not a promise
  `(svc) => { svc.onIssuesEligibilityChanged(batchIds) }`
- L4760 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => svc.reconcileQueued()`

### apps/server/src/modules/settings/service.commands.test.ts (9)
- L126 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => service.setSettingsFor(USER, { ...current, apiKeys: { ...curre`
- L133 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => service.setSettingsFor(USER, { ...current, apiKeys: { ...curre`
- L155 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => service.setSettingsFor(USER, { ...current, apiKeys: { ...current.apiKeys, anth`
- L168 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => service.setSettingsFor(USER, { ...current, apiKeys: { ...curre`
- L193 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => service.setSettingsFor(USER, next)`
- L276 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => service.updatePreferences(USER, { 'hibernation.memoryPct': 'not a number' })`
- L279 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => service.updatePreferences(USER, { 'gitWorkflow.mergeStyle': 'octopus' })`
- L286 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => service.updatePreferences(USER, { 'apiKeys.openai': 'sk-via-patch' })`
- L292 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => service.updatePreferences(USER, { [key]: 'x' })`

### apps/server/src/modules/sessions/command-plane.ts (8)
- L648 — passed where Handler is expected — that caller reads the value, not a promise
  `(ctx: SessionCommandCtx, input: TargetInput) => { // Absent ⇒ the pinned shape: kill n`
- L654 — passed where Handler is expected — that caller reads the value, not a promise
  `(ctx: SessionCommandCtx, input: TargetInput) => ctx.target(input.sessionId, 'sessions.`
- L659 — passed where Handler is expected — that caller reads the value, not a promise
  `(ctx: SessionCommandCtx, input: InterruptInput) => { if (!ctx.target(input.sessionId, `
- L693 — passed where Handler is expected — that caller reads the value, not a promise
  `( ctx: SessionCommandCtx, input: { sessionId: SessionId; model?: string; effort?: `
- L703 — passed where Handler is expected — that caller reads the value, not a promise
  `(ctx: SessionCommandCtx, input: TargetInput) => ctx.target(input.sessionId, 'sessions.`
- L712 — passed where Handler is expected — that caller reads the value, not a promise
  `(ctx: SessionCommandCtx, input: AnswerInput) => { // WHICH HUMAN answered is the trans`
- L723 — passed where Handler is expected — that caller reads the value, not a promise
  `(ctx: SessionCommandCtx, input: TargetInput) => { if (!ctx.target(input.sessionId, 'se`
- L735 — passed where Handler is expected — that caller reads the value, not a promise
  `(ctx: SessionCommandCtx, input: { sessionId: SessionId; force?: boolean }) => { if (!c`

### apps/server/src/modules/superagent/registry.ts (8)
- L67 — passed where (service: SuperagentService, input: never, ownerUserId: string & BRAND<"UserId">) => unknown is expected — that caller reads the value, not a promise
  `(s: SuperagentService, input: In<'sendTurn'>, ownerUserId: UserId) => s.sendTurn({ .`
- L72 — passed where (service: SuperagentService, input: never, ownerUserId: string & BRAND<"UserId">) => unknown is expected — that caller reads the value, not a promise
  `(s: SuperagentService, input: In<'interruptTurn'>, ownerUserId: UserId) => s.interru`
- L77 — passed where (service: SuperagentService, input: never, ownerUserId: string & BRAND<"UserId">) => unknown is expected — that caller reads the value, not a promise
  `(s: SuperagentService, input: In<'openInTerminal'>, ownerUserId: UserId) => s.openIn`
- L82 — passed where (service: SuperagentService, input: never, ownerUserId: string & BRAND<"UserId">) => unknown is expected — that caller reads the value, not a promise
  `(s: SuperagentService, input: In<'clear'>, ownerUserId: UserId) => s.clear(ownerUser`
- L87 — passed where (service: SuperagentService, input: never, ownerUserId: string & BRAND<"UserId">) => unknown is expected — that caller reads the value, not a promise
  `(s: SuperagentService, input: In<'restart'>, ownerUserId: UserId) => s.restartThread`
- L92 — passed where (service: SuperagentService, input: never, ownerUserId: string & BRAND<"UserId">) => unknown is expected — that caller reads the value, not a promise
  `(s: SuperagentService, input: In<'ensureSession'>, ownerUserId: UserId) => s.ensureS`
- L97 — passed where (service: SuperagentService, input: never, ownerUserId: string & BRAND<"UserId">) => unknown is expected — that caller reads the value, not a promise
  `(s: SuperagentService, input: In<'startBtw'>, ownerUserId: UserId) => s.startBtwTurn`
- L102 — passed where (service: SuperagentService, input: never, ownerUserId: string & BRAND<"UserId">) => unknown is expected — that caller reads the value, not a promise
  `(s: SuperagentService, input: In<'concierge'>, ownerUserId: UserId) => s.conciergeTu`

### apps/server/src/modules/settings/registry.ts (7)
- L75 — passed where SettingsHandler<{ values: Record<string, unknown>; mutationId?: (string & BRAND<"MutationId">) | undefined; }, unknown> is expected — that caller reads the value, not a promise
  `(svc, input, actor) => svc.updatePreferences(actor, input.values)`
- L86 — passed where SettingsHandler<{ values: Record<string, unknown>; }, unknown> is expected — that caller reads the value, not a promise
  `(svc, input, actor) => svc.updatePreferences(actor, input.values)`
- L94 — passed where SettingsHandler<{ value: string; key: "apiKeys.openrouter" | "apiKeys.anthropic" | "apiKeys.openai" | "integrations.linearApiKey" | "notifications.telegramBotToken"; }, unknown> is expected — that caller reads the value, not a promise
  `(svc, input) => svc.setSecret(input.key, input.value)`
- L101 — passed where SettingsHandler<{ key: "apiKeys.openrouter" | "apiKeys.anthropic" | "apiKeys.openai" | "integrations.linearApiKey" | "notifications.telegramBotToken"; }, unknown> is expected — that caller reads the value, not a promise
  `(svc, input) => svc.clearSecret(input.key)`
- L118 — passed where SettingsHandler<{}, unknown> is expected — that caller reads the value, not a promise
  `(svc) => svc.secretPresenceList()`
- L125 — passed where SettingsHandler<{}, unknown> is expected — that caller reads the value, not a promise
  `(svc, _input, actor) => svc.startTelegramSetup(actor)`
- L132 — passed where SettingsHandler<{ setupId: string; }, unknown> is expected — that caller reads the value, not a promise
  `(svc, input) => svc.pollTelegramSetup(input.setupId)`

### apps/server/src/issues.attach.test.ts (7)
- L169 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => svc.attachSession({ sessionId: asSessionId('s1'), targetId: other.id })`
- L233 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => svc.attachSession({ sessionId: asSessionId('s1'), newSubissue:`
- L332 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => svc.attachSession({ sessionId: asSessionId('s1'), newSpinoff: `
- L338 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => svc.attachSession({ sessionId: asSessionId('s1'), newSubissue:`
- L348 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => svc.attachSession({ sessionId: asSessionId('s1'), newSpinoff: `
- L437 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => svc.attachSession({ sessionId: asSessionId('s1') })`
- L438 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => svc.attachSession({ sessionId: asSessionId('s1'), targetId: 'iss_nope' })`

### apps/server/src/modules/issues/service/issue-registry-model.test.ts (7)
- L110 — passed where () => void is expected — that caller reads the value, not a promise
  `() => { svc.update(id, { title: 'from the inner write' }) }`
- L113 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => svc.update(id, { title: 'from the outer write' })`
- L131 — passed where () => void is expected — that caller reads the value, not a promise
  `() => { observedDuringWrite = svc.get(id)?.title }`
- L135 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => svc.update(id, { title: 'never committed' })`
- L166 — passed where () => void is expected — that caller reads the value, not a promise
  `() => { svc.update(id, { title: 'winner' }) }`
- L169 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => svc.update(id, { title: 'loser' })`
- L182 — passed where () => void is expected — that caller reads the value, not a promise
  `() => { observed.push(svc.get(id)?.title) observed.push(svc.list().find((issue`

### apps/server/src/store/shipping.ts (6)
- L1019 — argument of .map() — the caller consumes the value synchronously
  `(id) => { const order = this.getOrder(id) if (!order || order.state !== 'q`
- L1052 — argument of .map() — the caller consumes the value synchronously
  `(order) => { const row = this.db .select({ branch: iss`
- L1183 — argument of .map() — the caller consumes the value synchronously
  `(order) => { const previous = this.latestAttemptForOrder(order.id) return `
- L1650 — argument of .map() — the caller consumes the value synchronously
  `(member) => this.getOrder(member.orderId)`
- L1654 — argument of .every() — the caller consumes the value synchronously
  `(member) => !this.getAttempt(member.attemptId)?.finishedAt`
- L1715 — argument of .map() — the caller consumes the value synchronously
  `(order) => { const issue = this.db .select({ repoPath: issues.repoPath, `

### apps/server/src/modules/sessions/session-state/service.ts (6)
- L339 — passed where () => void is expected — that caller reads the value, not a promise
  `() => this.ports.store.sessions.markSessionRead( principal.userId, s`
- L350 — passed where () => void is expected — that caller reads the value, not a promise
  `() => this.ports.store.sessions.markSessionUnread(principal.userId, sessionId)`
- L362 — passed where () => void is expected — that caller reads the value, not a promise
  `() => this.ports.store.sessions.setSnooze(principal.userId, sessionId, until)`
- L369 — passed where () => void is expected — that caller reads the value, not a promise
  `() => this.ports.store.sessions.clearSnooze(principal.userId, sessionId)`
- L378 — passed where (() => void) | undefined is expected — that caller reads the value, not a promise
  `() => this.ports.store.sessions.clearAllSnoozes(sessionId)`
- L674 — passed where () => void is expected — that caller reads the value, not a promise
  `() => { this.draftDocWriteTimers.delete(sessionId) this.writeDraftDoc(this.dra`

### apps/server/src/modules/lock/service.ts (6)
- L339 — passed where () => { granted: true; alreadyHeld: false; lock: Promise<LockWire>; position?: undefined; } | { granted: true; alreadyHeld: true; lock: Promise<LockWire>; position?: undefined; } | { ...; } is expected — that caller reads the value, not a promise
  `() => { this.sweepExpired(repoId) const existing = this.deps.locks.get`
- L409 — passed where () => { released: true; next: LockHolderWire | null; } is expected — that caller reads the value, not a promise
  `() => { this.sweepExpired(repoId) const existing = this.deps.locks.get`
- L438 — passed where () => { cancelled: true; } is expected — that caller reads the value, not a promise
  `() => { this.sweepExpired(repoId) const existing = this.deps.locks.get`
- L492 — argument of .map() — the caller consumes the value synchronously
  `(l) => this.toWire(l)`
- L508 — passed where () => { lock: Promise<LockWire>; previousHolder: LockHolderWire | null; } is expected — that caller reads the value, not a promise
  `() => { this.sweepExpired(repoId) const existing = this.deps.locks.get`
- L549 — passed where () => void is expected — that caller reads the value, not a promise
  `() => { for (const w of this.deps.locks.listWaitsBySession(sessionId)) { `

### apps/server/src/modules/superagent/service.ts (6)
- L269 — passed where () => void is expected — that caller reads the value, not a promise
  `() => this.reapStaleTurns()`
- L292 — passed where Listener<"machine.connected"> is expected — that caller reads the value, not a promise
  `({ machineId }) => { service.resumePendingTurns(machineId) }`
- L900 — passed where () => void is expected — that caller reads the value, not a promise
  `() => { const current = this.store.superagent .listPendingTurns() `
- L1121 — passed where () => void is expected — that caller reads the value, not a promise
  `() => { this.interruptFallbacks.delete(pending.turnId) const still = this.`
- L1514 — argument of .map() — the caller consumes the value synchronously
  `(s) => this.sessionInfo(s.sessionId) ?? { sessionId: s.sessionId }`
- L1571 — argument of .map() — the caller consumes the value synchronously
  `(id) => this.sessionInfo(id)`

### apps/server/src/issues.ledger.test.ts (6)
- L96 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => ledger.commit({ write: async () => await store.issues.upsertIssue({ ..`
- L167 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => svc.create({ repoPath: '/r', title: 'phantom', startNow: false })`
- L196 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => svc.update(wire.id, { title: 'phantom' })`
- L236 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => svc.setLabels(wire.id, ['urgent'])`
- L257 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => svc.purgeEmptyDraft(wire.id)`
- L387 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => ledger.commit({ write: async () => await store.issues.upsertIssue({ ..`

### apps/server/src/store.test.ts (6)
- L64 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => store.repos.addRepo('/home/u/fenced', store.hostMachineId)`
- L66 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => store.repos.addRepo('/home/u/reopened', store.hostMachineId)`
- L198 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => s.sessions.upsertSession(row({ agentKind: 'auto' }))`
- L514 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => store.sessions.markSessionRead(asUserId(''), asSessionId('s_read'), 't')`
- L740 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => store.sessions.setTabOrder(asUserId(SOLE_USER_ID), ' ', ['s1'])`
- L1043 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => s.superagent.loadSuperagentMessages('global')`

### apps/server/src/modules/automations/scheduler.test.ts (6)
- L289 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => h.service.create({ name: 'Too late', scheduleKind: 'once', `
- L303 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => h.service.create({ name: 'Bad', cron: 'every tuesday', agentKind: 'codex', pro`
- L390 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => h.service.setEnabled(a.id, true)`
- L395 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => h.service.create({ name: 'Unknown harness', scheduleKind: 'onc`
- L414 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => h.service.update(ok.id, { agentKind: 'not-a-harness' })`
- L761 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => h.service.tick()`

### apps/server/src/modules/updates/operation.test.ts (6)
- L1289 — passed where (() => boolean) | undefined is expected — that caller reads the value, not a promise
  `() => driver()?.active(LIFECYCLE_EXCLUSION_GROUP) !== undefined`
- L1290 — passed where ((channel: "stable" | "edge" | "dev") => string | undefined) | undefined is expected — that caller reads the value, not a promise
  `(channel) => exclusiveUpdateVersion(driver()?.active(LIFECYCLE_EXCLUSION_GROUP), c`
- L1316 — passed where ((operationId: string, patch: Record<string, unknown>) => void) | undefined is expected — that caller reads the value, not a promise
  `(id, patch) => { driver().recordDetails(id, patch) }`
- L1329 — passed where ((operationId: string, stepId: string, patch: StepProgressPatch) => void) | undefined is expected — that caller reads the value, not a promise
  `(id, stepId, patch) => { void driver().recordProgress(id, stepId, patch) }`
- L1335 — passed where ((operationId: string, stepId: string) => boolean) | undefined is expected — that caller reads the value, not a promise
  `(id, stepId) => driver().watching(id, stepId)`
- L1619 — passed where (() => void) | undefined is expected — that caller reads the value, not a promise
  `() => { const operation = h.read() seen.push({ state: stepState(`

### apps/server/src/modules/issues/service/core.ts (5)
- L812 — argument of .map() — the caller consumes the value synchronously
  `(r) => this.toWireMemo(r, commentCounts, batch, prefixFor(r.repoPath))`
- L1183 — passed where (result: { type: "task" | "automation" | "bug" | "feature" | "chore" | "epic" | "decision" | "spike" | "story" | "milestone"; id: string & BRAND<"IssueId">; title: string; createdAt: string; ... 71 more ...; comments?: { ...; }[] | undefined; }) => EntityChangeSpec[] is expected — that caller reads the value, not a promise
  `(w) => [ { entity: 'issue', id: row.id, op: 'upsert', value: w }, ...this.`
- L1260 — argument of .map() — the caller consumes the value synchronously
  `(row) => this.toWire(row, undefined, batch)`
- L1261 — argument of .map() — the caller consumes the value synchronously
  `(event) => this.deps.store.events.appendEvent( { ts: t`
- L1279 — argument of .flatMap() — the caller consumes the value synchronously
  `(row, index) => [ { entity: 'issue' as const, id: row.id`

### apps/server/src/modules/sessions/session-state/registry.ts (5)
- L330 — passed where SessionStateHandler is expected — that caller reads the value, not a promise
  `(input, principal, deps) => { deps.state.setSnooze( principal, sessi`
- L341 — passed where SessionStateHandler is expected — that caller reads the value, not a promise
  `(input, principal, deps) => { deps.state.clearSnooze(principal, sessionIdOf(input.se`
- L348 — passed where SessionStateHandler is expected — that caller reads the value, not a promise
  `(input, principal, deps) => { return deps.state.setPin(principal, input.kind as neve`
- L354 — passed where SessionStateHandler is expected — that caller reads the value, not a promise
  `(input, principal, deps) => { return deps.state.setTabOrder( principal, `
- L364 — passed where SessionStateHandler is expected — that caller reads the value, not a promise
  `(input, principal, deps) => { const edit = input.edit as { kind: 'replace'; text: st`

### apps/server/src/modules/sessions/command-ctx.ts (5)
- L77 — passed where (sessionId: string & BRAND<"SessionId">) => (string & BRAND<"SessionId">) | undefined is expected — that caller reads the value, not a promise
  `(sessionId) => // POD-1646: the narrow read. Authorization runs on essentially every`
- L115 — passed where (repoPath: string, agentKind: "claude-code" | "codex" | "grok" | "opencode" | "cursor" | "pi" | "shell" | undefined, issueId: (string & BRAND<"IssueId">) | undefined, ownership: { ...; }) => { ...; } is expected — that caller reads the value, not a promise
  `(repoPath, agentKind, issueId, ownership) => issues.createDraftFor(repoPath, agentKi`
- L120 — passed where (issueId: string & BRAND<"IssueId">) => boolean is expected — that caller reads the value, not a promise
  `(issueId) => issues.discardUnlaunchedDraft(issueId)`
- L123 — passed where () => SessionTargetRow[] is expected — that caller reads the value, not a promise
  `() => sessions.listSessions()`
- L124 — passed where ((sessionId: string & BRAND<"SessionId">) => SessionTargetRow | undefined) | undefined is expected — that caller reads the value, not a promise
  `(sessionId) => sessions.sessionById(sessionId)`

### apps/server/src/modules/fleet/authz.ts (5)
- L351 — passed where (sessionId: string & BRAND<"SessionId">) => (string & BRAND<"SessionId">) | undefined is expected — that caller reads the value, not a promise
  `(sessionId) => // POD-1646: the narrow read. Authorization runs on essentially every`
- L361 — passed where () => string & BRAND<"MachineId"> is expected — that caller reads the value, not a promise
  `() => machines.defaultMachine()`
- L362 — passed where () => (string & BRAND<"MachineId">)[] is expected — that caller reads the value, not a promise
  `() => machines.ownershipRows().map((row) => row.id)`
- L363 — passed where (machineId: string & BRAND<"MachineId">) => string | undefined is expected — that caller reads the value, not a promise
  `(machineId) => machines.ownershipRows().find((r) => r.id === machineId)?.name`
- L364 — passed where (machineId: string & BRAND<"MachineId">) => string | null | undefined is expected — that caller reads the value, not a promise
  `(machineId) => machines.effectiveOwner(machineId)`

### apps/server/src/ledger.commit-application.test.ts (5)
- L88 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => store.transact(async () => { await ledger.commit({ write: as`
- L143 — passed where ((result: unknown, changes: ({ id: string; seq: number; entity: "session"; op: "upsert" | "remove"; value?: { status: "live" | "starting" | "reconnecting" | "hibernated" | "exited"; geometry: { rows: number; cols: number; }; ... 68 more ...; upstreamStale?: boolean | undefined; } | undefined; } | ... 11 more ... | {... is expected — that caller reads the value, not a promise
  `() => seen.push(foldedIds().includes('c-order-outer') ? 'after' : 'before')`
- L150 — passed where ((result: unknown, changes: ({ id: string; seq: number; entity: "session"; op: "upsert" | "remove"; value?: { status: "live" | "starting" | "reconnecting" | "hibernated" | "exited"; geometry: { rows: number; cols: number; }; ... 68 more ...; upstreamStale?: boolean | undefined; } | undefined; } | ... 11 more ... | {... is expected — that caller reads the value, not a promise
  `() => seen.push(foldedIds().includes('c-order-inner') ? 'after' : 'before')`
- L169 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => ledger.commit({ arbitrate: { rowId: expRevRow.id, `
- L192 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => ledger.commit({ write: () => { throw new Error('the write fa`

### apps/server/src/modules/lifecycle-runtime-fold.test.ts (5)
- L49 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => store.transact(() => { registry.modules.sessions.killSession({ session`
- L87 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => store.transact(async () => { await registry.modules.issueSessionLifecy`
- L119 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => store.transact(async () => { await registry.modules.issueSessionLifecy`
- L155 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => store.transact(async () => { await registry.modules.issueSessionLifecy`
- L185 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => store.transact(async () => { await registry.modules.issueSessionLifecy`

### apps/server/src/modules/issues/service/row-install-fold.test.ts (5)
- L47 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => store.transact(async () => { await issues.update(created.id, { title: `
- L99 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => store.transact(async () => { await issues.update(created.id, { title: `
- L124 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => store.transact(async () => { await issues.create({ repoPath: '/repo', `
- L152 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => store.transact(async () => { await issues.create({ repoPath: '/repo', `
- L210 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => store.transact(async () => { await issues.create({ repoPath: '/rep`

### apps/server/src/modules/memory/lake.ts (4)
- L86 — passed where ((machineId: string & BRAND<"MachineId">, nativeId: string, lakePath: string) => void) | undefined is expected — that caller reads the value, not a promise
  `(machineId, nativeId, lakePath) => indexer.onBytes(machineId, nativeId, lakePath)`
- L87 — passed where ((machineId: string & BRAND<"MachineId">, nativeId: string) => void) | undefined is expected — that caller reads the value, not a promise
  `(machineId, nativeId) => indexer.onTruncate(machineId, nativeId)`
- L88 — passed where ((machineId: string & BRAND<"MachineId">, nativeId: string) => void) | undefined is expected — that caller reads the value, not a promise
  `(machineId, nativeId) => indexer.onIncarnation(machineId, nativeId)`
- L219 — passed where (resolve: (value: MirrorReadReply | PromiseLike<MirrorReadReply>) => void, reject: (reason?: any) => void) => void is expected — that caller reads the value, not a promise
  `(resolve) => { let settled = false const settle = (result: MirrorReadReply): v`

### apps/server/src/modules/memory/search.ts (4)
- L78 — argument of .filter() — the caller consumes the value synchronously
  `(row) => // POD-318: every stored row carries its reporting machine, and there i`
- L127 — argument of .filter() — the caller consumes the value synchronously
  `(row) => !row.deletedAt && visibility.mayRead(reader, { class: 'issue', id: row.id })`
- L208 — argument of .filter() — the caller consumes the value synchronously
  `(row) => visibility.mayRead(reader, { class: 'transcript', mac`
- L223 — argument of .find() — the caller consumes the value synchronously
  `(candidate) => candidate.machineId === row.machineId && candidate.resu`

### apps/server/src/modules/messages/handlers/context.ts (4)
- L271 — passed where (ref: string) => string is expected — that caller reads the value, not a promise
  `(ref) => this.deps.issues.resolveRef(ref)`
- L272 — passed where (id: string) => boolean is expected — that caller reads the value, not a promise
  `(id) => this.deps.issues.has(id)`
- L286 — passed where (ref: string) => string is expected — that caller reads the value, not a promise
  `(r) => this.deps.issues.resolveRef(r)`
- L287 — passed where (id: string) => boolean is expected — that caller reads the value, not a promise
  `(id) => this.deps.issues.has(id)`

### apps/server/src/modules/settings/service.ts (4)
- L285 — passed where ((machineId: string & BRAND<"MachineId">) => ModelCatalogSnapshot | null) | undefined is expected — that caller reads the value, not a promise
  `(machineId) => this.store.getModelCatalog(machineId)`
- L286 — passed where ((snapshot: ModelCatalogSnapshot) => void) | undefined is expected — that caller reads the value, not a promise
  `(snapshot) => this.store.setModelCatalog(snapshot)`
- L373 — argument of .filter() — the caller consumes the value synchronously
  `(key) => { const incoming = leaf(next, key) const stored = this.secrets.getOrE`
- L566 — argument of .map() — the caller consumes the value synchronously
  `(row) => secretPresence(row.key, this.secrets.getOrEmpty(row.key), serverKey, row.`

### apps/server/src/modules/sessions/handoff/preflight.ts (4)
- L109 — argument of .map() — the caller consumes the value synchronously
  `(ref) => this.ports.rpc.repoOp('revParseVerify', sourceRepo.path, { ref }, sessi`
- L115 — argument of .map() — the caller consumes the value synchronously
  `(ref) => this.ports.rpc.repoOp('revParseVerify', targetRepo.path, { ref }, input`
- L138 — argument of .map() — the caller consumes the value synchronously
  `(ref) => this.ports.rpc.repoOp('revParseVerify', targetRepo.path, { ref }, inp`
- L144 — argument of .map() — the caller consumes the value synchronously
  `(ref) => this.ports.rpc.repoOp('revParseVerify', sourceRepo.path, { ref }, ses`

### apps/server/src/modules/sessions/workspace.ts (4)
- L189 — argument of .map() — the caller consumes the value synchronously
  `(ref) => rpc.repoOp('revParseVerify', sourceRepoPath, { ref }, sourceMachineId)`
- L194 — argument of .map() — the caller consumes the value synchronously
  `(ref) => rpc.repoOp('revParseVerify', input.targetRepoPath, { ref }, input.targetM`
- L435 — argument of .map() — the caller consumes the value synchronously
  `(ref) => this.ports.rpc.repoOp('revParseVerify', sourceRepo.path, { ref }, source.`
- L441 — argument of .map() — the caller consumes the value synchronously
  `(ref) => this.ports.rpc.repoOp('revParseVerify', fetcherRepo.path, { ref }, caller`

### apps/server/src/modules/automations/registry.ts (4)
- L61 — passed where (service: AutomationsService, input: any, principal: CommandPrincipal) => unknown is expected — that caller reads the value, not a promise
  `(service, input: z.infer<typeof AUTOMATION_CONTRACTS.create.input>, principal) => se`
- L66 — passed where (service: AutomationsService, input: any, principal: CommandPrincipal) => unknown is expected — that caller reads the value, not a promise
  `(service, input: z.infer<typeof AUTOMATION_CONTRACTS.update.input>, principal) => se`
- L71 — passed where (service: AutomationsService, input: any, principal: CommandPrincipal) => unknown is expected — that caller reads the value, not a promise
  `(service, input: z.infer<typeof AUTOMATION_CONTRACTS.setEnabled.input>, principal) => `
- L76 — passed where (service: AutomationsService, input: any, principal: CommandPrincipal) => unknown is expected — that caller reads the value, not a promise
  `(service, input: z.infer<typeof AUTOMATION_CONTRACTS.remove.input>, principal) => se`

### apps/server/src/enrollment-durability.test.ts (4)
- L63 — passed where ((userId: string & BRAND<"UserId">) => boolean) | undefined is expected — that caller reads the value, not a promise
  `(id) => store.users.get(id) !== undefined`
- L248 — passed where ((userId: string & BRAND<"UserId">) => boolean) | undefined is expected — that caller reads the value, not a promise
  `(id) => afterReconcile.store.users.get(id) !== undefined`
- L348 — passed where ((userId: string & BRAND<"UserId">) => boolean) | undefined is expected — that caller reads the value, not a promise
  `(id) => id !== OTHER && store.users.get(id) !== undefined`
- L415 — passed where ((userId: string & BRAND<"UserId">) => boolean) | undefined is expected — that caller reads the value, not a promise
  `(id) => restarted.store.users.get(id) !== undefined`

### apps/server/src/ledger.baseline-fold.test.ts (4)
- L52 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => store.transact(async () => { // A nested ledger.commit: its own transa`
- L87 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => store.transact(async () => { await ledger.commit({ write: ()`
- L168 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => store.transact(async () => { await ledger.commit({ write: ()`
- L191 — passed where (result: unknown) => EntityChangeSpec[] is expected — that caller reads the value, not a promise
  `() => { seenInsideTheSpan = baselineIds(ledger) return [ { entit`

### apps/server/src/machine-identity.test.ts (4)
- L85 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => openTestStore(path, HOST)`
- L106 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => openTestStore(path, HOST)`
- L190 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => openTestStore(path, HOST)`
- L320 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => // @ts-expect-error machineId is REQUIRED (POD-318) — this is the compile-time`

### apps/server/src/transcript-indexer.test.ts (4)
- L75 — passed where ((machineId: string & BRAND<"MachineId">, nativeId: string, lakePath: string) => void) | undefined is expected — that caller reads the value, not a promise
  `(m, n, p) => indexer.onBytes(m, n, p)`
- L76 — passed where ((machineId: string & BRAND<"MachineId">, nativeId: string) => void) | undefined is expected — that caller reads the value, not a promise
  `(m, n) => indexer.onTruncate(m, n)`
- L208 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => indexer.onBytes(asMachineId('m1'), 'nofts', '/nonexistent/lake.jsonl')`
- L213 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => indexer.onTruncate(asMachineId('m1'), 'nofts')`

### apps/server/src/migrations/restore.test.ts (4)
- L85 — passed where () => FeedIdentity is expected — that caller reads the value, not a promise
  `() => registry.current()`
- L332 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => restoreDatabase({ backupPath, dbPath, freeBytes: () => 1 })`
- L344 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => restoreDatabase({ backupPath: `${dbPath}.nope`, dbPath, freeBytes: PLENTY })`
- L347 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => restoreDatabase({ backupPath: dbPath, dbPath, freeBytes: PLENTY })`

### apps/server/src/modules/issues/service/hard-delete-fold.test.ts (4)
- L46 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => store.transact(async () => { await registry.issues.purgeEmptyDraft(doo`
- L91 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => store.transact(async () => { await registry.issues.update(bystander.id`
- L120 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => store.transact(async () => { await registry.issues.purgeEmptyDraft(par`
- L141 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => store.transact(async () => { await registry.issues.purgeEmptyDraft(doo`

### apps/server/src/modules/messages/characterization-support.ts (4)
- L403 — passed where ((input: { ownerUserId: string & BRAND<"UserId">; createdByActor: string; createdByOnBehalfOf: string & BRAND<"UserId">; repoPath: string; title: string; description?: string | undefined; parentId?: (string & BRAND<...>) | undefined; origin: "human" | "agent"; }) => { ...; }) | undefined is expected — that caller reads the value, not a promise
  `(input) => issues.create({ ...input, startNow: false })`
- L464 — passed where (input: { title: string; repoPath?: string | undefined; parentId?: (string & BRAND<"IssueId">) | undefined; }) => { id: string & BRAND<"IssueId">; seq: number; } is expected — that caller reads the value, not a promise
  `(input) => { const wire = issues.create({ repoPath: input.repoPath ?? '/repo`
- L473 — passed where (issueId: string & BRAND<"IssueId">, worktreePath: string) => void is expected — that caller reads the value, not a promise
  `(issueId, worktreePath) => { issues.update(issueId, { worktreePath }) }`
- L476 — passed where (issueId: string & BRAND<"IssueId">) => void is expected — that caller reads the value, not a promise
  `(issueId) => { issues.update(issueId, { archived: true }) }`

### apps/server/src/modules/messages/cutover.test.ts (4)
- L449 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => mailHarness({ ceiling, authorizeAtApply: mailPolicy({ ceiling: other }).author`
- L455 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => mailHarness({ ceiling })`
- L460 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => mailHarness({ ceiling: policy.gateOptions.ceiling, authorizeAt`
- L469 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => mailHarness()`

### apps/server/src/modules/messages/gate-agent.test.ts (4)
- L131 — passed where ((factKey: string, target: string) => void) | undefined is expected — that caller reads the value, not a promise
  `(factKey, target) => { retired.push({ factKey, target }) store.notificationFacts.r`
- L152 — passed where ((e: { ts: string; kind: string; subject: string; payload: unknown; }) => void) | undefined is expected — that caller reads the value, not a promise
  `(e) => store.events.appendEvent(e)`
- L955 — passed where () => void is expected — that caller reads the value, not a promise
  `() => svc.onSessionIdle(target({}))`
- L995 — passed where () => void is expected — that caller reads the value, not a promise
  `() => svc.onSessionIdle(target({}))`

### apps/server/src/modules/issues/service/mail.ts (3)
- L43 — passed where (() => void) | undefined is expected — that caller reads the value, not a promise
  `() => this.store.deps.store.issues.addIssueComment({ id: `cmt_${randomUUID()`
- L133 — passed where () => void is expected — that caller reads the value, not a promise
  `() => { const at = this.store.now() if (unreadIds.length) { `
- L229 — passed where ((fromIssue: string) => string) | undefined is expected — that caller reads the value, not a promise
  `(fromIssue) => { const issue = this.reports().get(fromIssue) return issue `

### apps/server/src/gateway/feed-serving.ts (3)
- L302 — passed where (scope: ReadScope) => void is expected — that caller reads the value, not a promise
  `() => { if (resumeFrom === undefined) { this.serveWorld(peer, principal, rou`
- L657 — passed where (scope: ReadScope) => void is expected — that caller reads the value, not a promise
  `() => { this.serveWorld(peer, principal, routingPrincipal, 'version-change') }`
- L699 — passed where (scope: ReadScope) => void is expected — that caller reads the value, not a promise
  `() => { for (const { principal, deliveries } of pending) { for (const delive`

### apps/server/src/modules/machines/service.ts (3)
- L650 — argument of .find() — the caller consumes the value synchronously
  `(machine) => agentCapabilityRejectionForSelection(machine, agentKind) === undefine`
- L836 — argument of .find() — the caller consumes the value synchronously
  `(id) => this.deps.store.repos .listRepos(id) .some((r) => cwd === r.`
- L940 — argument of .map() — the caller consumes the value synchronously
  `(m) => ({ id: m.id, name: m.name, ownerUserId: this.effectiveOwn`

### apps/server/src/modules/issues/authority-arbitration.ts (3)
- L50 — passed where <T>(op: LedgerCommitOp<T>) => LedgerCommitResult<T> is expected — that caller reads the value, not a promise
  `(op) => this.commit(op)`
- L51 — passed where (specs: EntityChangeSpec[]) => ({ id: string; seq: number; entity: "session"; op: "upsert" | "remove"; value?: { status: "live" | "starting" | "reconnecting" | "hibernated" | "exited"; ... 69 more ...; upstreamStale?: boolean | undefined; } | undefined; } | ... 11 more ... | { ...; })[] is expected — that caller reads the value, not a promise
  `(specs) => this.source.capture(specs)`
- L52 — passed where (entity: "session" | "issue" | "issueProjection" | "issueDep" | "repo" | "shipOrder" | "conversation" | "automation" | "automationRun" | "userLayout" | "userReadPosition" | "issueEvent" | "pendingInteraction", rows: { ...; }[]) => ({ ...; } | ... 11 more ... | { ...; })[] is expected — that caller reads the value, not a promise
  `(entity, rows) => this.source.reconcile(entity, rows)`

### apps/server/src/modules/issues/command-ctx.ts (3)
- L135 — passed where (id: string) => boolean is expected — that caller reads the value, not a promise
  `(id) => tracker.reports.has(id)`
- L137 — passed where (id: string) => string[] is expected — that caller reads the value, not a promise
  `(id) => tracker.hierarchy.ancestorIds(id)`
- L223 — argument of .filter() — the caller consumes the value synchronously
  `(row) => this.mayReadIssue(row.id)`

### apps/server/src/feed-visibility.ts (3)
- L680 — passed where (ref: EntityRef) => { readonly audience: readonly string[]; readonly subjects: readonly EntityRef[]; } | null is expected — that caller reads the value, not a promise
  `(ref) => { if (ref.entity !== 'issue') return null const audience = store.gran`
- L714 — passed where (ref: EntityRef) => unknown is expected — that caller reads the value, not a promise
  `(ref) => durableChangeValueOf(ref)`
- L722 — passed where () => number is expected — that caller reads the value, not a promise
  `() => store.grants.visibilityRevision()`

### apps/server/src/modules/messages/scheduler.ts (3)
- L176 — passed where () => void is expected — that caller reads the value, not a promise
  `() => { this.deliveryTriggerTimer = null try { this.flushDeliveryTrigg`
- L337 — passed where () => void is expected — that caller reads the value, not a promise
  `() => this.runReconcilePage(next)`
- L396 — passed where () => void is expected — that caller reads the value, not a promise
  `() => this.runRetryBackstopPage(next)`

### apps/server/src/modules/sessions/view.ts (3)
- L238 — argument of .map() — the caller consumes the value synchronously
  `(session) => this.wire(session, principal, memo)`
- L312 — passed where (() => void) | PromiseLike<(() => void) | undefined> | undefined is expected — that caller reads the value, not a promise
  `() => { session.refLetter = this.ports.store.issues.allocateSessionLetter(birthI`
- L320 — passed where (() => void) | PromiseLike<(() => void) | undefined> | undefined is expected — that caller reads the value, not a promise
  `() => { session.refDraft = this.ports.store.repos.nextDraftSeq(repoId) }`

### apps/server/src/modules/sessions/session-revival.ts (3)
- L248 — passed where () => HandoffRepo[] is expected — that caller reads the value, not a promise
  `() => this.ports.store.repos.listRepos()`
- L249 — passed where () => HandoffMachine[] is expected — that caller reads the value, not a promise
  `() => this.ports.machines.listMachines()`
- L266 — passed where (event: { ts: string; kind: string; subject: string; payload: unknown; }) => void is expected — that caller reads the value, not a promise
  `(event) => { this.ports.store.events.appendEvent(event) }`

### apps/server/src/store.ts (3)
- L319 — passed where ((sessionId: string & BRAND<"SessionId">) => void) | undefined is expected — that caller reads the value, not a promise
  `(id) => this.observationCheckpoints.purge(id)`
- L322 — passed where (repoPath: string) => string is expected — that caller reads the value, not a promise
  `(repoPath) => this.repos.resolveRepoIdForPath(repoPath)`
- L327 — passed where (repoId: string & BRAND<"RepoId">, repoPath: string) => void is expected — that caller reads the value, not a promise
  `(repoId, repoPath) => this.issues.assignRepoIdToIssuesUnder(repoId, repoPath)`

### apps/server/src/modules/updates/trpc.ts (3)
- L330 — passed where ((operationId: string, patch: Record<string, unknown>) => void) | undefined is expected — that caller reads the value, not a promise
  `(operationId, patch) => { input.operations.engine.recordDetails(operationId, patch) `
- L339 — passed where ((operationId: string, stepId: string, patch: StepProgressPatch) => void) | undefined is expected — that caller reads the value, not a promise
  `(operationId, stepId, patch) => { void input.operations.engine.recordProgress(operat`
- L344 — passed where ((operationId: string, stepId: string) => boolean) | undefined is expected — that caller reads the value, not a promise
  `(operationId, stepId) => input.operations.engine.watching(operationId, stepId)`

### apps/server/src/modules/messaging/service.ts (3)
- L219 — passed where Listener<"superagent.turnEnded"> is expected — that caller reads the value, not a promise
  `(ev) => this.onTurnEnded(ev)`
- L220 — passed where Listener<"notification.telegramRequested"> is expected — that caller reads the value, not a promise
  `(request) => this.sendUserNotice(request)`
- L277 — passed where (msg: InboundChatMessage) => void is expected — that caller reads the value, not a promise
  `(msg) => this.onInbound(msg)`

### apps/server/src/feed-bootstrap-scaling.test.ts (3)
- L403 — passed where () => void is expected — that caller reads the value, not a promise
  `() => { internals(small).ledger.authority.bootstrap(feedPrincipal) }`
- L406 — passed where () => void is expected — that caller reads the value, not a promise
  `() => { internals(large).ledger.authority.bootstrap(feedPrincipal) }`
- L438 — passed where () => void is expected — that caller reads the value, not a promise
  `() => { ledger.capture([ { entity: 'issue', id: 'iss_shared_0', op: 'ups`

### apps/server/src/session-cutover.audit.test.ts (3)
- L182 — passed where (repoPath: string, agentKind: "claude-code" | "codex" | "grok" | "opencode" | "cursor" | "pi" | "shell" | undefined, issueId: (string & BRAND<"IssueId">) | undefined, ownership: { ...; }) => { ...; } is expected — that caller reads the value, not a promise
  `(repoPath, agentKind, issueId, ownership) => modules.issues.createDraftFor(repoPath,`
- L187 — passed where (issueId: string & BRAND<"IssueId">) => boolean is expected — that caller reads the value, not a promise
  `(issueId) => modules.issues.discardUnlaunchedDraft(issueId)`
- L190 — passed where () => SessionTargetRow[] is expected — that caller reads the value, not a promise
  `() => modules.sessions.listSessions()`

### apps/server/src/store.refs.test.ts (3)
- L31 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => s.repos.addRepo('/b/thing', asMachineId('__local__'), undefined, 'lower')`
- L34 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => s.repos.addRepo('/c/thing', asMachineId('__local__'), undefined, 'PDM')`
- L56 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => s.repos.setRepoPrefix(asMachineId('__local__'), '/a/podium', otherPrefix)`

### apps/server/src/modules/approvals/service.test.ts (3)
- L72 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => req(svc, { kind: 'rm-rf' })`
- L73 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => req(svc, { kind: 'set-server' })`
- L107 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => svc.approve(id)`

### apps/server/src/store/shipping-uncovered.test.ts (3)
- L365 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => call({ generation: leaderAttempt.leaseGeneration + 1 })`
- L369 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => call({ failureOrderIds: [] })`
- L370 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => call({ failureOrderIds: [asShipOrderId('order-not-a-member')] })`

### apps/server/src/store/user-layout.test.ts (3)
- L37 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => layout.setMany(ALICE, { dockTab: 'mail', 'not.a.key': 1 }, AT)`
- L57 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => layout.set(ALICE, 'view', 'workspace', AT)`
- L58 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => layout.set(ALICE, 'podium.view', 'workspace', AT)`

### apps/server/src/store/user-preferences.test.ts (3)
- L158 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => settings.userPreferences.set(ALICE, 'gitWorkflow.mergeStyle', 'pr', AT)`
- L161 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => settings.userPreferences.set(ALICE, 'apiKeys.openai', 'sk-x', AT)`
- L166 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => settings.userPreferences.set(ALICE, 'sidebar.repoSort', 'custom', AT)`

### apps/server/src/store/executor/span-side-effects.test.ts (3)
- L74 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => store.transact(async () => { await ledger.commit({ write: () => 'ok', `
- L111 — passed where (changes: ({ id: string; seq: number; entity: "session"; op: "upsert" | "remove"; value?: { status: "live" | "starting" | "reconnecting" | "hibernated" | "exited"; geometry: { rows: number; cols: number; }; ... 68 more ...; upstreamStale?: boolean | undefined; } | undefined; } | ... 11 more ... | { ...; })[]) => void is expected — that caller reads the value, not a promise
  `(changes) => { for (const change of changes) seen.push(`${name}:${change.id}`) `
- L181 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => store.transact(async () => { await store.events.appendEvent({ `

### apps/server/src/store/repos.ts (2)
- L102 — passed where () => void is expected — that caller reads the value, not a promise
  `() => this.invalidateRegistry()`
- L219 — passed where (prefix: string) => boolean is expected — that caller reads the value, not a promise
  `(p) => this.isPrefixTaken(p)`

### apps/server/src/modules/issues/service/attention.ts (2)
- L287 — argument of .filter() — the caller consumes the value synchronously
  `(row) => row.id !== anchor.id && row.repoId === anchor.repoId && `
- L491 — passed where () => { updated: Promise<boolean>; } is expected — that caller reads the value, not a promise
  `() => ({ updated: this.store.deps.store.events.setSubscriptionEnabled(id, enabled) })`

### apps/server/src/modules/issues/service/hierarchy.ts (2)
- L61 — passed where (() => void) | undefined is expected — that caller reads the value, not a promise
  `() => this.store.deps.store.issues.addIssueDep(fromId, toId, type)`
- L80 — passed where (() => void) | undefined is expected — that caller reads the value, not a promise
  `() => this.store.deps.store.issues.removeIssueDep(fromId, toId, type)`

### apps/server/src/modules/issues/service/assistant.ts (2)
- L54 — passed where () => void is expected — that caller reads the value, not a promise
  `() => { this.assistantTimers.delete(row.id) void this.refreshAssistant(row`
- L102 — passed where (provider: string) => string | undefined is expected — that caller reads the value, not a promise
  `(provider) => this.store.d.store.secrets.apiKeyFor(provider)`

### apps/server/src/modules/machines/rpc.ts (2)
- L619 — argument of .map() — the caller consumes the value synchronously
  `(machineId) => this.request( QUOTA_HISTORY, 120_000, `
- L1356 — passed where (requestId: string) => { type: "shippingJobRequest"; repoId: string & BRAND<"RepoId">; repoPath: string; approvedHeadSha: string; targetBranch: string; destination: string; ... 16 more ...; repair?: { ...; } | undefined; } | ... 74 more ... | { ...; } is expected — that caller reads the value, not a promise
  `(requestId) => ({ type: 'transcriptRead', requestId, s`

### apps/server/src/modules/automations/scheduler.ts (2)
- L32 — passed where () => void is expected — that caller reads the value, not a promise
  `() => { this.tick() this.timer = setInterval(() => this.tick(), AUTOMATIONS_IN`
- L34 — passed where () => void is expected — that caller reads the value, not a promise
  `() => this.tick()`

### apps/server/src/modules/events/retention.ts (2)
- L42 — passed where () => void is expected — that caller reads the value, not a promise
  `() => { this.schedulePrune() this.timer = setInterval(() => this.schedulePrune`
- L44 — passed where () => void is expected — that caller reads the value, not a promise
  `() => this.schedulePrune()`

### apps/server/src/modules/quota-history/service.ts (2)
- L113 — passed where () => void is expected — that caller reads the value, not a promise
  `() => { void this.sampleNow() this.timer = setInterval(() => void this.sampleN`
- L115 — passed where () => void is expected — that caller reads the value, not a promise
  `() => void this.sampleNow()`

### apps/server/src/modules/issues/auto-archive.ts (2)
- L28 — passed where () => void is expected — that caller reads the value, not a promise
  `() => { this.sweep() this.timer = setInterval(() => this.sweep(), AUTO_ARCHIVE`
- L30 — passed where () => void is expected — that caller reads the value, not a promise
  `() => this.sweep()`

### apps/server/src/modules/issues/git-watch.ts (2)
- L36 — passed where () => void is expected — that caller reads the value, not a promise
  `() => { void this.sweep() this.timer = setInterval(() => void this.sweep(), GI`
- L38 — passed where () => void is expected — that caller reads the value, not a promise
  `() => void this.sweep()`

### apps/server/src/modules/messages/handlers/inbox-consume.ts (2)
- L44 — argument of .map() — the caller consumes the value synchronously
  `(m) => access.wire(m)`
- L52 — argument of .map() — the caller consumes the value synchronously
  `(m) => access.wire(m)`

### apps/server/src/modules/workflows/service.ts (2)
- L338 — argument of .map() — the caller consumes the value synchronously
  `(row) => this.toRun(row)`
- L455 — argument of .map() — the caller consumes the value synchronously
  `(step) => ({ ...step, profile: step.executionProfileId ? this.de`

### apps/server/src/modules/operations/engine.ts (2)
- L1009 — passed where () => void is expected — that caller reads the value, not a promise
  `() => { void this.enqueue(operationId, async () => { await this.expi`
- L1235 — passed where () => void is expected — that caller reads the value, not a promise
  `() => { // A deadline is the other drive site nobody awaits (POD-2151). `

### apps/server/src/modules/superagent/tools.ts (2)
- L130 — argument of .map() — the caller consumes the value synchronously
  `(s) => { // Reverse of issue_show's session list (issue #72): session cwd → `
- L777 — argument of .map() — the caller consumes the value synchronously
  `(r) => { // Issues read by display seq (what users and issue_* tools speak). `

### apps/server/src/modules/sessions/runtime-event-gate.ts (2)
- L181 — passed where () => void is expected — that caller reads the value, not a promise
  `() => { this.ports.events.saveRuntimeEventCheckpoint({ ...curren`
- L224 — passed where (draft: SessionDurableState) => void is expected — that caller reads the value, not a promise
  `(draft) => { if (event.t === 'state') { stateProjection = this.ports.sta`

### apps/server/src/modules/sessions/daemon-lifecycle.ts (2)
- L597 — passed where (() => void) | undefined is expected — that caller reads the value, not a promise
  `() => { outcome = this.store.observationCheckpoints.rebindExact({ `
- L753 — passed where (() => void) | undefined is expected — that caller reads the value, not a promise
  `() => { this.store.observationCheckpoints.save(outcome.checkpoint) if `

### apps/server/src/modules/sessions/activity-history.ts (2)
- L72 — passed where Listener<"session.stateChanged"> is expected — that caller reads the value, not a promise
  `({ sessionId, prev, next }) => { this.record(sessionId, next.phase, prev?.phase) `
- L77 — passed where Listener<"session.exited"> is expected — that caller reads the value, not a promise
  `({ sessionId }) => { this.record(sessionId, 'ended', undefined) }`

### apps/server/src/modules/sessions/concurrency-history.ts (2)
- L122 — passed where Listener<"session.stateChanged"> is expected — that caller reads the value, not a promise
  `() => this.capture()`
- L123 — passed where Listener<"session.exited"> is expected — that caller reads the value, not a promise
  `() => this.capture()`

### apps/server/src/modules/sessions/session-teardown.ts (2)
- L338 — passed where (() => void) | undefined is expected — that caller reads the value, not a promise
  `() => this.ports.store.observationCheckpoints.cancelTerminalCandidate(input.sessionId)`
- L571 — passed where (() => void) | undefined is expected — that caller reads the value, not a promise
  `() => { const currentLease = this.ports.store.observationCheckpoints.get(ses`

### apps/server/src/modules/sessions/session-wiring.ts (2)
- L184 — passed where (sessionId: string & BRAND<"SessionId">, input: { commits?: string[] | undefined; touched?: string[] | undefined; }) => void is expected — that caller reads the value, not a promise
  `(sessionId, input) => { if (runtimeEventGate?.ready(sessionId) === true) return `
- L562 — passed where (principal: ClientPrincipal) => { id: string & BRAND<"MachineId">; name: string; hostname: string; lastSeenAt: string; online: boolean; podiumManaged?: boolean | undefined; ... 14 more ...; targetUnavailableReason?: string | ... 1 more ... | undefined; }[] is expected — that caller reads the value, not a promise
  `(principal) => projectMachinesForPrincipal( { machines: bag.machines }, `

### apps/server/src/modules/messages/mailbox.ts (2)
- L424 — argument of .flatMap() — the caller consumes the value synchronously
  `(p) => this.deps.messages.listMessagesFor(p, { limit: opts?.limit ?? 50 })`
- L446 — argument of .map() — the caller consumes the value synchronously
  `(m) => { // Per-READER receipt first [POD-1379]: this session has now been shown the`

### apps/server/src/modules/accounts/registry.ts (2)
- L59 — passed where AccountHandler<{ kind: "api-key" | "oauth"; provider: "openrouter" | "anthropic" | "openai"; credential: string; }, unknown> is expected — that caller reads the value, not a promise
  `(state, input) => { // A Claude setup-token is its own account, distinct from an Ant`
- L79 — passed where AccountHandler<{ id: string & BRAND<"AccountId">; }, unknown> is expected — that caller reads the value, not a promise
  `(state, input) => { state.accounts.remove(input.id) return { ok: true as const`

### apps/server/src/modules/accounts/queries.ts (2)
- L42 — passed where (provider: string) => string | undefined is expected — that caller reads the value, not a promise
  `(provider) => state.settings.apiKeyFor(provider)`
- L45 — argument of .map() — the caller consumes the value synchronously
  `(account) => { if (account.source !== 'native' || !account.harness) return account `

### apps/server/src/modules/approvals/registry.ts (2)
- L51 — passed where ApprovalHandler<{ id: string; }, unknown> is expected — that caller reads the value, not a promise
  `(svc, input) => svc.approve(input.id)`
- L58 — passed where ApprovalHandler<{ id: string; }, unknown> is expected — that caller reads the value, not a promise
  `(svc, input) => svc.deny(input.id)`

### apps/server/src/modules/models/registry.ts (2)
- L54 — passed where ModelHandler<objectOutputType<{ machineId: ZodOptional<ZodBranded<ZodString, "MachineId">>; }, ZodTypeAny, "passthrough"> | undefined, unknown> is expected — that caller reads the value, not a promise
  `(state, input) => state.settings.refreshModelCatalog( input?.machineId ?? st`
- L82 — passed where () => string & BRAND<"MachineId"> is expected — that caller reads the value, not a promise
  `() => modules.machines.defaultMachine()`

### apps/server/src/conversations.ledger.test.ts (2)
- L119 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => ledger.commit({ // BOTH store methods open their own transaction() int`
- L271 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => registry.modules.memory.setConversationMeta( { kind: 'user', id: FIRST`

### apps/server/src/modules/maintenance/service.ts (2)
- L118 — passed where () => { status: "busy"; retryAt: any; fencingToken?: undefined; expiresAt?: undefined; messageWaitTtlMs?: undefined; autoArchiveReadWindowMs?: undefined; eventRetentionMaxAgeDays?: undefined; ... 5 more ...; worktreeGcAfterDays?: undefined; } | { ...; } is expected — that caller reads the value, not a promise
  `() => { const nowMs = this.now() const now = new Date(nowMs).toISOString() `
- L461 — passed where () => { status: "applied"; jobKind: "message-expiry" | "event-log-prune" | "change-log-prune" | "maintenance-commands-prune" | "issue-auto-archive" | "session-auto-archive" | "automation-fire" | "steward-poll" | "connect-scan" | "worktree-gc"; runKey: string; deleted?: number | undefined; } | { ...; } | { ...; } is expected — that caller reads the value, not a promise
  `() => { const nowMs = this.now() const lease = this.store.maintenance.getLease`

### apps/server/src/event-log.test.ts (2)
- L228 — argument of .map() — the caller consumes the value synchronously
  `() => store.events.appendEvent({ ts: daysAgo(0), kind: 'k', subject: 's' })`
- L313 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => svc.create({ repoPath: '/r', title: 'A', startNow: false })`

### apps/server/src/issues.boot-quarantine.test.ts (2)
- L67 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => rebooted.init()`
- L89 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => rebooted.init()`

### apps/server/src/machine-capability-guard.test.ts (2)
- L99 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => registry.modules.machines.requireRepoHostStructure(coordinator)`
- L151 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => registry.modules.issues.create({ title: 'homed wrongly', repoP`

### apps/server/src/relay.issue-session-delete.test.ts (2)
- L104 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => registry.modules.issueSessionLifecycle.deleteIssue(issue.id)`
- L137 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => registry.modules.issueSessionLifecycle.restoreIssue(issue.id)`

### apps/server/src/relay.test.ts (2)
- L335 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => reg.modules.sessions.createSession({ agentKind: 'claude-code', `
- L7104 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => { registry = SessionRegistry.create(undefined, undefined, { instanceId: 'd`

### apps/server/src/sessions.ledger.test.ts (2)
- L87 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => ledger.commit({ write: async () => await store.sessions.upse`
- L630 — passed where () => void is expected — that caller reads the value, not a promise
  `() => registry.modules.machines.renameMachine( registry.modules.machines`

### apps/server/src/store.issues.test.ts (2)
- L146 — argument of .forEach() — the caller consumes the value synchronously
  `(id, i) => { store.issues.upsertIssue(baseRow({ id: asIssueId(id), seq: 100 + i })) `
- L265 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => store.issues.setIssueUserState(asUserId(''), asIssueId('iss_read'), { readAt: `

### apps/server/src/store.repo-id.test.ts (2)
- L186 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => openTestStore(file)`
- L206 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => openTestStore(file)`

### apps/server/src/superagent-headless.test.ts (2)
- L386 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => h.sa.clear(FIRST_ADMIN_USER_ID, asThreadId('global'))`
- L1013 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => h.sa.interruptTurn({ ownerUserId: FIRST_ADMIN_USER_ID, threadId: asThreadId('b`

### apps/server/src/gateway/feed-test-plumbing.ts (2)
- L100 — passed where () => number | null is expected — that caller reads the value, not a promise
  `() => store.sync.minChangeSeq()`
- L102 — passed where (() => number) | undefined is expected — that caller reads the value, not a promise
  `() => store.grants.visibilityRevision()`

### apps/server/src/modules/funnel.test.ts (2)
- L126 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => funnel.run({ authorize: () => { throw new Error('forbidden')`
- L317 — passed where Listener<"oplog.appended"> is expected — that caller reads the value, not a promise
  `() => { if (reentered) return reentered = true ledger.commit({ w`

### apps/server/src/modules/memory/conversation-list-fold.test.ts (2)
- L55 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => store.transact(async () => { await memory.onDiscovery(machine, [conver`
- L87 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => store.transact(async () => { await memory.setConversationMeta(SYSTEM_R`

### apps/server/src/modules/sessions/oracle-decomposition.test.ts (2)
- L522 — passed where () => unknown is expected — that caller reads the value, not a promise
  `() => o.reg.modules.sessions.createSession({ agentKind: 'claude-code', `
- L534 — passed where () => unknown is expected — that caller reads the value, not a promise
  `() => o.reg.modules.sessions.createSession({ agentKind: 'claude-code', `

### apps/server/src/modules/sessions/repository.durable-baseline-fold.test.ts (2)
- L81 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => f.store.transact(() => { // A NESTED persist: the ledger's span degrad`
- L106 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => f.store.transact(() => { f.repo.persist(f.session) throw new E`

### apps/server/src/modules/shipping/lease-projection-fold.test.ts (2)
- L41 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => store.transact(() => { const pinned = leases.pin(['order-1']) `
- L81 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => store.transact(() => { leases.delete('order-1') expect(leases.`

### apps/server/src/store/maintenance.golden.test.ts (2)
- L156 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => store.maintenance.pruneCommandsBatch('2026-09-02T00:00:00.000Z', bad)`
- L178 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => store.maintenance.getCommand('message-expiry', 'run-1')`

### apps/server/src/store/executor/executor.ts (1)
- L782 — argument of .map() — the caller consumes the value synchronously
  `(runner) => runner.effectsSettled()`

### packages/sync/src/outbox/outbox.ts (1)
- L588 — argument of .map() — the caller consumes the value synchronously
  `(bucket) => this.drainPartition(bucket)`

### apps/server/src/migrations/snapshot-verifier.ts (1)
- L539 — passed where () => void is expected — that caller reads the value, not a promise
  `() => { void this.verify(candidate.path) .catch((error: unknown) => { `

### apps/server/src/store/issues.ts (1)
- L1191 — argument of .map() — the caller consumes the value synchronously
  `(r) => this.mapIssueMessage(r)`

### apps/server/src/store/observation-checkpoints.ts (1)
- L193 — argument of .map() — the caller consumes the value synchronously
  `(row) => this.mapRow(row)`

### apps/server/src/modules/issues/service/workflow.ts (1)
- L1700 — argument of .map() — the caller consumes the value synchronously
  `(id) => this.refreshGitState(id).catch(() => {})`

### apps/server/src/modules/memory/visibility.ts (1)
- L257 — argument of .some() — the caller consumes the value synchronously
  `(row) => { if (!row.resumeValue && !row.conversationId) return false const row`

### apps/server/src/modules/automations/service.ts (1)
- L461 — passed where (result: { runId: string & BRAND<"AutomationRunId">; automation: AutomationRow; }) => EntityChangeSpec[] is expected — that caller reads the value, not a promise
  `(result) => [ { entity: 'automationRun', id: result.runI`

### apps/server/src/modules/quota-history/backfill.ts (1)
- L106 — passed where () => void is expected — that caller reads the value, not a promise
  `() => void this.runOnce()`

### apps/server/src/modules/issue-session-lifecycle.ts (1)
- L160 — passed where () => void is expected — that caller reads the value, not a promise
  `() => { void this.sweepClosedIssues('periodic').catch((error) => { log.warn(`

### apps/server/src/modules/issues/dispatcher.ts (1)
- L71 — passed where () => number | undefined is expected — that caller reads the value, not a promise
  `() => this.deps.issues.reports.get(issue.id)?.revision`

### apps/server/src/modules/messages/handlers/ledger.ts (1)
- L42 — argument of .map() — the caller consumes the value synchronously
  `(m) => access.wire(m)`

### apps/server/src/repo-discovery.ts (1)
- L219 — passed where () => void is expected — that caller reads the value, not a promise
  `() => { this.scan(machineId, { deep: false }).catch((err) => { this.deps.log`

### apps/server/src/store/users.ts (1)
- L144 — argument of .flatMap() — the caller consumes the value synchronously
  `(row) => { const account = this.get(row.id) return account ? [account] : [] `

### apps/server/src/modules/operations/trpc.ts (1)
- L25 — passed where ProcedureResolver<Context, object, {}, { group?: string | undefined; } | undefined, UnsetMarker, unknown> is expected — that caller reads the value, not a promise
  `({ ctx, input }) => { const row = operationsModule(ctx).engine.active(input?.group`

### apps/server/src/steward.ts (1)
- L447 — passed where () => void is expected — that caller reads the value, not a promise
  `() => void this.tick()`

### apps/server/src/modules/sessions/repository.ts (1)
- L519 — passed where (result: void) => EntityChangeSpec[] is expected — that caller reads the value, not a promise
  `() => [ { entity: 'session', id: session.sessionId, `

### apps/server/src/modules/sessions/session-kill.ts (1)
- L141 — passed where ((result: void, changes: ({ id: string; seq: number; entity: "session"; op: "upsert" | "remove"; value?: { status: "live" | "starting" | "reconnecting" | "hibernated" | "exited"; geometry: { ...; }; ... 68 more ...; upstreamStale?: boolean | undefined; } | undefined; } | ... 11 more ... | { ...; })[]) => void) | und... is expected — that caller reads the value, not a promise
  `(_result, changes) => { this.removeSessionRuntime(input.sessionId, { retiredAt: de`

### apps/server/src/modules/sessions/lifecycle.ts (1)
- L701 — passed where (sessionId: string & BRAND<"SessionId">) => (string & BRAND<"SessionId">) | undefined is expected — that caller reads the value, not a promise
  `(sessionId) => spawnedByParentSessionId( // POD-1646: one field, one vis`

### apps/server/src/modules/messages/render.ts (1)
- L232 — argument of .map() — the caller consumes the value synchronously
  `(m) => this.fromLabel(m)`

### apps/server/src/store/superagent.ts (1)
- L163 — argument of .map() — the caller consumes the value synchronously
  `(r) => this.mapSuperagentThread(r)`

### apps/server/src/modules/accounts/native-login.ts (1)
- L35 — passed where Listener<"machine.metadataChanged"> is expected — that caller reads the value, not a promise
  `({ machineId, inventory }) => { if (inventory) this.onInventory(machineId) }`

### apps/server/src/modules/conversations/registry.ts (1)
- L35 — passed where ConversationHandler<{ id: string & BRAND<"ConversationId">; name?: string | undefined; summary?: string | undefined; }, unknown> is expected — that caller reads the value, not a promise
  `(svc, input) => svc.setConversationMeta(input)`

### apps/server/src/modules/files/registry.ts (1)
- L67 — passed where FileHandler<{ path: string; sessionId: string & BRAND<"SessionId">; content: string; baseHash?: string | undefined; } | { path: string; content: string; root: string; machineId?: (string & BRAND<...>) | undefined; baseHash?: string | undefined; }, unknown> is expected — that caller reads the value, not a promise
  `(state, input) => { // The union's session-addressed arm resolves its root from the `

### apps/server/src/modules/fleet/queries.ts (1)
- L32 — passed where (service: FamilyState, input: { path: string; }) => { repoPath: Promise<string | undefined>; } is expected — that caller reads the value, not a promise
  `(s, input) => ({ repoPath: s.repos.inferFromPath(input.path) ?? null, })`

### apps/server/src/modules/updates/dev-bundle.ts (1)
- L2298 — argument of .flatMap() — the caller consumes the value synchronously
  `(artifact) => [ fs.remove(artifact.path), fs.remove(artifa`

### apps/server/src/modules/interactions/registry.ts (1)
- L57 — passed where InteractionHandler<{ id: string; text?: string | undefined; answer?: objectOutputType<{ kind: ZodString; }, ZodTypeAny, "passthrough"> | undefined; }, unknown> is expected — that caller reads the value, not a promise
  `(svc, input) => svc.answer({ id: input.id, ...(input.text !== undefi`

### apps/server/src/modules/layout/authz.ts (1)
- L61 — passed where (sessionId: string & BRAND<"SessionId">) => (string & BRAND<"SessionId">) | undefined is expected — that caller reads the value, not a promise
  `(sessionId) => // POD-1646: the narrow read. Authorization runs on essentially every`

### apps/server/src/modules/misc-queries.ts (1)
- L238 — passed where (service: FamilyState, input: objectOutputType<{}, ZodTypeAny, "passthrough"> | undefined) => { devMode: boolean; channel: "stable" | "edge"; flags: FeatureStateWire[]; } is expected — that caller reads the value, not a promise
  `(s) => getFeatureStates(s.modules.settings.getSettings(), loadConfig())`

### apps/server/src/modules/read-position/authz.ts (1)
- L66 — passed where (sessionId: string & BRAND<"SessionId">) => (string & BRAND<"SessionId">) | undefined is expected — that caller reads the value, not a promise
  `(sessionId) => // POD-1646: the narrow read. Authorization runs on essentially every`

### apps/server/src/modules/settings/authz.ts (1)
- L162 — passed where (sessionId: string & BRAND<"SessionId">) => (string & BRAND<"SessionId">) | undefined is expected — that caller reads the value, not a promise
  `(sessionId) => // POD-1646: the narrow read. Authorization runs on essentially every`

### apps/server/src/modules/fleet/trpc.ts (1)
- L112 — passed where ProcedureResolver<Context, object, {}, void | { id: string; name: string; } | { id: string; channel: "stable" | "edge" | "dev" | null; } | { id: string; grantee: string; verb: "see" | "use" | "manage"; } | ... 15 more ... | undefined, UnsetMarker, unknown> is expected — that caller reads the value, not a promise
  `({ ctx, input }) => { // THE AUTHORIZATION GATE, DERIVED (POD-1079). It runs for every`

### apps/server/src/characterization.test.ts (1)
- L361 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => reg.issues.update(w.id, { stage: 'in_progress', closedReason: 'wontfix' })`

### apps/server/src/gateway/daemon-socket.ts (1)
- L168 — passed where (raw: string | Buffer<ArrayBufferLike>) => void is expected — that caller reads the value, not a promise
  `(raw) => { if (failed) return if (typeof raw !== 'string') { if (principal =`

### apps/server/src/modules/updates/dev-web-build.ts (1)
- L310 — passed where () => void is expected — that caller reads the value, not a promise
  `() => { const start = async (): Promise<void> => { let headSha: string `

### apps/server/src/modules/updates/target-refresh.ts (1)
- L124 — passed where () => void is expected — that caller reads the value, not a promise
  `() => { void tick() }`

### apps/server/src/relay.machines.test.ts (1)
- L494 — passed where DaemonControlPeer is expected — that caller reads the value, not a promise
  `(msg) => { source.push(msg) if (msg.type === 'repoOpRequest') { const exists`

### apps/server/src/relay.outbox.test.ts (1)
- L157 — passed where () => boolean is expected — that caller reads the value, not a promise
  `() => !queuedRows(reg, sessionId).some((row) => row.text === text)`

### apps/server/src/sessions.refs.test.ts (1)
- L96 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => reg.modules.sessions.createSession({ agentKind: 'shell', cwd: '/r/podium' })`

### apps/server/src/steward.single-flight.test.ts (1)
- L81 — passed where () => void is expected — that caller reads the value, not a promise
  `() => { if (inner) return inner = h.steward.tick() }`

### apps/server/src/steward.test.ts (1)
- L795 — argument of .flatMap() — the caller consumes the value synchronously
  `(w) => issues.comments(w.id)`

### apps/server/src/store-issues-frame-cache.test.ts (1)
- L177 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => store.transact(async () => { await store.issues.upsertIssue(issue('iss`

### apps/server/src/superagent-concierge.test.ts (1)
- L296 — passed where (threadId?: (string & BRAND<"ThreadId">) | undefined) => { name: string; description: string; inputSchema: unknown; }[] is expected — that caller reads the value, not a promise
  `(threadId) => h.sa.mcpToolSpecs(threadId)`

### apps/server/src/terminal-hibernation-proof.test.ts (1)
- L422 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => registry.modules.sessions.hibernateSession({ sessionId, requireTerminalProof: `

### apps/server/src/composition/reactions.runtime.test.ts (1)
- L28 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => SessionRegistry.create(undefined, undefined, { instanceId: 'defaul`

### apps/server/src/gateway/feed-serving.test.ts (1)
- L393 — argument of .forEach() — the caller consumes the value synchronously
  `(peer, index) => { // Interleaved, which is what a storm is: a write lands between a`

### apps/server/src/gateway/reattach-storm.integration.test.ts (1)
- L248 — argument of .map() — the caller consumes the value synchronously
  `(client) => client.nextMatching( (message) => message.type === 'pres`

### apps/server/src/modules/approvals/service.single-flight.test.ts (1)
- L83 — passed where () => void is expected — that caller reads the value, not a promise
  `() => { if (reentered) return reentered = true h.svc.sweepStalledExecuti`

### apps/server/src/modules/issue-events/feed.test.ts (1)
- L151 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => publisher.publish(1, event())`

### apps/server/src/modules/messages/authz.test.ts (1)
- L710 — argument of .map() — the caller consumes the value synchronously
  `(fromKind) => h.svc.send( fromKind === 'system' ? { kind: 'system', name`

### apps/server/src/modules/messages/characterization.delivery.test.ts (1)
- L1001 — passed where (() => void) | null is expected — that caller reads the value, not a promise
  `() => { const row = h.svc.inbox([{ kind: 'session', id: 's1' }]).at(-1)! h.svc`

### apps/server/src/modules/messages/scheduler.single-flight.test.ts (1)
- L75 — passed where () => void is expected — that caller reads the value, not a promise
  `() => { if (reentered) return reentered = true h.scheduler.sweep() }`

### apps/server/src/modules/operations/engine.test.ts (1)
- L1509 — passed where ((row: OperationRow) => void) | undefined is expected — that caller reads the value, not a promise
  `(row) => { // What an observer reads must already be what the database holds. `

### apps/server/src/modules/sessions/model-validation-wiring.test.ts (1)
- L63 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => reg.modules.sessions.createSession({ agentKind: 'codex', cwd: '/tmp/`

### apps/server/src/modules/sessions/oracle-ask-upload.test.ts (1)
- L165 — passed where () => boolean is expected — that caller reads the value, not a promise
  `() => { const rows = o.store.messages.listLedger({ sessionId: target.sessionId }) `

### apps/server/src/modules/sessions/oracle-handoff.test.ts (1)
- L189 — passed where DaemonControlPeer is expected — that caller reads the value, not a promise
  `(msg) => { source.push(msg) timeline.push({ machine: 'm1', type: msg.type, msg }) `

### apps/server/src/modules/sessions/oracle-session-state.test.ts (1)
- L467 — passed where () => boolean is expected — that caller reads the value, not a promise
  `() => o.store.sessions.loadDrafts()[sessionId] === 'still typing'`

### apps/server/src/modules/sessions/rename-shadow.test.ts (1)
- L281 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => expect({ row: observe(target.sessions, target.created.sessionId) }).toEqual({ `

### apps/server/src/modules/sessions/repository.volatile-slice.test.ts (1)
- L125 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => repo.drainVolatileCaptureSlice()`

### apps/server/src/modules/sessions/session-start.test.ts (1)
- L401 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => reg.modules.sessions.createSession({ agentKind: 'shell', cwd: `

### apps/server/src/modules/sessions/spawn-name.test.ts (1)
- L51 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => reg.modules.sessions.createSession({ agentKind: 'shell', cwd: `

### apps/server/src/modules/settings/wiring.test.ts (1)
- L214 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => store.settingsAudit.append({ command: 'settings.updateInstance', `

### apps/server/src/modules/workflows/multi-user.test.ts (1)
- L577 — passed where () => unknown is expected — that caller reads the value, not a promise
  `() => h.service.executionProfileForLaunch({ profileId: profile.id, `

### apps/server/src/modules/workflows/service.test.ts (1)
- L438 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => service.prepareStart({ sessionId: asSessionId('s2'), cwd: '/re`

### apps/server/src/store/locks.golden.test.ts (1)
- L198 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => store.locks.deleteLock(repo, 'build')`

### apps/server/src/store/observation-checkpoints.test.ts (1)
- L51 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => store.observationCheckpoints.save(checkpoint)`

### apps/server/src/store/quota-history.test.ts (1)
- L48 — argument of .forEach() — the caller consumes the value synchronously
  `(t, i) => { store.quotaHistory.record( sample({ resetsAtMs: Date.parse(`2026`

### apps/server/src/store/repos-read-cost.test.ts (1)
- L82 — argument of .map() — the caller consumes the value synchronously
  `(p) => repos.resolveRepoIdForPath(p)`

### apps/server/src/store/runtime-events.test.ts (1)
- L937 — passed where () => void is expected — that caller reads the value, not a promise
  `() => { records = [{ id: 1, sessionId, event }] lateReplay = g`

### apps/server/src/store/session-oom-death.test.ts (1)
- L80 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => sessions.upsertSession(row('sess-oom', { stopReason: 'oom', oomKilledAt: DIED_`

### apps/server/src/store/user-read-position.test.ts (1)
- L88 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => cursors.advance(ALICE, 'notAStream', { lastEventId: 1, seenAt: null }, AT)`

### apps/server/src/store/executor/executor.test.ts (1)
- L517 — argument of .map() — the caller consumes the value synchronously
  `(i) => scheduler.run('read', async () => { order.push(`read${i}:start`) `

### apps/server/src/store/executor/synchronous-span.test.ts (1)
- L381 — argument of expect() — awaiting would turn .toThrow into a rejection assertion
  `() => store.transact(() => { postCommit().effect(() => void ran.push('ef`

### apps/server/src/store/spike/turso-append/run-lease.ts (1)
- L122 — passed where () => void is expected — that caller reads the value, not a promise
  `() => { void take(Date.now()).catch(() => {}) }`

## illegal-await-context (19)

### apps/server/src/relay.ts (8)
- L676 — no await possible here (parameter default, accessor or constructor)
  `machines.ensureHostMachine(hostname())`
- L897 — no await possible here (parameter default, accessor or constructor)
  `this.store.events.onAppend((id, event) => issueEventFeed?.publish(id, event))`
- L1436 — no await possible here (parameter default, accessor or constructor)
  `sessionsSvc.loadFromStore()`
- L1603 — no await possible here (parameter default, accessor or constructor)
  `sessionsSvc.runtimeGateway.replayBoardProjection()`
- L2990 — no await possible here (parameter default, accessor or constructor)
  `issues.boot(systemPrincipal('boot-reconcile'))`
- L2991 — no await possible here (parameter default, accessor or constructor)
  `issueSessionLifecycle.startClosedIssueSweep()`
- L2993 — no await possible here (parameter default, accessor or constructor)
  `shipping
      .reconcile()`
- L2999 — no await possible here (parameter default, accessor or constructor)
  `messagesSvc.reconcileQueued()`

### apps/server/src/modules/shipping/service.ts (3)
- L3154 — no await possible here (parameter default, accessor or constructor)
  `this.deps.repository.latestStepForEffect(
      attempt.id,
      this.effectKeyFor(attemp`
- L3156 — no await possible here (parameter default, accessor or constructor)
  `this.effectKeyFor(attempt, operation)`
- L3703 — no await possible here (parameter default, accessor or constructor)
  `this.deps.repository.listOrders()`

### packages/sync/src/conformance/authority.ts (2)
- L420 — no await possible here (parameter default, accessor or constructor)
  `this.identity.current()`
- L424 — no await possible here (parameter default, accessor or constructor)
  `this.identity.current()`

### apps/server/src/relay.test.ts (2)
- L5306 — no await possible here (parameter default, accessor or constructor)
  `openTestStore(':memory:', TEST_MACHINE)`
- L5478 — no await possible here (parameter default, accessor or constructor)
  `openTestStore(':memory:', TEST_MACHINE)`

### packages/sync/src/authority/authority.ts (1)
- L217 — no await possible here (parameter default, accessor or constructor)
  `this.baseline.seed(deps.store)`

### apps/server/src/modules/machines/service.ts (1)
- L275 — no await possible here (parameter default, accessor or constructor)
  `this.reconcileOwnersFromLedger()`

### apps/server/src/modules/sessions/launch-config.ts (1)
- L107 — no await possible here (parameter default, accessor or constructor)
  `this.ports.store.settings.getSettingsFor(this.ports.settingsViewer())`

### apps/server/src/relay.issue-session-delete.test.ts (1)
- L6 — no await possible here (parameter default, accessor or constructor)
  `openTestStore(':memory:')`
