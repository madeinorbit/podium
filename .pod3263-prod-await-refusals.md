# Await pass refusals — 300 sites

Generated by `bun scripts/awaitify.ts --pass=awaits
--keep-sync=scripts/awaitify-keep-sync.txt --report=<path>`. Line numbers are
pinned to the commit it was generated at and go stale as tests move; the
CATEGORIES do not. Regenerate rather than hand-edit.

## assertion-must-change-at-the-flip (158)

### apps/server/src/relay.ts (50)
- L568 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.sessions.getSession(asSessionId(candidate))`
- L571 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.sessions.getSession(asSessionId(candidate))`
- L661 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.users.get(userId)`
- L779 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.events.appendEvent( { ts: new Date(record.at).toISOString`
- L826 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.users.list()`
- L949 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.sync.minChangeSeq()`
- L983 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.repos.listRepoPaths()`
- L1172 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.users.roleOf(userId)`
- L1280 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.users.get(ownerUserId)`
- L1296 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.repos.listRepoPaths(machineId)`
- L1328 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.settings.getSettings()`
- L1372 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.automations.lastSpawnedSessions()`
- L1373 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.automations.list()`
- L1403 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.settings.getSettingsFor(ownerUserId)`
- L1405 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.secrets.getOrEmpty('notifications.telegramBotToken')`
- L1407 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.telegramBindings.listForUser(ownerUserId)`
- L1409 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.events.appendEvent(e)`
- L1469 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.settings.getSettingsFor(FIRST_ADMIN_USER_ID)`
- L1676 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.transact(fn)`
- L1679 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.repos.resolveRepoIdForPath(repoPath)`
- L1696 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.events.appendEvent(e)`
- L1737 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.transact(fn)`
- L1829 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.repos.resolveRepoIdForPath(path)`
- L1854 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.workflows.ownerOf(entity.kind, entity.id)`
- L1856 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.grants .listForResource(entity.kind, entity.id)`
- L1877 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.users.roleOf(userId)`
- L1888 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.sync.getAppliedMutation(asMutationId(key))`
- L1890 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.sync.recordAppliedMutation( asMutationId(key), 'wor`
- L1980 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.events.appendEvent(e)`
- L1994 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.notificationFacts.retire(factKey, target, new Date(this.now()).toISOString())`
- L2058 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.users.roleOf(ownerUserId)`
- L2082 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.repos.prefixForPath(row.repoPath)`
- L2135 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.settings.getSettingsFor(FIRST_ADMIN_USER_ID)`
- L2166 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.events.appendEvent({ ts: new Date().toISOString(), kind`
- L2202 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.repos.listRepoPaths()`
- L2203 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.repos.listRepoPaths()`
- L2241 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.users .list()`
- L2248 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.repos.listRepoPaths(machineId)`
- L2249 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.issues.getIssue(id)`
- L2278 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.settings.getSettings()`
- L2397 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.users.roleOf(humanId)`
- L2425 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.users.roleOf(userId)`
- L2474 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.shipping.rootIntegrationReceipt(rootIssueId, approvedHeadSha)`
- L2609 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.events.appendEvent({ ts: new Date(this.now()).toISOString(), `
- L2613 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.issues.getIssue(issueId)`
- L2779 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.events.runtimeEventCheckpoint(sessionId)`
- L2781 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.events.hasCausalTurnFailure(sessionId, checkpoint.turnEpoch)`
- L2960 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.repos.listRepos()`
- L3044 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.events.appendEvent({ ts: new Date().toISOString(), `
- L3061 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.settings.getSettings()`

### apps/server/src/modules/lock/service.ts (13)
- L341 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.deps.locks.getLock(repoId, input.name)`
- L354 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.deps.locks.upsertLock(row)`
- L360 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.deps.locks .listWaiters(repoId, input.name)`
- L383 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.deps.locks.enqueueWaiter({ repoId, name: input.name, `
- L411 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.deps.locks.getLock(repoId, input.name)`
- L440 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.deps.locks.getLock(repoId, input.name)`
- L445 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.deps.locks .listWaiters(repoId, input.name)`
- L449 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.deps.locks.removeWaiterBySession(repoId, input.name, key)`
- L510 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.deps.locks.getLock(repoId, input.name)`
- L519 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.deps.locks.removeWaiterBySession(repoId, input.name, this.sessionKey(caller))`
- L550 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.deps.locks.listWaitsBySession(sessionId)`
- L551 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.deps.locks.removeWaiter(w.id)`
- L553 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.deps.locks.listLocksHeldBySession(sessionId)`

### apps/server/src/server.ts (10)
- L722 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `store.repos.listRepos()`
- L753 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `store.telegramBindings.listForUser(userId)`
- L758 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `store.secrets.getOrEmpty('notifications.telegramBotToken')`
- L767 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `store.superagent.getSuperagentThread(threadId)`
- L828 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `store.machines .listMachines()`
- L834 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `store.machines.listMachines()`
- L1079 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `store.machines.getMachineByToken(hostMachineId, token)`
- L1086 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `store.settings.getSettings()`
- L1436 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `store.users.roleOf(principal.user)`
- L1502 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `store.users.get(ownerUserId)`

### apps/server/src/modules/shipping/service.ts (9)
- L715 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.deps.repository.openHoldForOrder(member.id)`
- L1157 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.deps.repository.getOrder(covered.id)`
- L1412 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.deps.repository.openHoldForOrder(member.id)`
- L1586 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.deps.repository.openHoldForOrder(member.id)`
- L1985 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.deps.repository.completeCoveredOrder(receipt, covering.id, effectEnvelopeKey)`
- L2146 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.deps.repository.raiseHold(hold)`
- L2254 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.deps.repository.openHoldForOrder(member.id)`
- L2715 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.deps.repository.openHoldForOrder(member.id)`
- L3064 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.deps.repository.openHoldForOrder(member.id)`

### apps/server/src/store/shipping.ts (7)
- L1020 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.getOrder(id)`
- L1053 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.db .select({ branch: issues.branch, machineId`
- L1184 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.latestAttemptForOrder(order.id)`
- L1185 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.claimAttempt({ orderId: order.id, expectedState: 'queued', `
- L1650 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.getOrder(member.orderId)`
- L1654 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.getAttempt(member.attemptId)`
- L1716 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.db .select({ repoPath: issues.repoPath, machineId: issues.machineId }) `

### apps/server/src/modules/messages/characterization-support.ts (6)
- L342 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `store.issues.addIssueMessage(row)`
- L344 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `store.issues.markIssueMessagesRead(FIRST_ADMIN_USER_ID, issueId, ids, now())`
- L356 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `store.transact(fn)`
- L404 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `store.events.appendEvent(e)`
- L418 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `store.notificationFacts.retire(factKey, target, now())`
- L491 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `store.events .listEventsSince(0, kinds ? { kinds, limit: 5000 } : { limit: 5000 })`

### apps/server/src/modules/maintenance/service.ts (5)
- L121 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.maintenance.getLease(LEASE_NAME)`
- L131 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.maintenance.putLease({ name: LEASE_NAME, generationId: request.`
- L463 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.maintenance.getLease(LEASE_NAME)`
- L470 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.maintenance.getCommand(command.jobKind, command.runKey)`
- L478 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.maintenance.recordCommand( { status: 'applied', jobKind: command.jobKin`

### apps/server/src/modules/sessions/session-state/service.ts (5)
- L340 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.ports.store.sessions.markSessionRead( principal.userId, sessionId, `
- L351 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.ports.store.sessions.markSessionUnread(principal.userId, sessionId)`
- L363 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.ports.store.sessions.setSnooze(principal.userId, sessionId, until)`
- L370 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.ports.store.sessions.clearSnooze(principal.userId, sessionId)`
- L378 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.ports.store.sessions.clearAllSnoozes(sessionId)`

### apps/server/src/modules/issues/service/crud.ts (5)
- L1421 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.deps.store.issues.upsertIssue(row, { expectedRevision: current.revisi`
- L1535 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.deps.store.issues.upsertIssue(row, { expectedRevision: current.revisi`
- L1553 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.deps.store.issues.setIssueLabels(asIssueId(id), labels)`
- L1575 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.deps.store.grants.upsert({ resourceKind: 'issue', resourceId: r`
- L1596 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.deps.store.grants.remove('issue', row.id, grantee, verb)`

### apps/server/src/modules/sessions/daemon-lifecycle.ts (4)
- L598 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.observationCheckpoints.rebindExact({ sessionId: session.sessionId`
- L754 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.observationCheckpoints.save(outcome.checkpoint)`
- L757 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.observationCheckpoints.recordTerminalCandidate( candidateFacts,`
- L762 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.observationCheckpoints.cancelTerminalCandidate(session.sessionId)`

### apps/server/src/modules/settings/service.ts (4)
- L285 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.getModelCatalog(machineId)`
- L286 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.setModelCatalog(snapshot)`
- L375 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.secrets.getOrEmpty(key)`
- L567 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.secrets.getOrEmpty(row.key)`

### apps/server/src/store.ts (3)
- L320 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.observationCheckpoints.purge(id)`
- L323 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.repos.resolveRepoIdForPath(repoPath)`
- L327 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.issues.assignRepoIdToIssuesUnder(repoId, repoPath)`

### apps/server/src/modules/messages/mailbox.ts (3)
- L425 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.deps.messages.listMessagesFor(p, { limit: opts?.limit ?? 50 })`
- L450 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.deps.messages.recordRead(m.id, opts.consume, at)`
- L452 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.deps.messages.markRead(m.id, opts.consume ?? null, at)`

### apps/server/src/modules/sessions/session-teardown.ts (3)
- L338 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.ports.store.observationCheckpoints.cancelTerminalCandidate(input.sessionId)`
- L572 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.ports.store.observationCheckpoints.get(sessionId)`
- L579 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.ports.store.observationCheckpoints.consumeTerminalCandidate( curren`

### apps/server/src/modules/sessions/runtime-event-gate.ts (3)
- L182 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.ports.events.saveRuntimeEventCheckpoint({ ...current, obs`
- L233 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.ports.events.appendEvent( { ts: event.at, kind: RUN`
- L242 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.ports.events.saveRuntimeEventCheckpoint(next)`

### apps/server/src/gateway/feed-test-plumbing.ts (2)
- L100 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `store.sync.minChangeSeq()`
- L102 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `store.grants.visibilityRevision()`

### apps/server/src/modules/accounts/registry.ts (2)
- L64 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `state.accounts.upsert({ id: asAccountId(id), provider: input.provider, `
- L80 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `state.accounts.remove(input.id)`

### apps/server/src/modules/sessions/session-revival.ts (2)
- L248 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.ports.store.repos.listRepos()`
- L267 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.ports.store.events.appendEvent(event)`

### apps/server/src/modules/sessions/view.ts (2)
- L313 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.ports.store.issues.allocateSessionLetter(birthIssueId)`
- L321 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.ports.store.repos.nextDraftSeq(repoId)`

### apps/server/src/modules/superagent/service.ts (2)
- L901 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.superagent .listPendingTurns()`
- L1123 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.superagent .listPendingTurns()`

### apps/server/src/feed-visibility.ts (2)
- L682 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `store.grants.visibilityAudienceFor('issue', ref.entityId)`
- L722 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `store.grants.visibilityRevision()`

### apps/server/src/modules/machines/service.ts (2)
- L652 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.deps.store.repos .listRepos(machine.id)`
- L837 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.deps.store.repos .listRepos(id)`

### apps/server/src/modules/issues/service/hierarchy.ts (2)
- L61 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.deps.store.issues.addIssueDep(fromId, toId, type)`
- L80 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.deps.store.issues.removeIssueDep(fromId, toId, type)`

### apps/server/src/store/repos.ts (2)
- L102 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.invalidateRegistry()`
- L219 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.isPrefixTaken(p)`

### apps/server/src/store/superagent.ts (1)
- L163 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.mapSuperagentThread(r)`

### apps/server/src/store/users.ts (1)
- L145 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.get(row.id)`

### apps/server/src/modules/workflows/service.ts (1)
- L458 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.deps.store.getProfile(step.executionProfileId)`

### apps/server/src/modules/automations/service.ts (1)
- L466 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.deps.store.getRun(result.runId)`

### apps/server/src/modules/issues/service/assistant.ts (1)
- L102 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.d.store.secrets.apiKeyFor(provider)`

### apps/server/src/modules/issues/service/mail.ts (1)
- L44 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.deps.store.issues.addIssueComment({ id: `cmt_${randomUUID()}`, `

### apps/server/src/modules/issues/service/attention.ts (1)
- L296 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.store.deps.store.issues .listIssueDeps(row.id)`

### apps/server/src/modules/issues/service/core.ts (1)
- L1262 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.deps.store.events.appendEvent( { ts: this.now(), `

### apps/server/src/store/observation-checkpoints.ts (1)
- L193 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.mapRow(row)`

### apps/server/src/store/issues.ts (1)
- L1191 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.mapIssueMessage(r)`

## would-change-what-the-caller-reads (138)

### apps/server/src/relay.ts (47)
- L566 — passed where (sessionId: string & BRAND<"SessionId">) => (string & BRAND<"SessionId">) | undefined is expected — that caller reads the value, not a promise
  `(candidate) => spawnedByParentSessionId( this.store.sessions.getSess`
- L570 — passed where ((sessionId: string & BRAND<"SessionId">) => (string & BRAND<"UserId">) | undefined) | undefined is expected — that caller reads the value, not a promise
  `(candidate) => this.store.sessions.getSession(asSessionId(candidate))?.ownerUser`
- L661 — passed where ((userId: string & BRAND<"UserId">) => boolean) | undefined is expected — that caller reads the value, not a promise
  `(userId) => this.store.users.get(userId) !== undefined`
- L778 — passed where RecordGrant | undefined is expected — that caller reads the value, not a promise
  `(record) => { this.store.events.appendEvent( { ts: new Date(`
- L825 — passed where () => (string & BRAND<"UserId">)[] is expected — that caller reads the value, not a promise
  `() => { const rows = this.store.users.list().map((row) => asUserId(row.id)) `
- L949 — passed where () => number | null is expected — that caller reads the value, not a promise
  `() => this.store.sync.minChangeSeq()`
- L983 — passed where () => string[] is expected — that caller reads the value, not a promise
  `() => this.store.repos.listRepoPaths()`
- L1161 — passed where (message: MessageRow) => CommandPrincipal | undefined is expected — that caller reads the value, not a promise
  `(message) => { if (message.fromKind === 'system') return systemPrincipal(message.f`
- L1279 — passed where (ownerUserId: string & BRAND<"UserId">) => (machineId: string & BRAND<"MachineId">) => string | undefined is expected — that caller reads the value, not a promise
  `(ownerUserId) => { const user = this.store.users.get(ownerUserId) if (user`
- L1296 — passed where (machineId: string & BRAND<"MachineId">) => string is expected — that caller reads the value, not a promise
  `(machineId) => this.store.repos.listRepoPaths(machineId)[0] ?? '/'`
- L1328 — passed where () => { issues: { assistantEnabled: boolean; }; steward: { enabled: boolean; }; notifications: { web: boolean; ntfyTopic: string; telegramChatId: string; telegramBotToken: string; }; roles: { superagent: { ...; }; shipwright: { ...; }; coding: { ...; }; background: { ...; }; }; ... 8 more ...; transcripts: { ...; }; } is expected — that caller reads the value, not a promise
  `() => this.store.settings.getSettings()`
- L1371 — passed where (sessionId: string & BRAND<"SessionId">, now: number) => boolean is expected — that caller reads the value, not a promise
  `(sessionId, now) => { const lastSpawned = this.store.automations.lastSpawnedSess`
- L1402 — passed where (ownerUserId?: (string & BRAND<"UserId">) | undefined) => { issues: { assistantEnabled: boolean; }; steward: { enabled: boolean; }; notifications: { web: boolean; ntfyTopic: string; telegramChatId: string; telegramBotToken: string; }; ... 9 more ...; transcripts: { ...; }; } is expected — that caller reads the value, not a promise
  `(ownerUserId = FIRST_ADMIN_USER_ID) => this.store.settings.getSettingsFor(ownerU`
- L1405 — passed where () => string is expected — that caller reads the value, not a promise
  `() => this.store.secrets.getOrEmpty('notifications.telegramBotToken')`
- L1406 — passed where ((ownerUserId: string & BRAND<"UserId">) => boolean) | undefined is expected — that caller reads the value, not a promise
  `(ownerUserId) => this.store.telegramBindings.listForUser(ownerUserId).length ===`
- L1409 — passed where (e: { ts: string; kind: string; subject: string; repoPath?: string | null | undefined; payload?: unknown; }) => void is expected — that caller reads the value, not a promise
  `(e) => this.store.events.appendEvent(e)`
- L1469 — passed where () => { issues: { assistantEnabled: boolean; }; steward: { enabled: boolean; }; notifications: { web: boolean; ntfyTopic: string; telegramChatId: string; telegramBotToken: string; }; roles: { superagent: { ...; }; shipwright: { ...; }; coding: { ...; }; background: { ...; }; }; ... 8 more ...; transcripts: { ...; }; } is expected — that caller reads the value, not a promise
  `() => this.store.settings.getSettingsFor(FIRST_ADMIN_USER_ID)`
- L1676 — passed where <T>(fn: () => T) => T is expected — that caller reads the value, not a promise
  `(fn) => this.store.transact(fn)`
- L1679 — passed where (repoPath: string) => string & BRAND<"RepoId"> is expected — that caller reads the value, not a promise
  `(repoPath) => this.store.repos.resolveRepoIdForPath(repoPath)`
- L1696 — passed where (e: { ts: string; kind: string; subject: string; payload?: unknown; }) => void is expected — that caller reads the value, not a promise
  `(e) => this.store.events.appendEvent(e)`
- L1737 — passed where (<T>(fn: () => T) => T) | undefined is expected — that caller reads the value, not a promise
  `(fn) => this.store.transact(fn)`
- L1829 — passed where (path: string) => string | null is expected — that caller reads the value, not a promise
  `(path) => this.store.repos.resolveRepoIdForPath(path)`
- L1854 — passed where (entity: WorkflowEntityRef) => string | null is expected — that caller reads the value, not a promise
  `(entity) => this.store.workflows.ownerOf(entity.kind, entity.id)`
- L1855 — passed where (user: string, entity: WorkflowEntityRef, verb: WorkflowVerb) => boolean is expected — that caller reads the value, not a promise
  `(user, entity, verb) => this.store.grants .listForResource(entit`
- L1860 — passed where ((principal: WorkflowPrincipal) => WorkflowMachineAccess) | undefined is expected — that caller reads the value, not a promise
  `(workflowPrincipal) => { let principal: CommandPrincipal | undefined i`
- L1888 — passed where (key: string) => string | undefined is expected — that caller reads the value, not a promise
  `(key) => this.store.sync.getAppliedMutation(asMutationId(key))`
- L1889 — passed where (key: string, result: string) => void is expected — that caller reads the value, not a promise
  `(key, result) => this.store.sync.recordAppliedMutation( asMutati`
- L1980 — passed where ((e: { ts: string; kind: string; subject: string; payload: unknown; }) => void) | undefined is expected — that caller reads the value, not a promise
  `(e) => this.store.events.appendEvent(e)`
- L1993 — passed where ((factKey: string, target: string) => void) | undefined is expected — that caller reads the value, not a promise
  `(factKey, target) => { this.store.notificationFacts.retire(factKey, target, new `
- L2057 — passed where (ownerUserId: string & BRAND<"UserId">) => CommandPrincipal | undefined is expected — that caller reads the value, not a promise
  `(ownerUserId) => { const role = this.store.users.roleOf(ownerUserId) retur`
- L2079 — passed where (issueId: string & BRAND<"IssueId">) => { seq: number; title: string; displayRef?: string | undefined; } | null is expected — that caller reads the value, not a promise
  `(issueId) => { const row = issues.getMeta(issueId) if (!row) return null `
- L2091 — passed where ((op: { kind: "update"; } | { kind: "channel"; target: "stable" | "edge" | "dev"; } | { kind: "stop"; } | { kind: "set-server"; target: string; } | { kind: "workflow-publish"; revisionId: string; } | { ...; } | { ...; }, sessionId: string & BRAND<...>) => string | null) | undefined is expected — that caller reads the value, not a promise
  `(op, sessionId) => { const caller = workflowCallerForCapability(sessionsSvc.capabi`
- L2164 — passed where (kind: string, issueId: (string & BRAND<"IssueId">) | null, payload: Record<string, unknown>) => void is expected — that caller reads the value, not a promise
  `(kind, issueId, payload) => { try { this.store.events.appendEvent({ `
- L2202 — passed where () => string[] is expected — that caller reads the value, not a promise
  `() => this.store.repos.listRepoPaths()`
- L2203 — passed where (path: string) => string | undefined is expected — that caller reads the value, not a promise
  `(path) => inferRepoFromRoots(this.store.repos.listRepoPaths(), path)`
- L2237 — passed where (machineId: string & BRAND<"MachineId">) => (string & BRAND<"UserId">)[] is expected — that caller reads the value, not a promise
  `(machineId) => { const owner = machines.ownershipRows().find((row) => row.id ===`
- L2247 — passed where (machineId: string & BRAND<"MachineId">) => string | undefined is expected — that caller reads the value, not a promise
  `(machineId) => this.store.repos.listRepoPaths(machineId)[0] ?? this.store.repos.`
- L2249 — passed where (id: string & BRAND<"IssueId">) => boolean is expected — that caller reads the value, not a promise
  `(id) => this.store.issues.getIssue(id) !== null`
- L2278 — passed where () => string is expected — that caller reads the value, not a promise
  `() => this.store.settings.getSettings().gitWorkflow.defaultParentBranch || 'main'`
- L2391 — passed where (input: { principal: CommandPrincipal; action: "cancel" | "enqueue" | "resolve-hold" | "read-receipt"; issue: { type: "task" | "automation" | "bug" | "feature" | "chore" | ... 4 more ... | "milestone"; ... 74 more ...; comments?: { ...; }[] | undefined; }; overrideScope: boolean; }) => void is expected — that caller reads the value, not a promise
  `({ principal, action, issue, overrideScope }) => { if (principal.kind === 'syste`
- L2421 — passed where (input: { order: { state: "queued" | "cancelled" | "preflight" | "composing" | "validating" | "repairing" | "landing" | "publishing" | "verifying" | "shipped" | "held"; id: string & BRAND<...>; ... 20 more ...; holdCode?: string | undefined; }; issue: { ...; }; machineId: string & BRAND<...>; effect: "preflight" | .... is expected — that caller reads the value, not a promise
  `({ order, issue, machineId, effect }) => { const attribution = order.requestedBy`
- L2473 — passed where (rootIssueId: string & BRAND<"IssueId">, approvedHeadSha: string) => { rootIssueId: string & BRAND<"IssueId">; approvedHeadSha: string; descendants: { issueId: string & BRAND<...>; approvedHeadSha: string; }[]; } | null is expected — that caller reads the value, not a promise
  `(rootIssueId, approvedHeadSha) => this.store.shipping.rootIntegrationReceipt(roo`
- L2607 — passed where ((kind: string, issueId: string & BRAND<"IssueId">, payload: Record<string, unknown>) => void) | undefined is expected — that caller reads the value, not a promise
  `(kind, issueId, payload) => { try { this.store.events.appendEvent({ `
- L2778 — passed where ((sessionId: string & BRAND<"SessionId">) => boolean) | undefined is expected — that caller reads the value, not a promise
  `(sessionId) => { const checkpoint = this.store.events.runtimeEventCheckpoint(sessi`
- L2960 — passed where () => FleetRepoRow[] is expected — that caller reads the value, not a promise
  `() => this.store.repos.listRepos()`
- L3038 — passed where ((receipt: { at: string; outcome: "accepted"; turnEpoch: number; deliveredAs: "interrupt" | "when-ready" | "queue" | "steer"; provenBy: "hook" | "sdk-callback" | "protocol-ack" | "transcript-echo"; } | { ...; } | { ...; } | { ...; }) => void) | undefined is expected — that caller reads the value, not a promise
  `(receipt) => { // LEDGER-VISIBLE, NEVER A RESEND — the uniform `unverified` po`
- L3061 — passed where () => { issues: { assistantEnabled: boolean; }; steward: { enabled: boolean; }; notifications: { web: boolean; ntfyTopic: string; telegramChatId: string; telegramBotToken: string; }; roles: { superagent: { ...; }; shipwright: { ...; }; coding: { ...; }; background: { ...; }; }; ... 8 more ...; transcripts: { ...; }; } is expected — that caller reads the value, not a promise
  `() => this.store.settings.getSettings()`

### apps/server/src/server.ts (10)
- L722 — passed where () => RepoRow[] is expected — that caller reads the value, not a promise
  `() => store.repos.listRepos()`
- L752 — passed where (userId: string & BRAND<"UserId">) => string | undefined is expected — that caller reads the value, not a promise
  `(userId) => { const routes = store.telegramBindings.listForUser(userId) re`
- L758 — passed where () => string is expected — that caller reads the value, not a promise
  `() => store.secrets.getOrEmpty('notifications.telegramBotToken')`
- L767 — passed where (threadId: string & BRAND<"ThreadId">) => { ownerUserId: string & BRAND<"UserId">; podiumSessionId?: (string & BRAND<"SessionId">) | null | undefined; originSessionId?: (string & BRAND<...>) | ... 1 more ... | undefined; } | undefined is expected — that caller reads the value, not a promise
  `(threadId) => store.superagent.getSuperagentThread(threadId)`
- L827 — passed where () => boolean is expected — that caller reads the value, not a promise
  `() => store.machines .listMachines() .some((machine) => isRemoteUpda`
- L834 — passed where (() => readonly string[]) | undefined is expected — that caller reads the value, not a promise
  `() => fleetHeadlessPlatforms(store.machines.listMachines())`
- L1079 — passed where (token: string) => boolean is expected — that caller reads the value, not a promise
  `(token) => store.machines.getMachineByToken(hostMachineId, token)`
- L1086 — passed where (() => { mode: "auto" | "off" | "propose"; afterDays: number; }) | undefined is expected — that caller reads the value, not a promise
  `() => store.settings.getSettings().worktreeGc`
- L1418 — passed where ((request: Request) => { userId: string & BRAND<"UserId">; userRole: "member" | "admin"; credentialId?: string | undefined; } | undefined) | undefined is expected — that caller reads the value, not a promise
  `(request) => { if ( request.headers.has('authorization') && `
- L1500 — passed where (threadId: string & BRAND<"ThreadId">) => IssueTrpc is expected — that caller reads the value, not a promise
  `(threadId) => { const ownerUserId = superagent.threadOwner(threadId) const acc`

### apps/server/src/modules/shipping/service.ts (9)
- L714 — passed where () => { kind: string; payload: { orderId: any; holdId: any; generation: any; reasonCode: any; actions: any; readonly reason?: undefined; }; } | { kind: string; payload: { orderId: any; reason: string; holdId?: undefined; generation?: undefined; reasonCode?: undefined; actions?: undefined; }; } is expected — that caller reads the value, not a promise
  `() => { const hold = this.deps.repository.openHoldForOrder(member.id) `
- L1156 — argument of .flatMap() — the caller consumes the value synchronously
  `(covered) => { const live = this.deps.repository.getOrder(covered.id) `
- L1410 — passed where () => { kind: string; payload: { orderId: any; holdId: any; generation: any; reasonCode: any; actions: any; cancelledPeer?: undefined; }; } | { kind: string; payload: { orderId: any; cancelledPeer: string & BRAND<...>; holdId?: undefined; generation?: undefined; reasonCode?: undefined; actions?: undefined; }; } is expected — that caller reads the value, not a promise
  `() => { const sibling = this.requiredOrder(member.id) `
- L1585 — passed where () => { kind: string; payload: { orderId: any; holdId: any; generation: any; reasonCode: any; actions: any; cancelledPeer?: undefined; }; } | { kind: string; payload: { orderId: any; cancelledPeer: string & BRAND<...>; holdId?: undefined; generation?: undefined; reasonCode?: undefined; actions?: undefined; }; } is expected — that caller reads the value, not a promise
  `() => { const hold = this.deps.repository.openHoldForOrder(member.id) `
- L1983 — passed where () => void is expected — that caller reads the value, not a promise
  `() => { for (const { receipt } of settlements) { this.deps.repository.co`
- L2145 — passed where () => void is expected — that caller reads the value, not a promise
  `() => { for (const { hold } of pending) this.deps.repository.raiseHold(hold) `
- L2253 — passed where () => { kind: string; payload: { orderId: string & BRAND<"ShipOrderId">; holdId: any; generation: any; reasonCode: any; actions: any; readonly reason?: undefined; }; } | { kind: string; payload: { ...; }; } is expected — that caller reads the value, not a promise
  `() => { const hold = this.deps.repository.openHoldForOrder(member.id) `
- L2714 — passed where () => { kind: string; payload: { orderId: any; holdId: any; generation: any; reasonCode: any; actions: any; isolatedFrom?: undefined; }; } | { kind: string; payload: { orderId: any; isolatedFrom: string & BRAND<...>; holdId?: undefined; generation?: undefined; reasonCode?: undefined; actions?: undefined; }; } is expected — that caller reads the value, not a promise
  `() => { const hold = this.deps.repository.openHoldForOrder(member.id) `
- L3063 — passed where () => { kind: string; payload: { orderId: any; holdId: any; generation: any; reasonCode: any; actions: any; heldPeer?: undefined; }; } | { kind: string; payload: { orderId: any; heldPeer: string & BRAND<"ShipOrderId">; holdId?: undefined; generation?: undefined; reasonCode?: undefined; actions?: undefined; }; } is expected — that caller reads the value, not a promise
  `() => { const liveHold = this.deps.repository.openHoldForOrder(member.id) `

### apps/server/src/store/shipping.ts (6)
- L1019 — argument of .map() — the caller consumes the value synchronously
  `(id) => { const order = this.getOrder(id) if (!order || order.state !== 'q`
- L1052 — argument of .map() — the caller consumes the value synchronously
  `(order) => { const row = this.db .select({ branch: iss`
- L1183 — argument of .map() — the caller consumes the value synchronously
  `(order) => { const previous = this.latestAttemptForOrder(order.id) return `
- L1650 — argument of .map() — the caller consumes the value synchronously
  `(member) => this.getOrder(member.orderId)`
- L1654 — argument of .every() — the caller consumes the value synchronously
  `(member) => !this.getAttempt(member.attemptId)?.finishedAt`
- L1715 — argument of .map() — the caller consumes the value synchronously
  `(order) => { const issue = this.db .select({ repoPath: issues.repoPath, `

### apps/server/src/modules/messages/characterization-support.ts (6)
- L342 — passed where ((row: IssueMessageRow) => void) | undefined is expected — that caller reads the value, not a promise
  `(row) => store.issues.addIssueMessage(row)`
- L343 — passed where ((issueId: string & BRAND<"IssueId">, ids: string[]) => void) | undefined is expected — that caller reads the value, not a promise
  `(issueId, ids) => store.issues.markIssueMessagesRead(FIRST_ADMIN_USER_ID, issueId, i`
- L356 — passed where (<T>(fn: () => T) => T) | undefined is expected — that caller reads the value, not a promise
  `(fn) => store.transact(fn)`
- L404 — passed where ((e: { ts: string; kind: string; subject: string; payload: unknown; }) => void) | undefined is expected — that caller reads the value, not a promise
  `(e) => store.events.appendEvent(e)`
- L417 — passed where ((factKey: string, target: string) => void) | undefined is expected — that caller reads the value, not a promise
  `(factKey, target) => store.notificationFacts.retire(factKey, target, now())`
- L490 — passed where (kinds?: string[] | undefined) => { kind: string; subject: string; payload: unknown; }[] is expected — that caller reads the value, not a promise
  `(kinds) => store.events .listEventsSince(0, kinds ? { kinds, limit: 5000 } :`

### apps/server/src/modules/issues/service/crud.ts (5)
- L1417 — passed where () => void is expected — that caller reads the value, not a promise
  `() => { // The prepare/apply pair has always been draft-then-install; POD-3259 `
- L1531 — passed where () => void is expected — that caller reads the value, not a promise
  `() => { // The prepare/apply pair has always been draft-then-install; POD-3259 `
- L1552 — passed where (() => void) | undefined is expected — that caller reads the value, not a promise
  `() => this.store.deps.store.issues.setIssueLabels(asIssueId(id), labels)`
- L1574 — passed where (() => void) | undefined is expected — that caller reads the value, not a promise
  `() => this.store.deps.store.grants.upsert({ resourceKind: 'issue', r`
- L1595 — passed where (() => void) | undefined is expected — that caller reads the value, not a promise
  `() => this.store.deps.store.grants.remove('issue', row.id, grantee, verb)`

### apps/server/src/modules/sessions/session-state/service.ts (5)
- L339 — passed where () => void is expected — that caller reads the value, not a promise
  `() => this.ports.store.sessions.markSessionRead( principal.userId, s`
- L350 — passed where () => void is expected — that caller reads the value, not a promise
  `() => this.ports.store.sessions.markSessionUnread(principal.userId, sessionId)`
- L362 — passed where () => void is expected — that caller reads the value, not a promise
  `() => this.ports.store.sessions.setSnooze(principal.userId, sessionId, until)`
- L369 — passed where () => void is expected — that caller reads the value, not a promise
  `() => this.ports.store.sessions.clearSnooze(principal.userId, sessionId)`
- L378 — passed where (() => void) | undefined is expected — that caller reads the value, not a promise
  `() => this.ports.store.sessions.clearAllSnoozes(sessionId)`

### apps/server/src/modules/lock/service.ts (5)
- L339 — passed where () => { granted: true; alreadyHeld: boolean; lock: Promise<LockWire>; position?: undefined; } | { granted: false; position: any; lock: Promise<LockWire>; alreadyHeld?: undefined; } is expected — that caller reads the value, not a promise
  `() => { this.sweepExpired(repoId) const existing = this.deps.locks.get`
- L409 — passed where () => { released: true; next: LockHolderWire | null; } is expected — that caller reads the value, not a promise
  `() => { this.sweepExpired(repoId) const existing = this.deps.locks.get`
- L438 — passed where () => { cancelled: true; } is expected — that caller reads the value, not a promise
  `() => { this.sweepExpired(repoId) const existing = this.deps.locks.get`
- L508 — passed where () => { lock: Promise<LockWire>; previousHolder: LockHolderWire | null; } is expected — that caller reads the value, not a promise
  `() => { this.sweepExpired(repoId) const existing = this.deps.locks.get`
- L549 — passed where () => void is expected — that caller reads the value, not a promise
  `() => { for (const w of this.deps.locks.listWaitsBySession(sessionId)) { `

### apps/server/src/modules/settings/service.ts (4)
- L285 — passed where ((machineId: string & BRAND<"MachineId">) => ModelCatalogSnapshot | null) | undefined is expected — that caller reads the value, not a promise
  `(machineId) => this.store.getModelCatalog(machineId)`
- L286 — passed where ((snapshot: ModelCatalogSnapshot) => void) | undefined is expected — that caller reads the value, not a promise
  `(snapshot) => this.store.setModelCatalog(snapshot)`
- L373 — argument of .filter() — the caller consumes the value synchronously
  `(key) => { const incoming = leaf(next, key) const stored = this.secrets.getOrE`
- L566 — argument of .map() — the caller consumes the value synchronously
  `(row) => secretPresence(row.key, this.secrets.getOrEmpty(row.key), serverKey, row.`

### apps/server/src/store.ts (3)
- L319 — passed where ((sessionId: string & BRAND<"SessionId">) => void) | undefined is expected — that caller reads the value, not a promise
  `(id) => this.observationCheckpoints.purge(id)`
- L322 — passed where (repoPath: string) => string is expected — that caller reads the value, not a promise
  `(repoPath) => this.repos.resolveRepoIdForPath(repoPath)`
- L327 — passed where (repoId: string & BRAND<"RepoId">, repoPath: string) => void is expected — that caller reads the value, not a promise
  `(repoId, repoPath) => this.issues.assignRepoIdToIssuesUnder(repoId, repoPath)`

### apps/server/src/store/repos.ts (2)
- L102 — passed where () => void is expected — that caller reads the value, not a promise
  `() => this.invalidateRegistry()`
- L219 — passed where (prefix: string) => boolean is expected — that caller reads the value, not a promise
  `(p) => this.isPrefixTaken(p)`

### apps/server/src/modules/issues/service/hierarchy.ts (2)
- L61 — passed where (() => void) | undefined is expected — that caller reads the value, not a promise
  `() => this.store.deps.store.issues.addIssueDep(fromId, toId, type)`
- L80 — passed where (() => void) | undefined is expected — that caller reads the value, not a promise
  `() => this.store.deps.store.issues.removeIssueDep(fromId, toId, type)`

### apps/server/src/modules/machines/service.ts (2)
- L650 — argument of .find() — the caller consumes the value synchronously
  `(machine) => agentCapabilityRejectionForSelection(machine, agentKind) === undefine`
- L836 — argument of .find() — the caller consumes the value synchronously
  `(id) => this.deps.store.repos .listRepos(id) .some((r) => cwd === r.`

### apps/server/src/feed-visibility.ts (2)
- L680 — passed where (ref: EntityRef) => { readonly audience: readonly string[]; readonly subjects: readonly EntityRef[]; } | null is expected — that caller reads the value, not a promise
  `(ref) => { if (ref.entity !== 'issue') return null const audience = store.gran`
- L722 — passed where () => number is expected — that caller reads the value, not a promise
  `() => store.grants.visibilityRevision()`

### apps/server/src/modules/superagent/service.ts (2)
- L900 — passed where () => void is expected — that caller reads the value, not a promise
  `() => { const current = this.store.superagent .listPendingTurns() `
- L1121 — passed where () => void is expected — that caller reads the value, not a promise
  `() => { this.interruptFallbacks.delete(pending.turnId) const still = this.`

### apps/server/src/modules/sessions/runtime-event-gate.ts (2)
- L181 — passed where () => void is expected — that caller reads the value, not a promise
  `() => { this.ports.events.saveRuntimeEventCheckpoint({ ...curren`
- L224 — passed where (draft: SessionDurableState) => void is expected — that caller reads the value, not a promise
  `(draft) => { if (event.t === 'state') { stateProjection = this.ports.sta`

### apps/server/src/modules/sessions/daemon-lifecycle.ts (2)
- L597 — passed where (() => void) | undefined is expected — that caller reads the value, not a promise
  `() => { outcome = this.store.observationCheckpoints.rebindExact({ `
- L753 — passed where (() => void) | undefined is expected — that caller reads the value, not a promise
  `() => { this.store.observationCheckpoints.save(outcome.checkpoint) if `

### apps/server/src/modules/sessions/view.ts (2)
- L312 — passed where (() => void) | PromiseLike<(() => void) | undefined> | undefined is expected — that caller reads the value, not a promise
  `() => { session.refLetter = this.ports.store.issues.allocateSessionLetter(birthI`
- L320 — passed where (() => void) | PromiseLike<(() => void) | undefined> | undefined is expected — that caller reads the value, not a promise
  `() => { session.refDraft = this.ports.store.repos.nextDraftSeq(repoId) }`

### apps/server/src/modules/sessions/session-revival.ts (2)
- L248 — passed where () => HandoffRepo[] is expected — that caller reads the value, not a promise
  `() => this.ports.store.repos.listRepos()`
- L266 — passed where (event: { ts: string; kind: string; subject: string; payload: unknown; }) => void is expected — that caller reads the value, not a promise
  `(event) => { this.ports.store.events.appendEvent(event) }`

### apps/server/src/modules/sessions/session-teardown.ts (2)
- L338 — passed where (() => void) | undefined is expected — that caller reads the value, not a promise
  `() => this.ports.store.observationCheckpoints.cancelTerminalCandidate(input.sessionId)`
- L571 — passed where (() => void) | undefined is expected — that caller reads the value, not a promise
  `() => { const currentLease = this.ports.store.observationCheckpoints.get(ses`

### apps/server/src/modules/messages/mailbox.ts (2)
- L424 — argument of .flatMap() — the caller consumes the value synchronously
  `(p) => this.deps.messages.listMessagesFor(p, { limit: opts?.limit ?? 50 })`
- L446 — argument of .map() — the caller consumes the value synchronously
  `(m) => { // Per-READER receipt first [POD-1379]: this session has now been shown the`

### apps/server/src/modules/accounts/registry.ts (2)
- L59 — passed where AccountHandler<{ kind: "api-key" | "oauth"; provider: "openrouter" | "anthropic" | "openai"; credential: string; }, unknown> is expected — that caller reads the value, not a promise
  `(state, input) => { // A Claude setup-token is its own account, distinct from an Ant`
- L79 — passed where AccountHandler<{ id: string & BRAND<"AccountId">; }, unknown> is expected — that caller reads the value, not a promise
  `(state, input) => { state.accounts.remove(input.id) return { ok: true as const`

### apps/server/src/modules/maintenance/service.ts (2)
- L118 — passed where () => { status: "busy"; retryAt: any; fencingToken?: undefined; expiresAt?: undefined; messageWaitTtlMs?: undefined; autoArchiveReadWindowMs?: undefined; eventRetentionMaxAgeDays?: undefined; ... 5 more ...; worktreeGcAfterDays?: undefined; } | { ...; } is expected — that caller reads the value, not a promise
  `() => { const nowMs = this.now() const now = new Date(nowMs).toISOString() `
- L461 — passed where () => { status: "applied"; jobKind: "message-expiry" | "event-log-prune" | "change-log-prune" | "maintenance-commands-prune" | "issue-auto-archive" | "session-auto-archive" | "automation-fire" | "steward-poll" | "connect-scan" | "worktree-gc"; runKey: string; deleted?: number | undefined; } | { ...; } | { ...; } is expected — that caller reads the value, not a promise
  `() => { const nowMs = this.now() const lease = this.store.maintenance.getLease`

### apps/server/src/gateway/feed-test-plumbing.ts (2)
- L100 — passed where () => number | null is expected — that caller reads the value, not a promise
  `() => store.sync.minChangeSeq()`
- L102 — passed where (() => number) | undefined is expected — that caller reads the value, not a promise
  `() => store.grants.visibilityRevision()`

### apps/server/src/store/issues.ts (1)
- L1191 — argument of .map() — the caller consumes the value synchronously
  `(r) => this.mapIssueMessage(r)`

### apps/server/src/store/observation-checkpoints.ts (1)
- L193 — argument of .map() — the caller consumes the value synchronously
  `(row) => this.mapRow(row)`

### apps/server/src/modules/issues/service/core.ts (1)
- L1261 — argument of .map() — the caller consumes the value synchronously
  `(event) => this.deps.store.events.appendEvent( { ts: t`

### apps/server/src/modules/issues/service/attention.ts (1)
- L287 — argument of .filter() — the caller consumes the value synchronously
  `(row) => row.id !== anchor.id && row.repoId === anchor.repoId && `

### apps/server/src/modules/issues/service/mail.ts (1)
- L43 — passed where (() => void) | undefined is expected — that caller reads the value, not a promise
  `() => this.store.deps.store.issues.addIssueComment({ id: `cmt_${randomUUID()`

### apps/server/src/modules/issues/service/assistant.ts (1)
- L102 — passed where (provider: string) => string | undefined is expected — that caller reads the value, not a promise
  `(provider) => this.store.d.store.secrets.apiKeyFor(provider)`

### apps/server/src/modules/automations/service.ts (1)
- L461 — passed where (result: { runId: string & BRAND<"AutomationRunId">; automation: AutomationRow; }) => EntityChangeSpec[] is expected — that caller reads the value, not a promise
  `(result) => [ { entity: 'automationRun', id: result.runI`

### apps/server/src/modules/workflows/service.ts (1)
- L455 — argument of .map() — the caller consumes the value synchronously
  `(step) => ({ ...step, profile: step.executionProfileId ? this.de`

### apps/server/src/store/users.ts (1)
- L144 — argument of .flatMap() — the caller consumes the value synchronously
  `(row) => { const account = this.get(row.id) return account ? [account] : [] `

### apps/server/src/store/superagent.ts (1)
- L163 — argument of .map() — the caller consumes the value synchronously
  `(r) => this.mapSuperagentThread(r)`

## illegal-await-context (4)

### apps/server/src/modules/shipping/service.ts (2)
- L3154 — no await possible here (parameter default, accessor or constructor)
  `this.deps.repository.latestStepForEffect(
      attempt.id,
      this.effectKeyFor(attemp`
- L3703 — no await possible here (parameter default, accessor or constructor)
  `this.deps.repository.listOrders()`

### apps/server/src/relay.ts (1)
- L897 — no await possible here (parameter default, accessor or constructor)
  `this.store.events.onAppend((id, event) => issueEventFeed?.publish(id, event))`

### apps/server/src/modules/sessions/launch-config.ts (1)
- L107 — no await possible here (parameter default, accessor or constructor)
  `this.ports.store.settings.getSettingsFor(this.ports.settingsViewer())`
