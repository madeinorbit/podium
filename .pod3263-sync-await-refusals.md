# Await pass refusals — 19 sites

Generated by `bun scripts/awaitify.ts --pass=awaits
--keep-sync=scripts/awaitify-keep-sync.txt --report=<path>`. Line numbers are
pinned to the commit it was generated at and go stale as tests move; the
CATEGORIES do not. Regenerate rather than hand-edit.

## would-change-what-the-caller-reads (8)

### packages/sync/src/replica/replica.test.ts (4)
- L557 — passed where (h: Harness) => void is expected — that caller reads the value, not a promise
  `(h) => void h.replica.receive({ kind: 'rescope', feedId: FEED_ID, epoch: EPOCH })`
- L562 — passed where (h: Harness) => void is expected — that caller reads the value, not a promise
  `(h) => void h.replica.receive({ kind: 'resync-required', feedId: FEED_ID, epoch: E`
- L568 — passed where (h: Harness) => void is expected — that caller reads the value, not a promise
  `(h) => void h.replica.receive(deltaFrame(10, 11, [session(11, 'x', 'y')], { epoch:`
- L574 — passed where (h: Harness) => void is expected — that caller reads the value, not a promise
  `(h) => void h.replica.receive(deltaFrame(10, 14, [session(99, 'x', 'y')]))`

### packages/sync/src/mirror.test.ts (2)
- L302 — passed where () => void is expected — that caller reads the value, not a promise
  `() => { pausePromise ??= mirror.pause() }`
- L466 — passed where () => void is expected — that caller reads the value, not a promise
  `() => { replaceDuringWrite = () => {} fs.replace(path, replacement) stor`

### packages/sync/src/outbox/outbox.ts (1)
- L588 — argument of .map() — the caller consumes the value synchronously
  `(bucket) => this.drainPartition(bucket)`

### packages/sync/src/authority/authority.test.ts (1)
- L337 — passed where (changes: readonly { entity: "session" | "issue" | "issueProjection" | "issueDep" | "repo" | "shipOrder" | "conversation" | "automation" | "automationRun" | "userLayout" | "userReadPosition" | "issueEvent" | "pendingInteraction"; ... 6 more ...; mutationId?: (string & BRAND<...>) | undefined; }[]) => void is expected — that caller reads the value, not a promise
  `() => { if (reentered) return reentered = true authority.commit({ write:`

## assertion-must-change-at-the-flip (8)

### packages/sync/src/replica/replica.test.ts (4)
- L557 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `h.replica.receive({ kind: 'rescope', feedId: FEED_ID, epoch: EPOCH })`
- L563 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `h.replica.receive({ kind: 'resync-required', feedId: FEED_ID, epoch: EPOCH })`
- L569 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `h.replica.receive(deltaFrame(10, 11, [session(11, 'x', 'y')], { epoch: 'epoch-2' }))`
- L574 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `h.replica.receive(deltaFrame(10, 14, [session(99, 'x', 'y')]))`

### packages/sync/src/mirror.test.ts (2)
- L303 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `mirror.pause()`
- L470 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `mirror.enqueueDirty(M1)`

### packages/sync/src/authority/authority.test.ts (1)
- L340 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `authority.commit({ write: async () => 'ok', changes: () => [upsert('s2', { b: 1 })] })`

### packages/sync/src/outbox/outbox.ts (1)
- L588 — reached from a synchronous-only context; at the flip this call becomes a promise and the assertion around it has to change
  `this.drainPartition(bucket)`

## illegal-await-context (3)

### packages/sync/src/conformance/authority.ts (2)
- L420 — no await possible here (parameter default, accessor or constructor)
  `this.identity.current()`
- L424 — no await possible here (parameter default, accessor or constructor)
  `this.identity.current()`

### packages/sync/src/authority/authority.ts (1)
- L217 — no await possible here (parameter default, accessor or constructor)
  `this.baseline.seed(deps.store)`
