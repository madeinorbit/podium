{
  "fixture": "customer-upgrade",
  "source": "sanitised ~/podium-incidents/2026-09-14-pdm52-quarantine (ludovico dev.139 -> dev.140, 2026-09-14)",
  "identityMap": "one deterministic map: every real uuid became a name; user:sole kept because it is the literal under test; paths under /fixture; timestamps preserved",
  "customerRelease": { "tag": "v0.1.1-edge.4", "commit": "d09e5d47a4c3200c2ded231269d91ff0d1d90cd1", "migrations": "server-release-migrations.json (the exact 87 the release shipped; seven later-merged migrations carry earlier timestamps and are NOT in it)" },
  "oldestSupportedDaemon": { "tag": "v0.1.0", "wireVersion": 2 },
  "retiredPrincipal": "user:sole",
  "hostMachineId": "machine-host",
  "machines": [
    { "id": "machine-host", "hostname": "host", "installKind": "supervisor", "inLedger": true, "note": "the server's own machine (ludovico)" },
    { "id": "machine-laptop-live", "hostname": "laptop", "installKind": "supervisor", "inLedger": true, "note": "live laptop row; duplicate hostname of two stale rows" },
    { "id": "machine-laptop-stale-1", "hostname": "laptop", "installKind": null, "inLedger": true, "note": "stale duplicate; handoff target of the exported binding" },
    { "id": "machine-laptop-stale-2", "hostname": "laptop-lan", "installKind": null, "inLedger": true, "note": "stale duplicate" },
    { "id": "machine-peer", "hostname": "peer", "installKind": "legacy-daemon", "inLedger": false, "note": "flatblock: DB-only row, two stray sessions placed here" },
    { "id": "machine-laptop-dev", "hostname": "laptop", "installKind": null, "inLedger": false, "note": "DB-only row" }
  ],
  "daemon": {
    "machineId": "machine-host",
    "bindings": {
      "session-healthy": "bound on machine-host; server places it on machine-host",
      "session-moved-peer-a": "bound on machine-host; server places it on machine-peer (handoff residue)",
      "session-moved-peer-b": "bound on machine-host; server places it on machine-peer (handoff residue)",
      "session-exported-stale": "exported from machine-host to machine-laptop-stale-1; server places it there"
    },
    "receiptOnly": { "session-deleted": "codex receipt for a session the server no longer has" },
    "inertResidue": "inert.json.123.dead.tmp (interrupted atomic write, POD-3969: not a binding)",
    "expectedQuarantined": ["session-deleted", "session-exported-stale", "session-moved-peer-a", "session-moved-peer-b"]
  },
  "client": {
    "basePrefix": "podium.replica",
    "oldNamespacePrefix": "podium.replica.principal.user%3Asole",
    "successorPrincipalNote": "the member id the retire migration mints; read from the upgraded database, never hard-coded"
  }
}
