{
  "//": "Extends node.json (not base.json) purely so the golden-fixture TESTS in src can read their recorded payloads with node:fs. The SHIPPED code is browser-safe and imports nothing from node — tsup bundles src/index.ts only, and the architecture manifest tags this package browser-safe. Same choice, for the same reason, as packages/domain (also browser-safe, also L0) — which matters twice over here, because ADR 8 D4 folds domain into this package, and a shared tsconfig makes that merge a no-op. Consequence to know (POD-1124): types: ['node'] means THIS typecheck cannot catch an ambient node/DOM global creeping into the shipped source. The guard is packages/protocol, which resolves this package's SOURCE through the @podium/source condition under the lean base.json lib — it is what caught `new URLSearchParams(...)` here, and it reddens if one returns.",
  "extends": "../../tooling/tsconfig/node.json",
  "compilerOptions": {
    "outDir": "dist",
    "rootDir": "src",
    "ignoreDeprecations": "6.0"
  },
  "include": ["src"]
}
