{
  "//": "base.json — the lean lib. This package resolves @podium/model's SOURCE through the @podium/source condition (POD-300), so model files are checked under THIS config. It was widened to dom.json only for the URLSearchParams global in model/src/predicates/mobile-entry.ts, which POD-1124 removed. See packages/protocol/tsconfig.json for the same note.",
  "extends": "../../tooling/tsconfig/base.json",
  "compilerOptions": {
    "outDir": "dist",
    "rootDir": "src",
    "ignoreDeprecations": "6.0"
  },
  "include": ["src"]
}
