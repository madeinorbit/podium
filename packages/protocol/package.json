{
  "name": "@podium/protocol",
  "version": "0.0.0",
  "private": true,
  "description": "Wire protocol types for Podium agent/terminal sessions (output frames, input, resize, takeover, transcript).",
  "license": "Apache-2.0",
  "type": "module",
  "main": "./dist/index.js",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "@podium/source": "./src/index.ts",
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    },
    "./daemon": {
      "@podium/source": "./src/daemon.ts",
      "types": "./dist/daemon.d.ts",
      "import": "./dist/daemon.js"
    },
    "./update-refusal": {
      "@podium/source": "./src/update/refusal.ts",
      "types": "./dist/update/refusal.d.ts",
      "import": "./dist/update/refusal.js"
    }
  },
  "files": [
    "dist"
  ],
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "typecheck": "tsgo --noEmit",
    "typecheck:tsc": "tsc --noEmit",
    "test": "bun ../../scripts/validation-admission.ts focused --label @podium/protocol:test -- bun --bun ../../node_modules/vitest/vitest.mjs run --config vitest.config.ts --passWithNoTests",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@podium/model": "workspace:*",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "tsup": "^8.5.1",
    "typescript": "^6.0.3"
  }
}
